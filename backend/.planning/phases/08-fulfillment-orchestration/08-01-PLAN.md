---
phase: 08-fulfillment-orchestration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Domain/Checkout/DeliveryInstruction.cs
  - src/Domain/Checkout/DeliveryStatus.cs
  - src/Infrastructure/Persistence/Configurations/DeliveryInstructionConfiguration.cs
  - src/Infrastructure/Persistence/AppDbContext.cs
autonomous: true
requirements:
  - FUL-01
  - FUL-02

must_haves:
  truths:
    - "Delivery status is persisted per order item"
    - "Status can be Pending, Completed, or Failed"
    - "Completion timestamp is recorded when status becomes Completed"
    - "Failure captures reason for admin review"
  artifacts:
    - path: "src/Domain/Checkout/DeliveryStatus.cs"
      provides: "Delivery status enum"
      contains: "Pending, Completed, Failed"
    - path: "src/Domain/Checkout/DeliveryInstruction.cs"
      provides: "Delivery status + timestamp fields"
      contains: "DeliveryStatus, CompletedAtUtc, FailureReason"
  key_links:
    - from: "DeliveryInstruction"
      to: "Order aggregate"
      via: "Navigation property"
      pattern: "DeliveryInstructions.*DeliveryStatus"
---

<objective>
Add delivery status model to track fulfillment state per order item. Creates DeliveryStatus enum and extends DeliveryInstruction with status, completion timestamp, and failure reason fields.

Purpose: Foundation for tracking delivery progress and enabling automated/manual completion workflows.
Output: Updated DeliveryInstruction entity with status tracking capabilities.
</objective>

<execution_context>
@/home/natan/workspace/Site-Tibia/.opencode/get-shit-done/workflows/execute-plan.md
@/home/natan/workspace/Site-Tibia/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-fulfillment-orchestration/08-CONTEXT.md
@.planning/phases/08-fulfillment-orchestration/08-RESEARCH.md

# Existing code patterns
@src/Domain/Checkout/FulfillmentType.cs
@src/Domain/Checkout/DeliveryInstruction.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DeliveryStatus enum</name>
  <files>src/Domain/Checkout/DeliveryStatus.cs</files>
  <read_first>
    - src/Domain/Checkout/FulfillmentType.cs (existing enum pattern)
  </read_first>
  <action>
Create DeliveryStatus enum in Domain/Checkout:
```csharp
namespace Domain.Checkout;

public enum DeliveryStatus
{
    Pending = 0,
    Completed = 1,
    Failed = 2
}
```
Follow the same pattern as FulfillmentType.cs (simple enum, namespace, public).
  </action>
  <verify>
    <automated>ls src/Domain/Checkout/DeliveryStatus.cs && grep -q "Pending\|Completed\|Failed" src/Domain/Checkout/DeliveryStatus.cs</automated>
  </verify>
  <done>DeliveryStatus enum exists with Pending, Completed, Failed values</done>
</task>

<task type="auto">
  <name>Task 2: Extend DeliveryInstruction with status fields</name>
  <files>src/Domain/Checkout/DeliveryInstruction.cs</files>
  <read_first>
    - src/Domain/Checkout/DeliveryInstruction.cs (current state)
  </read_first>
  <action>
Add to DeliveryInstruction class:
1. Add properties after existing fields:
   - `public DeliveryStatus Status { get; private set; } = DeliveryStatus.Pending;`
   - `public DateTime? CompletedAtUtc { get; private set; }`
   - `public string? FailureReason { get; private set; }`

2. Add factory methods for Completed:
   - Create a private `MarkCompleted(DateTime completedAtUtc)` method that sets Status=Completed, CompletedAtUtc=completedAtUtc, clears FailureReason
   - Create a private `MarkFailed(string reason)` method that sets Status=Failed, FailureReason=reason, clears CompletedAtUtc

3. Add public completion methods accessible to Application layer:
   - `public void Complete() => MarkCompleted(DateTime.UtcNow);`
   - `public void Fail(string reason) => MarkFailed(reason);`

4. Update CreateAutomated factory to initialize Status=Pending
5. Update CreateManual factory to initialize Status=Pending
  </action>
  <verify>
    <automated>grep -q "DeliveryStatus" src/Domain/Checkout/DeliveryInstruction.cs && grep -q "CompletedAtUtc" src/Domain/Checkout/DeliveryInstruction.cs && grep -q "FailureReason" src/Domain/Checkout/DeliveryInstruction.cs</automated>
  </verify>
  <done>DeliveryInstruction has Status, CompletedAtUtc, FailureReason properties with Complete() and Fail() methods</done>
</task>

<task type="auto">
  <name>Task 3: Update EF configuration for new fields</name>
  <files>src/Infrastructure/Persistence/Configurations/DeliveryInstructionConfiguration.cs</files>
  <read_first>
    - src/Infrastructure/Persistence/Configurations/DeliveryInstructionConfiguration.cs
    - src/Domain/Checkout/DeliveryInstruction.cs
  </read_first>
  <action>
Update DeliveryInstructionConfiguration:
1. Add Status property configuration:
   - `builder.Property(x => x.Status).HasConversion<int>().HasDefaultValue(DeliveryStatus.Pending);`

2. Add CompletedAtUtc configuration:
   - `builder.Property(x => x.CompletedAtUtc).HasColumnName("completed_at_utc");`

3. Add FailureReason configuration:
   - `builder.Property(x => x.FailureReason).HasMaxLength(500).HasColumnName("failure_reason");`

4. Add index for efficient status queries:
   - `builder.HasIndex(x => x.Status);`
  </action>
  <verify>
  <automated>grep -q "HasConversion.*int" src/Infrastructure/Persistence/Configurations/DeliveryInstructionConfiguration.cs && grep -q "HasColumnName.*completed_at_utc" src/Infrastructure/Persistence/Configurations/DeliveryInstructionConfiguration.cs</automated>
  </verify>
  <done>EF configuration updated with column mappings and index for delivery status</done>
</task>

<task type="auto">
  <name>Task 4: Generate EF migration</name>
  <files>src/Infrastructure/Persistence/Migrations</files>
  <read_first>
    - src/Infrastructure/Persistence/Migrations (existing migration pattern)
  </read_first>
  <action>
Generate EF migration for new DeliveryInstruction fields:
```bash
cd /home/natan/workspace/Site-Tibia/backend/src/Infrastructure
dotnet ef migrations add AddDeliveryStatus --context AppDbContext --output-dir Persistence/Migrations
```
  </action>
  <verify>
  <automated>ls src/Infrastructure/Persistence/Migrations/*AddDeliveryStatus*.cs 2>/dev/null | head -1</automated>
  </verify>
  <done>Migration file created for delivery status fields</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Application → Domain | Valid completion reasons, status transitions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Repudiation | DeliveryStatus changes | mitigate | CompletedAtUtc timestamp and FailureReason logged for audit |
</threat_model>

<verification>
- [ ] DeliveryStatus enum created with all three states
- [ ] DeliveryInstruction has Status, CompletedAtUtc, FailureReason
- [ ] EF configuration maps new fields correctly
- [ ] Migration generates for new columns
</verification>

<success_criteria>
- DeliveryStatus enum exists at src/Domain/Checkout/DeliveryStatus.cs
- DeliveryInstruction has status tracking properties
- EF configuration updated for new fields
- Migration created successfully
</success_criteria>

<output>
After completion, create `.planning/phases/08-fulfillment-orchestration/08-01-SUMMARY.md`
</output>