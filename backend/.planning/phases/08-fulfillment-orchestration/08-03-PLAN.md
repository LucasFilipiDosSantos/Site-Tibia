---
phase: 08-fulfillment-orchestration
plan: 03
type: execute
wave: 3
depends_on:
  - 08-02
files_modified:
  - src/API/Checkout/CheckoutEndpoints.cs
  - src/API/Checkout/CheckoutDtos.cs
  - src/API/Admin/AdminEndpoints.cs
  - src/API/Auth/AuthPolicies.cs
  - src/Application/Checkout/Services/CheckoutService.cs
autonomous: true
requirements:
  - FUL-03
  - FUL-04

must_haves:
  truths:
    - "Customer sees per-item delivery status in order detail"
    - "Customer sees fulfillment type (Automated/Manual)"
    - "Customer sees CompletedAtUtc timestamp when available"
    - "Admin can force-complete any Pending or Failed delivery"
    - "Admin force-complete adds admin note and sets timestamp"
  artifacts:
    - path: "src/API/Checkout/CheckoutDtos.cs"
      provides: "Delivery info DTOs for customer visibility"
      contains: "DeliveryStatus, FulfillmentType, CompletedAtUtc"
    - path: "src/API/Admin/AdminEndpoints.cs"
      provides: "Admin force-complete endpoint"
      contains: "AdminNote, CompletedAtUtc"
  key_links:
    - from: "CheckoutEndpoints"
      to: "CheckoutDtos"
      via: "response mapping"
      pattern: "DeliveryInstruction.*DeliveryStatus"
    - from: "AdminEndpoints"
      to: "IFulfillmentService"
      via: "admin-only endpoint"
      pattern: "ForceCompleteDelivery"
---

<objective>
Add customer delivery visibility to order detail API and admin correction endpoint for manual fulfillment completion.

Purpose: Customers can track delivery progress per item; admins can complete manual fulfillments.
Output: Updated CheckoutDtos + Admin endpoint for force-complete.
</objective>

<execution_context>
@/home/natan/workspace/Site-Tibia/.opencode/get-shit-done/workflows/execute-plan.md
@/home/natan/workspace/Site-Tibia/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-fulfillment-orchestration/08-CONTEXT.md
@.planning/phases/08-fulfillment-orchestration/08-RESEARCH.md

# Existing code patterns
@src/API/Checkout/CheckoutEndpoints.cs
@src/API/Checkout/CheckoutDtos.cs
@src/API/Admin/AdminEndpoints.cs
@src/API/Auth/AuthPolicies.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend CheckoutDtos with delivery status</name>
  <files>src/API/Checkout/CheckoutDtos.cs</files>
  <read_first>
    - src/API/Checkout/CheckoutDtos.cs (current response DTOs)
  </read_first>
  <action>
Add to CheckoutDeliveryInstructionResponseDto (or create new one):
1. Add DeliveryStatus Status property
2. Add FulfillmentType FulfillmentType property (already has this as source)
3. Add DateTime? CompletedAtUtc property
4. Ensure existing mapping in CheckoutService includes these new fields

Per D-13, D-14: Customer sees per-item delivery status, fulfillment type, and completion timestamp.
  </action>
  <verify>
  <automated>grep -q "DeliveryStatus" src/API/Checkout/CheckoutDtos.cs && grep -q "CompletedAtUtc" src/API/Checkout/CheckoutDtos.cs</automated>
  </verify>
  <done>CheckoutDtos include delivery status fields</done>
</task>

<task type="auto">
  <name>Task 2: Update CheckoutService response mapping</name>
  <files>src/Application/Checkout/Services/CheckoutService.cs</files>
  <read_first>
    - src/Application/Checkout/Services/CheckoutService.cs (line 78-120, response mapping)
  </read_first>
  <action>
In CheckoutService, find where DeliveryInstructionResponse is created and add Status/CompletedAtUtc:
1. Add using for Domain.Checkout.DeliveryStatus
2. In the mapping from DeliveryInstruction to CheckoutDeliveryInstructionResponse:
   - Add Status = instruction.Status
   - Add CompletedAtUtc = instruction.CompletedAtUtc

This adds delivery visibility to the existing order detail endpoint.
  </action>
  <verify>
  <automated>grep -q "\.Status" src/Application/Checkout/Services/CheckoutService.cs</automated>
  </verify>
  <done>CheckoutService maps delivery status to response</done>
</task>

<task type="auto">
  <name>Task 3: Create ForceCompleteDelivery endpoint</name>
  <files>src/API/Admin/AdminEndpoints.cs</files>
  <read_first>
    - src/API/Admin/AdminEndpoints.cs (existing admin patterns)
    - src/API/Auth/AuthPolicies.cs (admin-only policy)
  </read_first>
  <action>
Add to AdminEndpoints.cs:
1. Add request DTO:
```csharp
public sealed record ForceCompleteDeliveryDto(
    Guid OrderId,
    Guid ProductId,
    string AdminNote
);
```

2. Add endpoint:
```csharp
adminGroup.MapPost("/orders/deliveries/complete", async (
    ForceCompleteDeliveryDto request,
    IAdminFulfillmentService service,
    CancellationToken ct) =>
{
    await service.ForceCompleteAsync(request.OrderId, request.ProductId, request.AdminNote, ct);
    return Results.Ok();
})
.WithName("ForceCompleteDelivery")
.WithTags("Admin");
```

3. Require AdminOnly policy on this endpoint

Per D-10, D-11: Admin can force-complete any Pending or Failed delivery, adds admin note, sets CompletedAtUtc.
  </action>
  <verify>
  <automated>grep -q "ForceComplete" src/API/Admin/AdminEndpoints.cs</automated>
  </verify>
  <done>Admin force-complete endpoint exists</done>
</task>

<task type="auto">
  <name>Task 4: Create AdminFulfillmentService</name>
  <files>src/Application/Checkout/Services/AdminFulfillmentService.cs</files>
  <read_first>
    - src/Application/Checkout/Services/CheckoutService.cs (repository pattern)
  </read_first>
  <action>
Create AdminFulfillmentService.cs:
1. Implement IAdminFulfillmentService interface
2. ForceCompleteAsync method:
   - Load order with DeliveryInstructions (Include)
   - Find specific DeliveryInstruction by ProductId
   - If status is Pending or Failed:
     - Call Complete() on instruction
     - Optionally record admin note in separate field (or ignore per D-11 simplicity)
   - Save changes
   - Append timeline event for delivery completion (per D-15)
3. Add admin authorization check

Per D-10, D-11, D-12: Admin-only action, sets CompletedAtUtc, appends timeline.
  </action>
  <verify>
  <automated>ls src/Application/Checkout/Services/AdminFulfillmentService.cs</automated>
  </verify>
  <done>AdminFulfillmentService implements force-complete</done>
</task>

<task type="auto">
  <name>Task 5: Register AdminFulfillmentService and add timeline event</name>
  <files>src/Infrastructure/DependencyInjection.cs</files>
  <read_first>
    - src/Infrastructure/DependencyInjection.cs
  </read_first>
  <action>
1. Add to DependencyInjection.cs:
```csharp
services.TryAddScoped<IAdminFulfillmentService, AdminFulfillmentService>();
```

2. Ensure OrderLifecycleRepository has AppendTimelineEvent method (from Phase 5)
3. In AdminFulfillmentService, call AppendTimelineEvent with DeliveryCompleted status
  </action>
  <verify>
  <automated>grep -q "IAdminFulfillmentService" src/Infrastructure/DependencyInjection.cs</automated>
  </verify>
  <done>AdminFulfillmentService registered in DI</done>
</task>

<task type="auto">
  <name>Task 6: Build verification</name>
  <files>src</files>
  <read_first>
  </read_first>
  <action>
Run build to verify all new code compiles:
```bash
cd /home/natan/workspace/Site-Tibia/backend/src/API && dotnet build --no-restore
```
  </action>
  <verify>
  <automated>cd /home/natan/workspace/Site-Tibia/backend/src/API && dotnet build --no-restore 2>&1 | tail -10</automated>
  </verify>
  <done>Build passes, all new code compiles</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin → Delivery Status | Only admins can force-complete |
| API → Domain | Validates status transitions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Authorization | Non-admin force-complete | mitigate | Require AdminOnly policy on endpoint |
| T-08-05 | Tampering | AdminNote injection | mitigate | Validate AdminNote length (max 500) |
</threat_model>

<verification>
- [ ] CheckoutDtos include delivery status for customers
- [ ] Admin force-complete endpoint exists with AdminOnly
- [ ] AdminFulfillmentService implements force-complete logic
- [ ] Timeline event appended on admin completion
- [ ] Build passes
</verification>

<success_criteria>
- Customer order detail shows delivery status + type + timestamp
- Admin endpoint allows force-complete with note
- Timeline events track delivery changes
- Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-fulfillment-orchestration/08-03-SUMMARY.md`
</output>