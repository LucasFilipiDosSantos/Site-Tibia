---
phase: 08-fulfillment-orchestration
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/Application/Checkout/Services/OrderLifecycleService.cs
  - src/Application/Checkout/Services/FulfillmentService.cs
  - src/Application/Checkout/Contracts/OrderLifecycleContracts.cs
  - src/Application/Checkout/Contracts/FulfillmentContracts.cs
autonomous: true
requirements:
  - FUL-01
  - FUL-02

must_haves:
  truths:
    - "Automated fulfillment completes items immediately on payment"
    - "Fulfillment routing happens within same transaction as Paid transition"
    - "Hangfire job handles routing failure with retry"
    - "Digital goods (gold, items, Tibia Coins) complete automatically"
  artifacts:
    - path: "src/Application/Checkout/Services/FulfillmentService.cs"
      provides: "Fulfillment routing logic"
      exports: ["RouteFulfillmentAsync"]
    - path: "src/Application/Checkout/Services/OrderLifecycleService.cs"
      provides: "Calls fulfillment routing after Paid transition"
  key_links:
    - from: "OrderLifecycleService"
      to: "FulfillmentService"
      via: "dependency injection"
      pattern: "IFulfillmentService.RouteFulfillmentAsync"
    - from: "PaymentWebhookProcessor"
      to: "OrderLifecycleService"
      via: "ApplySystemTransitionAsync"
      pattern: "OrderLifecycleService.*Paid"
---

<objective>
Implement fulfillment routing triggered at payment confirmation. FulfillmentService routes each paid order item to automated or manual path, marks automated items complete immediately, and integrates with OrderLifecycleService after Paid transition.

Purpose: Routes paid orders to delivery workflows based on product type; automated for digital goods.
Output: FulfillmentService + integration with OrderLifecycleService.
</objective>

<execution_context>
@/home/natan/workspace/Site-Tibia/.opencode/get-shit-done/workflows/execute-plan.md
@/home/natan/workspace/Site-Tibia/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-fulfillment-orchestration/08-CONTEXT.md
@.planning/phases/08-fulfillment-orchestration/08-RESEARCH.md
@.planning/phases/06-mercado-pago-payment-confirmation/06-CONTEXT.md

# Existing code patterns
@src/Application/Checkout/Services/OrderLifecycleService.cs
@src/Application/Checkout/Contracts/OrderLifecycleContracts.cs
@src/Domain/Checkout/Order.cs
@src/Domain/Checkout/DeliveryInstruction.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FulfillmentService interface and contracts</name>
  <files>src/Application/Checkout/Contracts/FulfillmentContracts.cs</files>
  <read_first>
    - src/Application/Checkout/Contracts/OrderLifecycleContracts.cs
  </read_first>
  <action>
Create FulfillmentContracts.cs:
```csharp
namespace Application.Checkout.Contracts;

public interface IFulfillmentService
{
    Task RouteFulfillmentAsync(Guid orderId, CancellationToken ct = default);
}
```
  </action>
  <verify>
  <automated>ls src/Application/Checkout/Contracts/FulfillmentContracts.cs</automated>
  </verify>
  <done>IFulfillmentService contract exists</done>
</task>

<task type="auto">
  <name>Task 2: Implement FulfillmentService</name>
  <files>src/Application/Checkout/Services/FulfillmentService.cs</files>
  <read_first>
    - src/Application/Checkout/Services/CheckoutService.cs (repository pattern)
    - src/Domain/Checkout/Order.cs
    - src/Domain/Checkout/DeliveryInstruction.cs
    - src/Domain/Checkout/FulfillmentType.cs
  </read_first>
  <action>
Create FulfillmentService.cs:
1. Constructor injects IOrderRepository and IOrderLifecycleRepository
2. RouteFulfillmentAsync method:
   - Load order with DeliveryInstructions (Include)
   - For each DeliveryInstruction:
     - If FulfillmentType.Automated: call Complete() on instruction
     - If FulfillmentType.Manual: leave as Pending (admin completes later)
   - Save changes
3. Add using for DeliveryStatus enum

The key logic per D-07, D-08:
- Automated fulfillment marks items Completed immediately
- Manual fulfillment waits for admin force-complete
  </action>
  <verify>
  <automated>ls src/Application/Checkout/Services/FulfillmentService.cs && grep -q "RouteFulfillmentAsync" src/Application/Checkout/Services/FulfillmentService.cs</automated>
  </verify>
  <done>FulfillmentService implements IFulfillmentService with routing logic</done>
</task>

<task type="auto">
  <name>Task 3: Integrate fulfillment routing in OrderLifecycleService</name>
  <files>src/Application/Checkout/Services/OrderLifecycleService.cs</files>
  <read_first>
    - src/Application/Checkout/Services/OrderLifecycleService.cs
    - src/Domain/Checkout/Order.cs
  </read_first>
  <action>
Modify OrderLifecycleService.cs:
1. Add IFulfillmentService to constructor parameters
2. In ApplySystemTransitionAsync, after successful Paid transition (after line that calls _lifecycleRepository.PersistTransitionAsync):
   - Call await _fulfillmentService.RouteFulfillmentAsync(orderId, ct);
   
Key per D-04, D-05: Routing happens within same transaction scope as Paid transition. If routing fails, the whole job fails with Hangfire retry (not separate scheduling).
  </action>
  <verify>
  <automated>grep -q "IFulfillmentService" src/Application/Checkout/Services/OrderLifecycleService.cs && grep -q "RouteFulfillmentAsync" src/Application/Checkout/Services/OrderLifecycleService.cs</automated>
  </verify>
  <done>Fulfillment routing called after Paid transition in OrderLifecycleService</done>
</task>

<task type="auto">
  <name>Task 4: Register FulfillmentService in DI</name>
  <files>src/Infrastructure/DependencyInjection.cs</files>
  <read_first>
    - src/Infrastructure/DependencyInjection.cs
  </read_first>
  <action>
Add to Infrastructure/DependencyInjection.cs:
```csharp
services.TryAddScoped<IFulfillmentService, FulfillmentService>();
```
Find similar service registrations (e.g., ICheckoutService) and add in same section.
  </action>
  <verify>
  <automated>grep -q "IFulfillmentService" src/Infrastructure/DependencyInjection.cs</automated>
  </verify>
  <done>FulfillmentService registered in DI container</done>
</task>

<task type="auto">
  <name>Task 5: Verify build and existing tests</name>
  <files>src</files>
  <read_first>
  </read_first>
  <action>
Run build to verify compilation:
```bash
cd /home/natan/workspace/Site-Tibia/backend/src/API && dotnet build --no-restore
```

Run existing tests to ensure no regressions:
```bash
cd /home/natan/workspace/Site-Tibia/backend && dotnet test --no-build --filter "FullyQualifiedName~Tests.Unit" 2>/dev/null || true
```
  </action>
  <verify>
  <automated>cd /home/natan/workspace/Site-Tibia/backend/src/API && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>Build passes, no regressions in existing tests</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Payment confirmation → Fulfillment | Order must be Paid before routing |
| Application → Domain | Status transitions validated by domain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02 | Elevation | Fulfillment routing on unpaid orders | mitigate | Only route after verified Paid transition in same transaction |
| T-08-03 | Information | Delivery details exposed to wrong user | mitigate | Order must be in user's order collection or admin check |
</threat_model>

<verification>
- [ ] FulfillmentService created with routing logic
- [ ] OrderLifecycleService calls fulfillment after Paid
- [ ] DI registration complete
- [ ] Build passes without errors
</verification>

<success_criteria>
- IFulfillmentService implemented
- Automated items marked Completed on payment
- Routing within same transaction scope as Paid
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/08-fulfillment-orchestration/08-02-SUMMARY.md`
</output>