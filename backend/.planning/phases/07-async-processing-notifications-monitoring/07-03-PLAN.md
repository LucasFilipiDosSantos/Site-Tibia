---
phase: 07-async-processing-notifications-monitoring
plan: 03
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - src/Application/Checkout/Services/OrderLifecycleService.cs
  - src/Application/Notifications/NotificationJobs.cs
  - src/API/Jobs/NotificationJobEndpoints.cs
autonomous: false
requirements:
  - NTF-01
  - NTF-02
  - NTF-03
  - REL-01

must_haves:
  truths:
    - "Order lifecycle events trigger WhatsApp notifications"
    - "Failed notifications retry with exponential backoff"
    - "Structured logs expose notification outcomes"
  artifacts:
    - path: "src/Application/Notifications/NotificationJobs.cs"
      provides: "Background job classes for notification dispatch"
      exports: ["OrderNotificationJob", "PaymentNotificationJob"]
    - path: "src/API/Jobs/NotificationJobEndpoints.cs"
      provides: "Manual trigger endpoints for testing/retry"
      exports: ["TriggerOrderNotification", "TriggerPaymentNotification"]
  key_links:
    - from: "OrderLifecycleService"
      to: "IBackgroundJobClient"
      via: "Enqueue<>() notification job"
    - from: "NotificationJobs"
      to: "IWhatsAppNotificationService"
      via: "DI in job execution context"
---

<objective>
Wire lifecycle events (order created, payment approved, delivery status) to background notification jobs with retry logic and structured logging.

Purpose: Notifications triggered on order/payment/delivery lifecycle events
Output: Job classes wired to lifecycle service, manual retry endpoints
</objective>

<execution_context>
@/home/natan/workspace/Site-Tibia/.opencode/get-shit-done/workflows/execute-plan.md
@/home/natan/workspace/Site-Tibia/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-async-processing-notifications-monitoring/07-CONTEXT.md

# Decision D-02
Exponential backoff: 1min, 5min, 15min, 1hr, 24hr max (configured in Plan 1)

# Decision D-05
Fire-and-forget - enqueue immediately on lifecycle event for fastest processing

# Existing Integration Points
- OrderLifecycleService: Transitions orders through Pending → Paid → ...
- IBackgroundJobClient from PaymentWebhookEndpoints.cs
- IWhatsAppNotificationService from Plan 02
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create notification job classes</name>
  <files>src/Application/Notifications/NotificationJobs.cs</files>
  <action>
Create NotificationJobs.cs in Application/Notifications folder:

1. OrderNotificationJob class:
   - [AutomaticRetry(Attempts = 5, DelaysInSeconds = new[] { 60, 300, 900, 3600, 86400 })]
   - Properties: OrderId, OrderNumber, CustomerPhone, NotificationType
   - ExecuteAsync method:
     - Inject IWhatsAppNotificationService via constructor (Hangfire resolves from DI)
     - Map NotificationType to template name:
       - OrderCreated → "order_created"
       - PaymentApproved → "payment_approved"  
       - DeliveryStarted → "delivery_started"
       - DeliveryCompleted → "delivery_completed"
     - Call IWhatsAppNotificationService.SendTemplateMessageAsync
     - Log success/failure with order ID and message ID

2. PaymentNotificationJob class (for payment events):
   - Same retry attributes
   - Properties: OrderId, PaymentId, CustomerPhone, Status
   - ExecuteAsync: Send payment status notification

3. Add Serilog ILogger to each job for structured logging
  </action>
  <verify>
<automatic>test -f src/Application/Notifications/NotificationJobs.cs && echo "JOBS_EXISTS"</automated>
  </verify>
  <done>Notification job classes with exponential backoff retry (5 attempts: 1min, 5min, 15min, 1hr, 24hr)</done>
</task>

<task type="auto">
<name>Task 2: Modify OrderLifecycleService to enqueue notifications</name>
  <files>src/Application/Checkout/Services/OrderLifecycleService.cs</files>
  <read_first>src/Application/Checkout/Services/OrderLifecycleService.cs</read_first>
  <action>
Modify OrderLifecycleService:

1. Add IBackgroundJobClient to constructor parameters

2. In method that transitions to Paid status:
   ```csharp
   // After successful transition to Paid
   _backgroundJobClient.Enqueue<OrderNotificationJob>(job =>
       job.ExecuteAsync(new OrderNotificationJobArgs
       {
           OrderId = order.Id,
           OrderNumber = order.OrderNumber,
           CustomerPhone = order.CustomerPhone, // Must exist on order
           NotificationType = NotificationType.PaymentApproved
       }, CancellationToken.None));
   ```

3. Add similar calls for:
   - OrderCreated (on checkout completion)
   - DeliveryStarted (on fulfillment initiation - Phase 8)
   - DeliveryCompleted (on fulfillment completion - Phase 8)

4. For each notification enqueue, wrap in try-catch:
   - Log failure but don't fail the lifecycle transition
   - Notification failure shouldn't block order processing
  </action>
  <verify>
<automated>grep -q "OrderNotificationJob" src/Application/Checkout/Services/OrderLifecycleService.cs && echo "WIRED"</automated>
  </verify>
  <done>Lifecycle service enqueues notifications on order state transitions</done>
</task>

<task type="auto">
<name>Task 3: Add manual retry/notification endpoints</name>
  <files>src/API/Jobs/NotificationJobEndpoints.cs</files>
  <action>
Create NotificationJobEndpoints.cs in API/Jobs folder:

1. POST /jobs/notifications/retry/{orderId}
   - Enqueue notification job for specified order
   - Returns job ID
   
2. POST /jobs/notifications/trigger
   - Accepts: { orderId, notificationType }
   - Enqueue appropriate notification job
   
3. Require Admin role for these endpoints (authorize)

4. Add appropriate using statements and DI for job classes
  </action>
  <verify>
<automated>test -f src/API/Jobs/NotificationJobEndpoints.cs && echo "ENDPOINTS_EXISTS"</automated>
  </done>
  <done>Manual notification trigger/retry endpoints for operators</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Event-to-notification wiring (D-05: fire-and-forget, D-02: retry)</what-built>
  <how-to-verify>
    1. Build: dotnet build
    2. Verify lifecycle service references OrderNotificationJob
    3. Confirm retry delays configured (5 attempts with exponential backoff)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|-----------|-------------|
| Phone number data | Customer phone in notification payloads |
| Manual trigger endpoint | Admin-only operation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09 | Spoofing | Fake notification trigger | mitigate | Require Admin authorization on manual endpoints |
| T-07-10 | Information Disclosure | Phone numbers in logs | mitigate | Sanitize phone numbers in structured logs |
| T-07-11 | Denial of Service | Mass notification trigger | mitigate | Rate limit admin notification endpoints |
| T-07-12 | Repudiation | Notification claims | mitigate | Log all enqueue calls with correlation ID |

</threat_model>

<verification>
- [ ] OrderNotificationJob with retry attributes
- [ ] Lifecycle service enqueues on transitions
- [ ] Manual retry endpoints with admin auth
- [ ] Build passes
</verification>

<success_criteria>
- Order lifecycle transitions trigger WhatsApp notifications
- Failed notifications retry automatically (5 attempts, exponential backoff)
- Operators can manually retry notifications via API
- All notification events logged with structured Serilog
</success_criteria>

<output>
After completion, create `.planning/phases/07-async-processing-notifications-monitoring/07-03-SUMMARY.md`
</output>