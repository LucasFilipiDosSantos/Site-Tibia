---
phase: 07-async-processing-notifications-monitoring
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/Infrastructure/Notifications/WhatsAppNotificationService.cs
  - src/Infrastructure/Notifications/WhatsAppOptions.cs
  - src/Application/Notifications/INotificationService.cs
  - src/Application/Notifications/NotificationContracts.cs
autonomous: false
requirements:
  - NTF-01
  - NTF-03

must_haves:
  truths:
    - "WhatsApp notifications sent via Meta Cloud API"
    - "Failed notifications retry with exponential backoff"
    - "Duplicate notifications prevented via deduplication"
  artifacts:
    - path: "src/Infrastructure/Notifications/WhatsAppOptions.cs"
      provides: "Configuration for WhatsApp API credentials"
      exports: ["WhatsAppOptions"]
    - path: "src/Infrastructure/Notifications/WhatsAppNotificationService.cs"
      provides: "WhatsApp template message sending"
      exports: ["IWhatsAppNotificationService", "SendOrderConfirmationAsync"]
    - path: "src/Application/Notifications/INotificationService.cs"
      provides: "Application-level notification interface"
      exports: ["INotificationService"]
  key_links:
    - from: "Hangfire job"
      to: "IWhatsAppNotificationService"
      via: "Dependency injection"
    - from: "WhatsAppNotificationService"
      to: "Meta Cloud API"
      via: "HttpClient POST to graph.facebook.com"
---

<objective>
Create WhatsApp notification service using Meta Cloud API with deduplication and idempotent send operations.

Purpose: Send WhatsApp template messages for order/payment/delivery events
Output: IWhatsAppNotificationService implementation with SendOrderConfirmationAsync
</objective>

<execution_context>
@/home/natan/workspace/Site-Tibia/.opencode/get-shit-done/workflows/execute-plan.md
@/home/natan/workspace/Site-Tibia/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-async-processing-notifications-monitoring/07-CONTEXT.md
@.planning/phases/07-async-processing-notifications-monitoring/07-RESEARCH.md

# Key Decisions from CONTEXT.md
- D-01: Use Meta Cloud API directly for WhatsApp notifications
- D-02: Exponential backoff 1min, 5min, 15min, 1hr, 24hr max

# Existing Patterns
- IBackgroundJobClient from PaymentWebhookEndpoints.cs
- HttpClient configuration in Infrastructure layer
- Serilog for structured logging
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define WhatsApp options configuration</name>
  <files>src/Infrastructure/Notifications/WhatsAppOptions.cs</files>
  <action>
Create WhatsAppOptions.cs in Infrastructure/Notifications folder:

1. WhatsAppOptions class with properties:
   - AccessToken (required) - Meta API access token
   - PhoneNumberId (required) - WhatsApp business phone number ID
   - WhatsAppBusinessId (required) - Business account ID
   - ApiVersion (default: "v25.0")
   - BaseUrl (default: "https://graph.facebook.com")

2. Configuration section name: "WhatsApp"

3. Add IValidateOptions<WhatsAppOptions> for startup validation

4. Add required using statements for Microsoft.Extensions.Options, etc.
  </action>
  <verify>
<automated>test -f src/Infrastructure/Notifications/WhatsAppOptions.cs && echo "OPTIONS_EXISTS"</automated>
  </verify>
  <done>WhatsApp options with validation registered in configuration</done>
</task>

<task type="auto">
  <name>Task 2: Create notification contracts</name>
  <files>src/Application/Notifications/INotificationService.cs</files>
  <action>
Create Application/Notifications/ folder and add:

1. NotificationContracts.cs:
   - NotificationType enum: OrderConfirmation, PaymentApproved, DeliveryStarted, DeliveryCompleted
   - NotificationPayload record with: Type, RecipientPhone, TemplateName, Parameters (dictionary)

2. INotificationService.cs interface:
   - Task SendAsync(NotificationPayload payload, CancellationToken ct)
   - Task<bool> TrySendAsync(NotificationPayload payload, CancellationToken ct) - returns false if already sent
  </action>
  <verify>
<automated>test -f src/Application/Notifications/INotificationService.cs && echo "INTERFACE_EXISTS"</automated>
  </verify>
  <done>Notification service contracts defined in Application layer</done>
</task>

<task type="auto">
  <name>Task 3: Implement WhatsApp notification service</name>
  <files>src/Infrastructure/Notifications/WhatsAppNotificationService.cs</files>
  <read_first>src/Infrastructure/Notifications/WhatsAppOptions.cs</read_first>
  <action>
Create WhatsAppNotificationService.cs:

1. IWhatsAppNotificationService interface:
   - Task SendTemplateMessageAsync(string to, string templateName, string languageCode, Dictionary<string, string> parameters, CancellationToken ct)

2. WhatsAppNotificationService implementation:
   - Inject IOptions<WhatsAppOptions>, HttpClient, ILogger<WhatsAppNotificationService>
   - SendTemplateMessageAsync:
     - Build POST to {BaseUrl}/{ApiVersion}/{PhoneNumberId}/messages
     - Include Authorization: Bearer {AccessToken}
     - Payload: messaging_product=whatsapp, to={to}, type=template, template={...}
     - Handle HttpRequestException with retry logging
     - Return message ID on success
   - Add dedup key generation: SHA256 of (to + templateName + parameters sorted)

3. Use ILogger for structured logging of sends/failures
4. Throw specific exceptions for API errors (not generic)
  </action>
  <verify>
<automated>test -f src/Infrastructure/Notifications/WhatsAppNotificationService.cs && echo "SERVICE_EXISTS"</automated>
  </done>
  <done>WhatsApp service sends template messages via Meta Cloud API</done>
</task>

<task type="auto">
  <name>Task 4: Register WhatsApp services in DI</name>
  <files>src/Infrastructure/DependencyInjection.cs</files>
  <read_first>src/Infrastructure/DependencyInjection.cs</read_first>
  <action>
Add to AddInfrastructure method:

```csharp
// WhatsApp notifications
services.AddOptions<WhatsAppOptions>()
    .Bind(configuration.GetSection(WhatsAppOptions.SectionName))
    .ValidateOnStart();
services.AddSingleton<IValidateOptions<WhatsAppOptions>, WhatsAppOptionsValidator>();
services.AddScoped<IWhatsAppNotificationService, WhatsAppNotificationService>();
```

Add using statements for Infrastructure.Notifications namespace.
  </action>
  <verify>
<automated>grep -q "IWhatsAppNotificationService" src/Infrastructure/DependencyInjection.cs && echo "REGISTERED"</automated>
  </done>
  <done>WhatsApp services registered with configuration validation</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>WhatsApp notification service (D-01: Meta Cloud API)</what-built>
  <how-to-verify>
    1. Build: dotnet build
    2. Verify WhatsAppOptions validates required fields at startup
    3. Confirm IWhatsAppNotificationService registered in DI
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|-----------|-------------|
| API credentials | Access token exposed in logs or config |
| Phone number validation | Invalid recipient causes API errors |
| Webhook callback | No incoming WhatsApp webhooks in this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Information Disclosure | Access token in logs | mitigate | Log sanitization - never log raw access tokens |
| T-07-06 | Denial of Service | Invalid phone number flood | mitigate | Validate phone format before API call |
| T-07-07 | Tampering | Template parameter injection | accept | Parameters treated as literals, not parsed |
| T-07-08 | Repudiation | Message send claims | mitigate | Log message ID and response for audit trail |

</threat_model>

<verification>
- [ ] WhatsAppOptions with validation
- [ ] IWhatsAppNotificationService interface
- [ ] WhatsAppNotificationService implementation
- [ ] DI registration
- [ ] Build passes
</verification>

<success_criteria>
- WhatsApp template messages sent via Meta Cloud API
- Access token and phone number ID configured via appsettings
- Service logs structured information about sends/failures
- Configuration validates required fields at startup
</success_criteria>

<output>
After completion, create `.planning/phases/07-async-processing-notifications-monitoring/07-02-SUMMARY.md`
</output>