---
phase: 07-async-processing-notifications-monitoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Infrastructure/DependencyInjection.cs
  - src/API/Program.cs
  - src/API/Jobs/HangfireDashboardEndpoints.cs
autonomous: false
requirements:
  - REL-01
  - REL-02

must_haves:
  truths:
    - "Background jobs execute durably with PostgreSQL storage"
    - "Operators can monitor job health via dashboard and health checks"
    - "Structured logs expose critical flow outcomes"
  artifacts:
    - path: "src/API/Jobs/HangfireDashboardEndpoints.cs"
      provides: "Hangfire dashboard with authorization"
      min_lines: 30
    - path: "src/Infrastructure/Jobs/HangfireConfiguration.cs"
      provides: "Hangfire PostgreSQL storage + retry policy configuration"
      exports: ["AddHangfireServices"]
    - path: "src/Infrastructure/Jobs/JobMonitoringExtensions.cs"
      provides: "Health check registration for Hangfire"
      exports: ["AddHangfireHealthChecks"]
  key_links:
    - from: "Program.cs"
      to: "HangfireDashboardEndpoints"
      via: "app.MapHangfireDashboard()"
    - from: "DependencyInjection.cs"
      to: "HangfireConfiguration"
      via: "AddHangfireServices"
---

<objective>
Set up Hangfire infrastructure with PostgreSQL storage, dashboard monitoring, and health checks. This establishes the foundation for background job execution.

Purpose: Durable job storage, job monitoring, and health observability
Output: Hangfire configured with PostgreSQL, dashboard protected, health checks registered
</objective>

<execution_context>
@/home/natan/workspace/Site-Tibia/.opencode/get-shit-done/workflows/execute-plan.md
@/home/natan/workspace/Site-Tibia/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-async-processing-notifications-monitoring/07-CONTEXT.md
@.planning/phases/07-async-processing-notifications-monitoring/07-RESEARCH.md

# Interface Context
## IBackgroundJobClient (existing from Phase 6)
```csharp
public interface IBackgroundJobClient
{
    void Enqueue<T>(Expression<Action<T>> methodCall);
}
```

## Existing Job Pattern from PaymentWebhookEndpoints.cs
```csharp
jobClient.Enqueue<PaymentWebhookProcessor>(processor => 
    processor.ProcessAsync(logEntry.Id, ct));
```

## PostgreSQL Connection (existing)
- Connection string from appsettings: "DefaultConnection"
- Database: tibia_webstore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Hangfire packages</name>
  <files>src/API/TibiaWebstore.API.csproj</files>
  <action>
Add the following packages to the API project:
- Hangfire.Core (1.8.x)
- Hangfire.PostgreSql (1.21.x)
- AspNetCore.HealthChecks.Hangfire (9.0.x)

Add PackageReference elements to .csproj with these versions.
  </action>
  <verify>
<automated>grep -q "Hangfire" src/API/TibiaWebstore.API.csproj && echo "PACKAGES_INSTALLED"</automated>
  </verify>
  <done>Hangfire packages installed in API project</done>
</task>

<task type="auto">
  <name>Task 2: Create Hangfire configuration service</name>
  <files>src/Infrastructure/Jobs/HangfireConfiguration.cs</files>
  <action>
Create HangfireConfiguration.cs in Infrastructure/Jobs/ folder with:

1. AddHangfireServices extension method that:
   - Configures GlobalJobFilters with AutomaticRetryAttribute:
     - Attempts = 5
     - DelaysInSeconds = new[] { 60, 300, 900, 3600, 86400 } (1min, 5min, 15min, 1hr, 24hr per D-02)
   - Uses PostgreSqlStorage with connection from configuration
   - Configures PostgreSqlStorageOptions:
     - PrepareSchemaIfNecessary = true
     - AllowDegradedModeWithoutStorage = true
     - QueuePollInterval = TimeSpan.FromSeconds(15)
   - Adds HangfireServer with worker count from config (default: processor count)
   
2. Reference existing IBackgroundJobClient interface (keep in API layer)
   - Do NOT create new IBackgroundJobClient - reuse existing from PaymentWebhookEndpoints.cs

Use Serilog for structured logging (already in stack).
  </action>
  <verify>
<automated>test -f src/Infrastructure/Jobs/HangfireConfiguration.cs && echo "CONFIG_EXISTS"</automated>
  </verify>
  <done>Hangfire configuration created with PostgreSQL storage and exponential backoff retry (5 attempts: 1min, 5min, 15min, 1hr, 24hr)</done>
</task>

<task type="auto">
  <name>Task 3: Register Hangfire in DependencyInjection</name>
  <files>src/Infrastructure/DependencyInjection.cs</files>
  <read_first>src/Infrastructure/DependencyInjection.cs</read_first>
  <action>
Add call to AddHangfireServices in AddInfrastructure method:

```csharp
services.AddHangfireServices(configuration);
```

Add using statement for Infrastructure.Jobs namespace.
  </action>
  <verify>
<automated>grep -q "AddHangfireServices" src/Infrastructure/DependencyInjection.cs && echo "REGISTERED"</automated>
  </verify>
  <done>Hangfire services registered in DI container</done>
</task>

<task type="auto">
  <name>Task 4: Create Hangfire dashboard endpoints</name>
  <files>src/API/Jobs/HangfireDashboardEndpoints.cs</files>
  <action>
Create HangfireDashboardEndpoints.cs in API/Jobs folder:

1. MapGroup("/hangfire") with authorization
2. MapDashboard with custom route (default: "/hangfire")
3. Add HangfireAuthorizationFilter that:
   - Requires user to have Admin role
   - Or allow in Development environment only
   
4. Add using for Hangfire.Dashboard namespace

**IMPORTANT:** Dashboard must be protected - don't expose to anonymous users in production.
  </action>
  <verify>
<automated>test -f src/API/Jobs/HangfireDashboardEndpoints.cs && echo "DASHBOARD_EXISTS"</automated>
  </verify>
  <done>Hangfire dashboard mapped at /hangfire with authorization protection</done>
</task>

<task type="auto">
  <name>Task 5: Add health check for Hangfire</name>
  <files>src/API/Program.cs</files>
  <read_first>src/API/Program.cs</read_first>
  <action>
Add Hangfire health check to Program.cs:

```csharp
builder.Services.AddHealthChecks()
    .AddHangfire(options => 
    {
        options.MaximumJobsFailed = 10;
        options.MinimumAvailableServers = 1;
    });
```

Add using for AspNetCore.Diagnostics.HealthChecks.Hangfire namespace.
  </action>
  <verify>
<automated>grep -q "AddHangfire" src/API/Program.cs && echo "HEALTH_CHECK_ADDED"</automated>
  </verify>
  <done>Health check registered, returns Unhealthy if >10 failed jobs or no servers</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Hangfire infrastructure (storage, dashboard, health checks)</what-built>
  <how-to-verify>
    1. Build the project: dotnet build
    2. Check dashboard route exists in endpoint list
    3. Verify health check responds at /health endpoint
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|-----------|-------------|
| Admin dashboard | Unauthenticated access to job monitoring |
| Job injection | Malicious job enqueue via internal API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | Dashboard authorization | mitigate | Require Admin role or Development-only in production |
| T-07-02 | Denial of Service | Job queue flood | mitigate | Limit concurrent workers, queue depth monitoring |
| T-07-03 | Information Disclosure | Job data in dashboard | accept | Dashboard restricted to admins only |
| T-07-04 | Elevation of Privilege | Job execution context | accept | Jobs run with same permissions as enqueueing user |

</threat_model>

<verification>
- [ ] Hangfire packages installed
- [ ] PostgreSQL storage configured with resilience options
- [ ] Automatic retry with exponential backoff (5 attempts)
- [ ] Dashboard protected with authorization
- [ ] Health check registered
- [ ] Build passes without errors
</verification>

<success_criteria>
- Hangfire processes background jobs with PostgreSQL persistence
- Dashboard accessible at /hangfire (auth-protected)
- Health check returns OK when jobs are processing
- Failed jobs retry up to 5 times with exponential backoff
- Serilog logs job lifecycle events at appropriate levels
</success_criteria>

<output>
After completion, create `.planning/phases/07-async-processing-notifications-monitoring/07-01-SUMMARY.md`
</output>