---
phase: 10-backend-hardening-reliability
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/API/Auth/HttpsSecurityExtensions.cs
  - src/API/Program.cs
  - docker-compose.yml
  - .github/workflows/deploy-staging.yml
autonomous: false
requirements:
  - SEC-01
user_setup: []

must_haves:
  truths:
    - "HTTP requests redirect to HTTPS with 301 status"
    - "HTTPS responses include Strict-Transport-Security header"
    - "No insecure HTTP endpoints are publicly reachable in staging"
    - "SEC-01 proof artifacts are evidence-based with CI output"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Staging compose with TLS termination"
      min_lines: 30
    - path: ".github/workflows/deploy-staging.yml"
      provides: "Staging deployment workflow"
      min_lines: 30
    - path: "scripts/verify-https-staging.sh"
      provides: "Runtime verification script"
      min_lines: 20
  key_links:
    - from: "docker-compose.yml (HTTP port)"
      to: "HTTPS redirect"
      via: "nginx config with HSTS"
      pattern: "return 301.*https"
    - from: "deploy-staging.yml"
      to: "verify-https-staging.sh"
      via: "curl assertions post-deploy"
      pattern: "curl.*-I.*Strict-Transport-Security"
---

<objective>
SEC-01 runtime proof for HTTPS-only communication in staging environment.

Purpose: Execute runtime verification of HTTPS redirect, HSTS header, and no-insecure-HTTP exposure in staging (production-like topology per D-11). Produce evidence artifacts for verification closure.

Output: CI artifacts folder containing HTTP/HTTPS request-response evidence, header assertions, and environment timestamp.
</objective>

<context>
@.planning/phases/10-backend-hardening-reliability/10-CONTEXT.md
@.planning/phases/01-identity-security-foundation/01-CONTEXT.md
@.planning/phases/01-identity-security-foundation/01-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure staging environment with HTTPS/HSTS</name>
  <files>docker-compose.yml, src/API/Auth/HttpsSecurityExtensions.cs</files>
  <action>
1. **docker-compose.yml** — add staging compose profile:
   - HTTP port: 80 (for redirect testing)
   - HTTPS port: 443 (terminated by nginx)
   - Environment: STAGING=true
   - Nginx config: enforce HSTS header on all responses from 443

2. **HttpsSecurityExtensions.cs** — verify production middleware:
   - UseHttpsRedirection with redirect policy (301 for HTTP→HTTPS)
   - UseHsts with maxAge=31536000 (1 year), includeSubDomains=true, preload=true
   - Configure for staging: enable both redirect and HSTS in STAGING=true environment

Ensure HSTS excluded from development mode (ASPNETCORE_ENVIRONMENT != Production && != Staging).
  </action>
  <verify>
<automated>ls docker-compose.yml | grep -E "443|80|HSTS"</automated>
  </verify>
  <done>Staging compose has HTTP/HTTPS ports configured</done>
</task>

<task type="auto">
  <name>Task 2: Create staging deployment workflow</name>
  <files>.github/workflows/deploy-staging.yml</files>
  <action>
Create deploy-staging.yml GitHub Actions workflow:

1. Trigger: push to main, manual dispatch
2. Jobs:
   - Build: dotnet build, dotnet test
   - Deploy: docker build/push to staging registry
   - Verify: call verification script post-deploy
3. Environment: staging (with TLS termination at load balancer)

Use existing deployment patterns from project.
  </action>
  <verify>
<automated>ls .github/workflows/deploy-staging.yml</automated>
  </verify>
  <done>Staging workflow exists with build/deploy/verify steps</done>
</task>

<task type="auto">
  <name>Task 3: Create runtime verification script</name>
  <files>scripts/verify-https-staging.sh</files>
  <action>
Create verification script that checks SEC-01 requirements:

1. **HTTP→HTTPS redirect test:**
   - curl -I http://staging-host/ -v → expect 301 redirect to https://
   
2. **HSTS header test:**
   - curl -I https://staging-host/ -sD - | grep "Strict-Transport-Security"
   - Assert: max-age >= 31536000, includeSubDomains, preload (if all required)

3. **No public HTTP endpoint test:**
   - Verify no HTTP endpoints respond with 200 (all redirect to HTTPS)

4. **Output artifacts:**
   - Create artifacts/10-sec01-proof-{date}/ with:
     - redirect-response.txt (301 status + Location header)
     - hsts-headers.txt (all HSTS headers received)
     - http-probe.txt (HTTP port probe results)
     - environment.json (date, host, IP)
   - Output result.json with pass/fail for each check
  </action>
  <verify>
<automated>ls scripts/verify-https-staging.sh && chmod +x scripts/verify-https-staging.sh</automated>
  </verify>
  <done>Verification script exists and runs checks</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>SEC-01 runtime proof for HTTPS/HSTS in staging</what-built>
  <how-to-verify>
    1. Deploy to staging: `gh workflow run deploy-staging.yml -f ref=main`
    2. Wait for deployment complete
    3. Run verification: `bash scripts/verify-https-staging.sh https://staging.yourdomain.com`
    4. Check artifacts:
       - result.json should show "http_redirect": true
       - result.json should show "hsts_present": true
       - result.json should show "no_public_http": true
    5. Review contents of artifacts/10-sec01-proof-*/ for evidence
  </how-to-verify>
  <resume-signal>Type "approved" when staging verification passes, or describe issues</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Archive verification artifacts and link to requirements</name>
  <files>artifacts/10-sec01-proof-*/result.json</files>
  <action>
1. Verify all checks passed in result.json
2. Create metadata linking: phase=10, requirement=SEC-01, plan=02, verified=YYYY-MM-DD
3. Link artifact path in verification closure document per D-13
  </action>
  <verify>
<automated>ls artifacts/10-sec01-proof-*/result.json</automated>
  </verify>
  <done>Verification artifacts archived with result metadata</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| {External → Staging HTTP} | Untrusted input crosses via curl tests |
| {Staging → Verification script} | Script validates trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05 | S | HTTP endpoint exposure | mitigate | Verify all HTTP returns 301 redirect; no 200 allowed |
| T-10-06 | D | HSTS header missing | accept | Header optional on subdomains in older browsers; preload required for browser opt-in |
</threat_model>

<verification>
- [ ] HTTP redirect returns 301 to HTTPS
- [ ] HTTPS includes Strict-Transport-Security header
- [ ] No HTTP endpoints return 200 (all redirect)
- [ ] Artifacts folder created with evidence
</verification>

<success_criteria>
SEC-01: HTTPS-only communication enforced in deployed staging with runtime proof artifacts
</success_criteria>

<output>
After completion, create `.planning/phases/10-backend-hardening-reliability/10-02-SUMMARY.md`
</output>