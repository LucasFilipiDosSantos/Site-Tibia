---
phase: 10-backend-hardening-reliability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Application/Orders/Services/OrderLifecycleService.cs
  - src/Application/Fulfillment/Services/FulfillmentService.cs
  - src/Application/Checkout/Services/CheckoutService.cs
  - src/Domain/Orders/Order.cs
  - src/Domain/Orders/Events/OrderPaidEvent.cs
  - src/Domain/Fulfillment/Events/DeliveryCompletedEvent.cs
  - src/Domain/Fulfillment/Events/DeliveryFailedEvent.cs
  - src/Infrastructure/Notifications/NotificationJobs.cs
  - src/Infrastructure/Notifications/WhatsAppNotificationService.cs
autonomous: true
requirements:
  - NTF-01
  - NTF-03
user_setup: []

must_haves:
  truths:
    - "Order status change to Paid triggers automatic WhatsApp notification enqueue"
    - "Delivery status change to Completed triggers automatic WhatsApp notification enqueue"
    - "Delivery status change to Failed triggers automatic WhatsApp notification enqueue"
    - "Notification uses snapshotted phone from order metadata with E.164 validation"
    - "Enqueue failure does not roll back the business transition"
    - "Enqueue idempotency uses OrderId+EventType+statusAtUtc key"
  artifacts:
    - path: "src/Application/Orders/Services/OrderLifecycleService.cs"
      provides: "Order state transitions with notification enqueue hooks"
      min_lines: 30
    - path: "src/Application/Fulfillment/Services/FulfillmentService.cs"
      provides: "Delivery status transitions with notification enqueue hooks"
      min_lines: 30
    - path: "src/Domain/Orders/Events/OrderPaidEvent.cs"
      provides: "Domain event for paid notification trigger"
      exports: ["OrderPaidEvent"]
    - path: "src/Domain/Fulfillment/Events/DeliveryCompletedEvent.cs"
      provides: "Domain event for delivery completed notification"
      exports: ["DeliveryCompletedEvent"]
    - path: "src/Domain/Fulfillment/Events/DeliveryFailedEvent.cs"
      provides: "Domain event for delivery failed notification"
      exports: ["DeliveryFailedEvent"]
    - path: "src/Infrastructure/Notifications/NotificationJobs.cs"
      provides: "Hangfire job payloads for WhatsApp enqueue"
      min_lines: 50
  key_links:
    - from: "OrderLifecycleService.cs (OnOrderPaid)"
      to: "NotificationJobs.EnqueueOrderNotification"
      via: "Domain event publish in transition method"
      pattern: "_publisher.Publish.*OrderPaidEvent"
    - from: "OrderPaidEvent"
      to: "WhatsAppNotificationService.SendAsync"
      via: "Hangfire enqueue with idempotency key"
      pattern: "OrderId.*EventType.*statusAtUtc"
    - from: "CheckoutService (CreateOrder)"
      to: "Order.NotificationPhoneSnapshot"
      via: "Phone number snapshot on order creation"
      pattern: "notificationPhone.*E\\.164"
---

<objective>
Automatic WhatsApp notification enqueue on order/delivery lifecycle events with deterministic contact sourcing.

Purpose: Close NTF-01/NTF-03 gap by wiring automatic notification triggers in Application lifecycle services (not API endpoints) and snapshotted phone contact from checkout/order metadata.

Output: Working automatic notification enqueue on Paid, DeliveryCompleted, DeliveryFailed events with idempotency and failure-outbox behavior.
</objective>

<context>
@.planning/phases/10-backend-hardening-reliability/10-CONTEXT.md
@.planning/phases/07-async-processing-notifications-monitoring/07-03-SUMMARY.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define domain events for lifecycle notification triggers</name>
  <files>src/Domain/Orders/Events/OrderPaidEvent.cs, src/Domain/Fulfillment/Events/DeliveryCompletedEvent.cs, src/Domain/Fulfillment/Events/DeliveryFailedEvent.cs</files>
  <action>
Create domain events in `src/Domain/Orders/Events/` and `src/Domain/Fulfillment/Events/`:

1. **OrderPaidEvent** — carries OrderId, CustomerId, NotificationPhone (snapshotted), OrderNumber, statusAtUtc
2. **DeliveryCompletedEvent** — carries OrderId, CustomerId, NotificationPhone, DeliveryType, statusAtUtc  
3. **DeliveryFailedEvent** — carries OrderId, CustomerId, NotificationPhone, FailureReason, statusAtUtc

Each event implements INotificationEvent interface with OrderId, EventType enum (Paid/DeliveryCompleted/DeliveryFailed), statusAtUtc (UTCNow), and NotificationPhone property.
  </action>
  <verify>
<automated>ls src/Domain/*/Events/*.cs | wc -l</automated>
</verify>
  <done>Domain events exist with OrderId, EventType, statusAtUtc, NotificationPhone properties</done>
</task>

<task type="auto">
  <name>Task 2: Add notification metadata snapshot to Order aggregate</name>
  <files>src/Domain/Orders/Order.cs, src/Application/Checkout/Services/CheckoutService.cs</files>
  <action>
Extend Order aggregate with immutable notification metadata:

1. **Order.cs** — add properties:
   - `NotificationPhone` (string, immutable after creation)
   - `NotificationAvailable` (bool, true if valid phone exists)
   - `NotificationFailedReason` (string?, null unless phone missing)

2. **CheckoutService.cs** — at order creation, snapshot from customer profile:
   - Fetch customer.PhoneNormalized from profile repository
   - If valid E.164: set.NotificationPhone = phone, NotificationAvailable = true
   - If missing/null/invalid: set.NotificationPhone = null, NotificationAvailable = false, NotificationFailedReason = "missing-contact"

Validate phone using PhoneNumberUtil from libphonenumber-csharp (add to project if not present).
  </action>
  <verify>
<automated>grep -l "NotificationPhone" src/Domain/Orders/Order.cs && grep -l "E\\.164" src/Application/Checkout/Services/CheckoutService.cs</automated>
</verify>
  <done>Order has immutable NotificationPhone snapshotted at creation with availability flag</done>
</task>

<task type="auto">
  <name>Task 3: Add automatic notification enqueue in OrderLifecycleService</name>
  <files>src/Application/Orders/Services/OrderLifecycleService.cs</files>
  <action>
Modify OrderLifecycleService.cs `MarkOrderAsPaid` method:

1. After successful status transition to Paid:
   - Publish OrderPaidEvent with order's NotificationPhone and statusAtUtc
   - Use IEventPublisher (existing from Phase 6) or create simple interface

2. Handle notification enqueue failure:
   - DO NOT rollback the payment transition
   - Record failure to outbox/retry table via NotificationOutbox entity
   - Log warning with order ID for observability

For idempotency: key = $"{orderId}:OrderPaid:{order.PaidAtUtc:statusAtUtc}" (from D-02)
  </action>
  <verify>
<automated>grep -A10 "MarkOrderAsPaid" src/Application/Orders/Services/OrderLifecycleService.cs | grep -c "OrderPaidEvent"</automated>
</verify>
  <done>OrderLifecycleService publishes OrderPaidEvent on successful paid transition</done>
</task>

<task type="auto">
  <name>Task 4: Add automatic notification enqueue in FulfillmentService</name>
  <files>src/Application/Fulfillment/Services/FulfillmentService.cs</files>
  <action>
Modify FulfillmentService.cs:

1. **MarkDeliveryCompleted** method:
   - After successful completion, publish DeliveryCompletedEvent
   - Key = $"{orderId}:DeliveryCompleted:{delivery.CompletedAtUtc}"

2. **MarkDeliveryFailed** method:  
   - After failed status, publish DeliveryFailedEvent with failure reason
   - Key = $"{orderId}:DeliveryFailed:{delivery.FailedAtUtc}"

3. **Handle failures** per D-04: persist to outbox, do not rollback delivery status
  </action>
  <verify>
<automated>grep -A10 "MarkDeliveryCompleted" src/Application/Fulfillment/Services/FulfillmentService.cs | grep -c "DeliveryCompletedEvent"</automated>
</verify>
  <done>FulfillmentService publishes DeliveryCompleted and DeliveryFailed events</done>
</task>

<task type="auto">
  <name>Task 5: Wire domain events to WhatsApp notification jobs</name>
  <files>src/Infrastructure/Notifications/NotificationJobs.cs</files>
  <action>
Extend NotificationJobs.cs to handle lifecycle events:

1. Add static method `EnqueueOrderNotification(NotificationEventArgs args)`:
   - Extract OrderId, EventType, NotificationPhone, statusAtUtc from args
   - Compute idempotency key: args.OrderId + args.EventType + args.statusAtUtc
   - Check NotificationOutbox for duplicate key (dedupe)
   - If not duplicate and NotificationAvailable == true:
     - Call WhatsAppNotificationService.SendAsync with template
   - If duplicate: log info "duplicate skipped"
   - If not available: log warning "notification unavailable"

2. Register handler in DI container to subscribe to domain events

Template mapping per D-06:
   - OrderPaid: "Your order {OrderNumber} is now paid! We'll notify you when it ships."
   - DeliveryCompleted: "Your order {OrderNumber} has been delivered! Enjoy."
   - DeliveryFailed: "Your order {OrderNumber} delivery encountered an issue. Contact support."
  </action>
  <verify>
<automated>grep -l "EnqueueOrderNotification" src/Infrastructure/Notifications/NotificationJobs.cs && grep -l "SendAsync" src/Infrastructure/Notifications/WhatsAppNotificationService.cs</automated>
  </verify>
  <done>Lifecycle events trigger WhatsApp notifications via Hangfire with idempotency checks</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| {OrderLifecycleService → NotificationJobs} | Untrusted event payload crosses here; phone number from DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | S | NotificationEventArgs | mitigate | Validate OrderId exists in DB before processing notification |
| T-10-02 | T | WhatsApp API calls | accept | WhatsApp API failures handled by Hangfire retry (existing); per-event timeouts prevent blocking |
| T-10-03 | I | NotificationPhone snapshot | mitigate | Validate phone is E.164 at snapshot time; reject invalid format |
| T-10-04 | R | Duplicate enqueue | mitigate | Idempotency key dedupe in NotificationOutbox before API call |
</threat_model>

<verification>
- [ ] OrderPaidEvent published from OrderLifecycleService.MarkOrderAsPaid
- [ ] DeliveryCompletedEvent published from FulfillmentService.MarkDeliveryCompleted
- [ ] Notification metadata snapshotted in Order at checkout
- [ ] Idempotency key prevents duplicate notification dispatch
- [ ] Missing phone does not block order status transition
</verification>

<success_criteria>
NTF-01: Automatic WhatsApp notification enqueue on Order Paid, Delivery Completed, Delivery Failed events
NTF-03: Notification failures retry via Hangfire; failure-outbox records retries
</success_criteria>

<output>
After completion, create `.planning/phases/10-backend-hardening-reliability/10-01-SUMMARY.md`
</output>