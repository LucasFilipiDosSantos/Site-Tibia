---
phase: 10-backend-hardening-reliability
plan: 03
type: execute
wave: 2
depends_on:
  - 10-01
  - 10-02
files_modified:
  - src/Infrastructure/Logging/RequestLoggingMiddleware.cs
  - src/Application/Orders/Services/OrderLifecycleService.cs
  - src/Application/Payments/Services/PaymentWebhookProcessor.cs
  - src/Application/Fulfillment/Services/FulfillmentService.cs
  - src/Infrastructure/Notifications/NotificationJobs.cs
  - src/API/Admin/AdminAuditEndpoints.cs
  - src/Infrastructure/Audit/AuditLogEntry.cs
  - tests/IntegrationTests/OrderFlowTests.cs
  - tests/IntegrationTests/AdminAuditTests.cs
autonomous: true
requirements:
  - REL-01
  - REL-02
  - ADM-02
user_setup: []

must_haves:
  truths:
    - "Every HTTP request carries correlation ID propagated through payment->order->fulfillment->notification chain"
    - "Structured logs emit correlation ID in every log statement within async chain"
    - "Integration tests verify correlation continuity across payment->order->fulfillment->notification"
    - "Admin audit logs include actor, action, entity, before/after for critical write actions"
    - "Failure paths (invalid signature, duplicate, retry exhaustion) are logged with telemetry"
  artifacts:
    - path: "src/Infrastructure/Logging/RequestLoggingMiddleware.cs"
      provides: "Per-request correlation ID injection"
      min_lines: 30
    - path: "tests/IntegrationTests/OrderFlowTests.cs"
      provides: "Integration tests for full chain correlation"
      min_lines: 50
    - path: "tests/IntegrationTests/AdminAuditTests.cs"
      provides: "Integration tests for audit coverage"
      min_lines: 40
    - path: "src/Infrastructure/Audit/AuditLogEntry.cs"
      provides: "Audit entry persistence model"
      min_lines: 30
  key_links:
    - from: "RequestLoggingMiddleware"
      to: "ILogger (all loggers)"
      via: "Set CorrelationId on scope"
      pattern: "BeginScope.*CorrelationId"
    - from: "PaymentWebhookProcessor"
      to: "OrderLifecycleService"
      via: "Call with correlation context"
      pattern: "OrderLifecycleService.*CorrelationId"
    - from: "AdminAuditEndpoints"
      to: "AuditLogEntry"
      via: "Log before/after state"
      pattern: "auditLogger.Log.*before.*after"
---

<objective>
REL-01/REL-02/ADM-02 evidence closure with correlation ID chain coverage and integration test assertions.

Purpose: Satisfy D-14 (correlation spanning payment→order→fulfillment→notification), D-15 (ADM-02 audit coverage), D-16 (evidence-gated completion), and D-17 (failure-path telemetry).

Output: Integration tests asserting correlation continuity and audit coverage; failure-path logging verified.
</objective>

<context>
@.planning/phases/10-backend-hardening-reliability/10-CONTEXT.md
@.planning/phases/07-async-processing-notifications-monitoring/07-CONTEXT.md
@.planning/phases/09-custom-orders-marketplace-assets-admin-ops/09-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add correlation ID middleware and propagation</name>
  <files>src/Infrastructure/Logging/RequestLoggingMiddleware.cs</files>
  <action>
Extend logging middleware for correlation chain:

1. **RequestLoggingMiddleware.cs** — modify:
   - Generate or extract correlation ID from request header X-Correlation-ID
   - Set in Microsoft.Extensions.Logging.MDC (if available) or custom activity context
   - Ensure correlation ID flows through async context (async local or correlation context)

2. **Propagate to downstream:**
   - OrderLifecycleService: accept correlation ID in method signature
   - PaymentWebhookProcessor: accept and pass correlation ID
   - FulfillmentService: accept and pass correlation ID
   - NotificationJobs: include correlation ID in all log statements
  </action>
  <verify>
<automated>grep -l "CorrelationId" src/API/Program.cs && grep -l "CorrelationId" src/Infrastructure/Logging/RequestLoggingMiddleware.cs</automated>
  </verify>
  <done>Correlation ID middleware exists and propagates through chain</done>
</task>

<task type="auto">
  <name>Task 2: Add correlation propagation to payment webhook processor</name>
  <files>src/Application/Payments/Services/PaymentWebhookProcessor.cs</files>
  <action>
Extend PaymentWebhookProcessor.cs:

1. On webhook receive, extract correlation ID from request or generate new
2. Pass to OrderLifecycleService.MarkOrderAsPaid(correlationId)
3. Log all webhook processing steps with correlation ID
4. Log failure paths per D-17:
   - Invalid signature logging
   - Duplicate event dedupe logging
   - Notification retry exhaustion logging
  </action>
  <verify>
<automated>grep -A5 "MarkOrderAsPaid" src/Application/Payments/Services/PaymentWebhookProcessor.cs | grep "CorrelationId"</automated>
  </verify>
  <done>Payment webhook passes correlation ID to order transitions</done>
</task>

<task type="auto">
  <name>Task 3: Define audit entry model and logging</name>
  <files>src/Infrastructure/Audit/AuditLogEntry.cs, src/Infrastructure/Audit/IAuditLogger.cs</files>
  <action>
1. **AuditLogEntry.cs** — create model:
   - ActorId (from JWT sub claim)
   - Action (e.g., "UpdateProduct", "CreateOrder", "AdjustStock")
   - EntityType (Product, Order, Stock, etc.)
   - EntityId
   - BeforeState (JSON, optional)
   - AfterState (JSON, optional)
   - TimestampUtc
   - CorrelationId (optional, for webhook-triggered actions)

2. **IAuditLogger.cs** — create interface:
   - LogAsync(AuditLogEntry entry)
   - QueryAsync(AuditFilter filter)

3. **AuditLogger.cs** — implement with EF persistence (add to existing DbContext)
  </action>
  <verify>
<parameter name="automated">grep -l "AuditLogEntry" src/Infrastructure/Audit/AuditLogEntry.cs || ls src/Infrastructure/Audit/