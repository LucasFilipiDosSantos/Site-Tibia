---
phase: 02-catalog-product-governance
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/Domain/Catalog/Category.cs
  - src/Domain/Catalog/Product.cs
  - src/Application/Catalog/Contracts/ICategoryRepository.cs
  - src/Application/Catalog/Contracts/IProductRepository.cs
  - src/Application/Catalog/Contracts/CatalogContracts.cs
  - src/Application/Catalog/Services/CatalogService.cs
  - tests/UnitTests/Catalog/CatalogDomainInvariantTests.cs
  - tests/UnitTests/Catalog/CatalogServiceFilterAndPaginationTests.cs
autonomous: true
requirements:
  - CAT-02
  - CAT-03
  - CAT-04
must_haves:
  truths:
    - "Admin mutations reject slug changes and unknown category slugs with validation outcomes."
    - "Catalog query contract supports category+slug filters with AND semantics and offset pagination."
    - "Products remain globally scoped (no server field in domain model)."
  artifacts:
    - path: src/Domain/Catalog/Product.cs
      provides: product invariants (global unique immutable slug, zero-price allowed)
      min_lines: 60
    - path: src/Domain/Catalog/Category.cs
      provides: category invariants (immutable slug, admin-manageable entity)
      min_lines: 40
    - path: src/Application/Catalog/Services/CatalogService.cs
      provides: query and admin mutation orchestration using contracts
      min_lines: 100
  key_links:
    - from: src/Application/Catalog/Services/CatalogService.cs
      to: src/Application/Catalog/Contracts/IProductRepository.cs
      via: list/get/create/update workflows
      pattern: "IProductRepository"
    - from: src/Application/Catalog/Services/CatalogService.cs
      to: src/Application/Catalog/Contracts/ICategoryRepository.cs
      via: category slug resolution and delete guard
      pattern: "ICategoryRepository"
---

<objective>
Define catalog domain + application contracts with test-first invariants so downstream persistence/API work can implement against stable behavior.

Purpose: Lock D-01..D-13 and D-15..D-18 in inner layers first, preventing shallow endpoint-driven logic.
Output: Domain entities, application contracts/service, and unit tests proving filtering, slug immutability, and validation semantics.
</objective>

<execution_context>
@/home/natan/workspace/Site-Tibia/.opencode/get-shit-done/workflows/execute-plan.md
@/home/natan/workspace/Site-Tibia/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-catalog-product-governance/02-CONTEXT.md
@.planning/phases/02-catalog-product-governance/02-RESEARCH.md
@src/Domain/Identity/UserAccount.cs
@src/Application/Identity/Services/IdentityService.cs

<interfaces>
From src/Domain/Identity/UserAccount.cs:
```csharp
public sealed class UserAccount
{
    public Guid Id { get; private set; }
    public string Email { get; private set; }
}
```

From src/Application/Identity/Services/IdentityService.cs (pattern reference):
```csharp
public sealed class IdentityService : IIdentityService
{
    public async Task<RegisterResult> RegisterAsync(RegisterCommand command, CancellationToken cancellationToken = default)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define catalog domain invariants per locked decisions</name>
  <read_first>src/Domain/Identity/UserAccount.cs, .planning/phases/02-catalog-product-governance/02-CONTEXT.md</read_first>
  <files>src/Domain/Catalog/Category.cs, src/Domain/Catalog/Product.cs, tests/UnitTests/Catalog/CatalogDomainInvariantTests.cs</files>
  <behavior>
    - Test 1: Product slug is required, normalized, and immutable after construction (D-06, D-07, D-08).
    - Test 2: Category slug is required and immutable after creation (D-10, D-11).
    - Test 3: Product can be created/updated with zero price (D-16).
    - Test 4: Product model has no Tibia server field (D-09).
  </behavior>
  <action>Create `Category` and `Product` domain entities under `src/Domain/Catalog/` with constructor and mutation methods that enforce: globally unique immutable slug intent for Product (no setter/mutator for slug), immutable category slug, required non-empty names/descriptions, and price validation that allows `0` but rejects negative values (D-16). Do not add any server property to product (D-09). Write unit tests first in `CatalogDomainInvariantTests` (RED), then implement domain code (GREEN), then refactor for clarity (REFACTOR).</action>
  <acceptance_criteria>
    - `tests/UnitTests/Catalog/CatalogDomainInvariantTests.cs` contains test names covering product slug immutability, category slug immutability, zero-price acceptance, and negative-price rejection.
    - `src/Domain/Catalog/Product.cs` does not contain `Server`, `World`, `Aurera`, or `Eternia` properties.
    - `src/Domain/Catalog/Product.cs` exposes no public method that changes `Slug`.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test tests/UnitTests/UnitTests.csproj -v minimal --filter "FullyQualifiedName~CatalogDomainInvariantTests"</automated>
  </verify>
  <done>Domain entities compile and invariant tests pass with explicit proof of D-06..D-11 and D-16 semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define application contracts and catalog service behavior</name>
  <read_first>src/Application/Identity/Services/IdentityService.cs, src/Application/Identity/Contracts/IUserRepository.cs, .planning/phases/02-catalog-product-governance/02-CONTEXT.md</read_first>
  <files>src/Application/Catalog/Contracts/ICategoryRepository.cs, src/Application/Catalog/Contracts/IProductRepository.cs, src/Application/Catalog/Contracts/CatalogContracts.cs, src/Application/Catalog/Services/CatalogService.cs, tests/UnitTests/Catalog/CatalogServiceFilterAndPaginationTests.cs</files>
  <behavior>
    - Test 1: `ListProducts` applies `category` + `slug` with AND semantics when both are provided (D-01, D-02, D-04).
    - Test 2: List contract enforces offset pagination via `page` and `pageSize` with guardrails (D-05).
    - Test 3: Product update rejects slug mutation and unknown category slug with validation error (D-15, D-17, D-18).
  </behavior>
  <action>Create catalog application contracts and a `CatalogService` orchestration class using repository interfaces only (Clean Architecture). Add explicit request/response contracts that include `page`, `pageSize`, `category`, and `slug` for `GET /products` behavior (D-01..D-05). Implement service methods for `GetBySlug`, `ListProducts`, `CreateProduct`, `UpdateProductPutReplace`, `CreateCategory`, and `DeleteCategory`, where update always uses full-replace semantics (D-15), category references are by category slug (D-17), unknown category slug throws validation error path (D-18), and product slug update attempts are rejected (D-08). Write unit tests first then implement.
</action>
  <acceptance_criteria>
    - `src/Application/Catalog/Contracts/CatalogContracts.cs` defines request contracts containing `page` and `pageSize` fields.
    - `tests/UnitTests/Catalog/CatalogServiceFilterAndPaginationTests.cs` contains a test asserting combined category+slug filter returns AND behavior.
    - `src/Application/Catalog/Services/CatalogService.cs` contains explicit guard rejecting slug changes on update.
    - `src/Application/Catalog/Services/CatalogService.cs` resolves category by slug and throws validation exception if not found.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test tests/UnitTests/UnitTests.csproj -v minimal --filter "FullyQualifiedName~CatalogServiceFilterAndPaginationTests"</automated>
  </verify>
  <done>Application layer provides executable catalog behaviors with deterministic validation semantics matching D-01..D-05 and D-15..D-18.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API input → Application contracts | Untrusted filter/pagination/admin payload values enter business logic. |
| Application service → Domain invariants | Mutation requests cross from orchestration to invariant enforcement. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | T | `Product` slug mutation path | mitigate | Enforce immutable slug in domain model and reject update attempts in service (D-07, D-08). |
| T-02-02 | T | `Category` slug mutation path | mitigate | Make category slug immutable in entity and avoid slug mutators (D-11). |
| T-02-03 | D | `ListProducts` pagination/filter input | mitigate | Validate `page>=1`, bounded `pageSize`, and deterministic AND filtering to prevent unbounded scans (D-02, D-05). |
| T-02-04 | E | Admin mutation logic reused by non-admin callers | mitigate | Keep mutation methods separated for Admin-only endpoint wiring in later API plan and cover with tests. |
</threat_model>

<verification>
- dotnet test tests/UnitTests/UnitTests.csproj -v minimal --filter "FullyQualifiedName~CatalogDomainInvariantTests|FullyQualifiedName~CatalogServiceFilterAndPaginationTests"
- dotnet build backend.slnx
</verification>

<success_criteria>
- Catalog domain/application contracts fully encode locked decisions D-01..D-13 and D-15..D-18 relevant to inner layers.
- Unit tests prove AND filters, pagination contract, immutable slugs, and zero-price behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/02-catalog-product-governance/02-01-SUMMARY.md`
</output>
