---
phase: 02-catalog-product-governance
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/Infrastructure/Persistence/AppDbContext.cs
  - src/Infrastructure/Persistence/Configurations/CategoryConfiguration.cs
  - src/Infrastructure/Persistence/Configurations/ProductConfiguration.cs
  - src/Infrastructure/DependencyInjection.cs
  - src/Infrastructure/Catalog/Repositories/CategoryRepository.cs
  - src/Infrastructure/Catalog/Repositories/ProductRepository.cs
  - src/Infrastructure/Persistence/Migrations/*
  - tests/IntegrationTests/Catalog/CatalogPersistenceContractTests.cs
autonomous: true
requirements:
  - CAT-03
  - CAT-04
must_haves:
  truths:
    - "Catalog entities are persisted with unique slug constraints and category-product relational integrity."
    - "Category deletion is blocked when linked products exist."
    - "Schema changes are applied before verification, preventing false green builds."
  artifacts:
    - path: src/Infrastructure/Persistence/Configurations/ProductConfiguration.cs
      provides: DB mapping including unique product slug and FK to category
      min_lines: 40
    - path: src/Infrastructure/Persistence/Configurations/CategoryConfiguration.cs
      provides: DB mapping including immutable slug column constraints and unique index
      min_lines: 30
    - path: src/Infrastructure/Persistence/Migrations
      provides: migration files for catalog schema
      contains: CreateTable for categories/products with indexes
  key_links:
    - from: src/Infrastructure/Catalog/Repositories/ProductRepository.cs
      to: src/Application/Catalog/Contracts/IProductRepository.cs
      via: concrete repository implementation
      pattern: "class ProductRepository : IProductRepository"
    - from: src/Infrastructure/Persistence/AppDbContext.cs
      to: src/Infrastructure/Persistence/Configurations/ProductConfiguration.cs
      via: ApplyConfigurationsFromAssembly
      pattern: "ApplyConfigurationsFromAssembly"
---

<objective>
Implement persistence and repository infrastructure for catalog/category contracts, including schema migration and database push.

Purpose: Translate Plan 01 contracts into durable relational storage with constraints that enforce slug and category governance rules.
Output: EF mappings, repositories, migration artifacts, and applied schema.
</objective>

<execution_context>
@/home/natan/workspace/Site-Tibia/.opencode/get-shit-done/workflows/execute-plan.md
@/home/natan/workspace/Site-Tibia/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-catalog-product-governance/02-CONTEXT.md
@.planning/phases/02-catalog-product-governance/02-RESEARCH.md
@.planning/phases/02-catalog-product-governance/02-01-PLAN.md
@src/Infrastructure/Persistence/AppDbContext.cs
@src/Infrastructure/Persistence/Configurations/UserAccountConfiguration.cs
@src/Infrastructure/Identity/Repositories/UserRepository.cs
@src/Infrastructure/Persistence/Migrations/20260415084643_AddUserRoleEnum.cs

<interfaces>
From Plan 01 expected outputs:
```csharp
public interface IProductRepository { /* list/get/create/update methods */ }
public interface ICategoryRepository { /* get/create/delete methods */ }
```

From src/Infrastructure/Persistence/AppDbContext.cs:
```csharp
public sealed class AppDbContext : DbContext
{
    public DbSet<UserAccount> Users => Set<UserAccount>();
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement EF mappings and repositories for catalog domain</name>
  <read_first>src/Infrastructure/Persistence/AppDbContext.cs, src/Infrastructure/Persistence/Configurations/UserAccountConfiguration.cs, src/Infrastructure/Identity/Repositories/UserRepository.cs, .planning/phases/02-catalog-product-governance/02-01-PLAN.md</read_first>
  <files>src/Infrastructure/Persistence/AppDbContext.cs, src/Infrastructure/Persistence/Configurations/CategoryConfiguration.cs, src/Infrastructure/Persistence/Configurations/ProductConfiguration.cs, src/Infrastructure/Catalog/Repositories/CategoryRepository.cs, src/Infrastructure/Catalog/Repositories/ProductRepository.cs, src/Infrastructure/DependencyInjection.cs, tests/IntegrationTests/Catalog/CatalogPersistenceContractTests.cs</files>
  <behavior>
    - Test 1: Product slug uniqueness is enforced globally in persistence (D-06).
    - Test 2: Category slug uniqueness is enforced and category deletion with linked products fails (D-11, D-13).
    - Test 3: Product repository supports list filters for category slug + product slug (AND-ready semantics) and pagination inputs from application contracts (D-01, D-02, D-05).
  </behavior>
  <action>Add catalog DbSets to `AppDbContext`; create EF configurations for `Category` and `Product` with table names, required columns, unique indexes on slugs, FK `Product.CategoryId -> Category.Id`, and restrictive delete behavior to block category deletion when products exist (D-13). Implement repository classes conforming to Plan 01 contracts and register them in `DependencyInjection`. Add integration tests (`CatalogPersistenceContractTests`) proving uniqueness and delete-guard semantics against real EF context behavior.
</action>
  <acceptance_criteria>
    - `src/Infrastructure/Persistence/Configurations/ProductConfiguration.cs` contains `HasIndex(x => x.Slug).IsUnique()`.
    - `src/Infrastructure/Persistence/Configurations/CategoryConfiguration.cs` contains `HasIndex(x => x.Slug).IsUnique()`.
    - Product configuration sets FK to category with restrictive/no-action delete behavior.
    - `src/Infrastructure/DependencyInjection.cs` registers `IProductRepository` and `ICategoryRepository` implementations.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test tests/IntegrationTests/IntegrationTests.csproj -v minimal --filter "FullyQualifiedName~CatalogPersistenceContractTests"</automated>
  </verify>
  <done>Infrastructure fully implements catalog persistence contracts with enforced relational invariants.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Generate and apply catalog schema migration before verification</name>
  <read_first>src/Infrastructure/Persistence/AppDbContext.cs, src/Infrastructure/Persistence/Migrations/AppDbContextModelSnapshot.cs</read_first>
  <files>src/Infrastructure/Persistence/Migrations/*</files>
  <action>Generate EF migration for new catalog/category schema and apply it to the configured database before final verification. Use command sequence: `dotnet ef migrations add AddCatalogAndCategoryGovernance --project src/Infrastructure/Infrastructure.csproj --startup-project src/API/API.csproj --context AppDbContext` then `dotnet ef database update --project src/Infrastructure/Infrastructure.csproj --startup-project src/API/API.csproj --context AppDbContext`. If migration naming conflict occurs, choose deterministic equivalent starting with `AddCatalogAndCategoryGovernance`.</action>
  <acceptance_criteria>
    - A new migration file exists under `src/Infrastructure/Persistence/Migrations/` creating categories/products tables and indexes.
    - `AppDbContextModelSnapshot` includes catalog/category model mappings.
    - `dotnet ef database update ...` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>dotnet ef database update --project src/Infrastructure/Infrastructure.csproj --startup-project src/API/API.csproj --context AppDbContext</automated>
  </verify>
  <done>Database schema reflects catalog entities and is synchronized before API contract checks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Application repository calls → Database | Trusted business requests cross into persistence where constraint bypass can occur. |
| Migration artifacts → Live DB schema | Schema drift or unapplied migrations can silently invalidate verification. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | T | Product/category slug persistence | mitigate | Enforce DB-level unique indexes for both slugs and keep immutable semantics in upper layers. |
| T-02-06 | T | Category delete path | mitigate | Use restrictive FK and repository guard to block deletes when products exist (D-13). |
| T-02-07 | D | Missing schema push | mitigate | Include blocking migration+database-update task before verification to avoid false green checks. |
| T-02-08 | R | Persistence mutation auditability gap | accept | Full write-audit instrumentation deferred to ADM-02 phase; current phase enforces invariant correctness only. |
</threat_model>

<verification>
- dotnet test tests/IntegrationTests/IntegrationTests.csproj -v minimal --filter "FullyQualifiedName~CatalogPersistenceContractTests"
- dotnet ef database update --project src/Infrastructure/Infrastructure.csproj --startup-project src/API/API.csproj --context AppDbContext
- dotnet build backend.slnx
</verification>

<success_criteria>
- Catalog tables + constraints exist in migration and are applied to DB.
- Repository implementations satisfy Plan 01 contracts without breaking layering boundaries.
</success_criteria>

<output>
After completion, create `.planning/phases/02-catalog-product-governance/02-02-SUMMARY.md`
</output>
