---
phase: 02-catalog-product-governance
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - src/API/Catalog/CatalogDtos.cs
  - src/API/Catalog/CatalogEndpoints.cs
  - src/API/Program.cs
  - tests/IntegrationTests/Catalog/CatalogCustomerEndpointsTests.cs
  - tests/IntegrationTests/Catalog/CatalogAdminEndpointsTests.cs
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - CAT-01
  - CAT-02
  - CAT-03
  - CAT-04
must_haves:
  truths:
    - "Customer can browse products by category/slug via `GET /products` and resolve canonical slug via `GET /products/{slug}`."
    - "Admin can create/update categories and products with PUT replace semantics and immutable slug enforcement."
    - "Requirement alignment artifacts explicitly reflect global catalog model override for CAT-01 per D-14."
  artifacts:
    - path: src/API/Catalog/CatalogEndpoints.cs
      provides: public and admin catalog route contracts
      exports: [MapCatalogEndpoints]
    - path: src/API/Catalog/CatalogDtos.cs
      provides: request/response contracts for filters, pagination, create/update payloads
      min_lines: 80
    - path: tests/IntegrationTests/Catalog/CatalogCustomerEndpointsTests.cs
      provides: customer contract verification for filters/slugs/pagination
      min_lines: 80
  key_links:
    - from: src/API/Program.cs
      to: src/API/Catalog/CatalogEndpoints.cs
      via: endpoint registration
      pattern: "app.MapCatalogEndpoints()"
    - from: src/API/Catalog/CatalogEndpoints.cs
      to: API.Auth.AuthPolicies.AdminOnly
      via: admin route protection
      pattern: "RequireAuthorization(AuthPolicies.AdminOnly)"
---

<objective>
Expose catalog customer/admin HTTP contracts and finalize requirement-alignment updates so Phase 2 delivery is executable and auditable.

Purpose: Deliver CAT-01..CAT-04 observable API behavior while honoring all locked decisions and documenting the CAT-01 override.
Output: Catalog endpoints + integration tests + roadmap/requirements alignment edits.
</objective>

<execution_context>
@/home/natan/workspace/Site-Tibia/.opencode/get-shit-done/workflows/execute-plan.md
@/home/natan/workspace/Site-Tibia/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-catalog-product-governance/02-CONTEXT.md
@.planning/phases/02-catalog-product-governance/02-01-PLAN.md
@.planning/phases/02-catalog-product-governance/02-02-PLAN.md
@src/API/Auth/AuthEndpoints.cs
@src/API/Auth/AuthDtos.cs
@src/API/Auth/AuthPolicies.cs
@src/API/Program.cs

<interfaces>
From src/API/Auth/AuthPolicies.cs:
```csharp
public const string AdminOnly = "AdminOnly";
```

From Plan 01/02 expected outputs:
```csharp
public sealed class CatalogService
{
    // list/get/create/update category/product operations
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement customer catalog read endpoints with filter + slug contracts</name>
  <read_first>src/API/Auth/AuthEndpoints.cs, src/API/Auth/AuthDtos.cs, .planning/phases/02-catalog-product-governance/02-CONTEXT.md, .planning/phases/02-catalog-product-governance/02-01-PLAN.md</read_first>
  <files>src/API/Catalog/CatalogDtos.cs, src/API/Catalog/CatalogEndpoints.cs, src/API/Program.cs, tests/IntegrationTests/Catalog/CatalogCustomerEndpointsTests.cs</files>
  <behavior>
    - Test 1: `GET /products?page={n}&pageSize={m}&category={slug}&slug={slug}` applies AND filters (D-01, D-02, D-04, D-05).
    - Test 2: `GET /products/{slug}` resolves canonical product by slug (D-03).
    - Test 3: Pagination guard rejects invalid values and returns ProblemDetails 400.
  </behavior>
  <action>Create catalog DTOs and route-group endpoints under `/products`. Implement list endpoint with query params `category`, `slug`, `page`, `pageSize` only (D-04), defaulting to safe page bounds and returning paged response metadata. Implement slug endpoint `GET /products/{slug}` using canonical lookup (D-03). Register `MapCatalogEndpoints` from `Program.cs`. Write integration tests first for filter semantics, slug lookup, and pagination validation using WebApplicationFactory.
</action>
  <acceptance_criteria>
    - `src/API/Catalog/CatalogEndpoints.cs` contains `MapGet("/products"` and `MapGet("/products/{slug}"`.
    - `src/API/Catalog/CatalogDtos.cs` contains list query contract with `category`, `slug`, `page`, `pageSize`.
    - `tests/IntegrationTests/Catalog/CatalogCustomerEndpointsTests.cs` includes explicit AND-filter test case.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test tests/IntegrationTests/IntegrationTests.csproj -v minimal --filter "FullyQualifiedName~CatalogCustomerEndpointsTests"</automated>
  </verify>
  <done>Customer catalog API contracts satisfy D-01..D-05 and CAT-02/CAT-03 behaviors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement admin product/category governance endpoints + requirement alignment updates</name>
  <read_first>src/API/Auth/AuthPolicies.cs, src/API/ErrorHandling/GlobalExceptionHandler.cs, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/phases/02-catalog-product-governance/02-CONTEXT.md</read_first>
  <files>src/API/Catalog/CatalogDtos.cs, src/API/Catalog/CatalogEndpoints.cs, tests/IntegrationTests/Catalog/CatalogAdminEndpointsTests.cs, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <behavior>
    - Test 1: Admin-only endpoints require `AdminOnly` and deny unauthorized/non-admin requests.
    - Test 2: PUT product update enforces replace semantics, rejects slug mutation, and allows zero price (D-15, D-16).
    - Test 3: Create/update with unknown category slug returns 400 validation (D-17, D-18).
    - Test 4: Category delete with linked products is blocked (D-13).
  </behavior>
  <action>Add admin routes (protected with `RequireAuthorization(AuthPolicies.AdminOnly)`) for category create/delete and product create/PUT update. Product write DTO must reference category by `categorySlug` and include full replace payload for PUT (D-15, D-17). Reject payload attempts to change existing product slug with validation 400 (D-08) and allow `price = 0` (D-16). Add integration tests for admin authz and mutation behavior. Update `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md` to record D-14 alignment: CAT-01 server-filter wording is overridden by locked global catalog model decision for this phase, with phase success criteria reflecting global category/slug discovery instead of server segmentation.
</action>
  <acceptance_criteria>
    - `src/API/Catalog/CatalogEndpoints.cs` admin mutation routes contain `RequireAuthorization(AuthPolicies.AdminOnly)`.
    - Integration tests assert 401/403 for unauthenticated/non-admin access and 2xx for admin happy paths.
    - `.planning/REQUIREMENTS.md` contains explicit note that CAT-01 is superseded by Phase 2 decision D-14 (global catalog, no server field).
    - `.planning/ROADMAP.md` Phase 2 success criteria text is updated to match D-14 without referencing server-specific segmentation.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test tests/IntegrationTests/IntegrationTests.csproj -v minimal --filter "FullyQualifiedName~CatalogAdminEndpointsTests" && dotnet test backend.slnx -v minimal</automated>
  </verify>
  <done>Admin governance API + requirement alignment artifacts fully satisfy CAT-04 and D-14 with locked-decision fidelity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Catalog API | Untrusted customer/admin input crosses into catalog behavior. |
| Auth middleware → Admin endpoints | Claims-based authorization boundary protecting mutation routes. |
| API error mapping → public response | Internal validation/operation errors are transformed to external contracts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | E | Admin catalog endpoints | mitigate | Protect all mutation endpoints with `AdminOnly` policy and integration tests for unauthorized/forbidden outcomes. |
| T-02-10 | T | Product update payload | mitigate | Reject slug mutation attempts and unknown category slug references with deterministic 400 contracts. |
| T-02-11 | I | Error response surface | mitigate | Reuse ProblemDetails mapping and avoid leaking internal repository/DB exceptions directly. |
| T-02-12 | R | Requirement semantics drift (CAT-01 vs D-14) | mitigate | Update roadmap/requirements alignment artifacts in-phase to keep audits traceable to locked decision. |
</threat_model>

<verification>
- dotnet test tests/IntegrationTests/IntegrationTests.csproj -v minimal --filter "FullyQualifiedName~CatalogCustomerEndpointsTests|FullyQualifiedName~CatalogAdminEndpointsTests"
- dotnet test backend.slnx -v minimal
- dotnet build backend.slnx
</verification>

<success_criteria>
- Public/admin catalog endpoints satisfy all locked decision contracts and CAT-01..CAT-04 requirements mapping.
- Requirements/roadmap artifacts are aligned with D-14 and no deferred ideas are introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/02-catalog-product-governance/02-03-SUMMARY.md`
</output>
