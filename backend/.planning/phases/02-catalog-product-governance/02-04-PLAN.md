---
phase: 02-catalog-product-governance
plan: 04
type: execute
wave: 4
depends_on:
  - 02-03
files_modified:
  - src/API/Catalog/CatalogDtos.cs
  - src/API/Catalog/CatalogEndpoints.cs
  - tests/IntegrationTests/Catalog/CatalogCustomerEndpointsTests.cs
  - tests/IntegrationTests/Catalog/CatalogAdminEndpointsTests.cs
autonomous: true
requirements:
  - CAT-02
  - CAT-03
  - CAT-04
gap_closure: true
must_haves:
  truths:
    - "Catalog API DTO contract is substantive and directly exercised by customer/admin endpoint bindings."
    - "Customer and admin catalog API behaviors from Phase 2 remain unchanged while DTO coverage is expanded."
    - "Phase verification no longer flags CatalogDtos.cs as a stub artifact."
  artifacts:
    - path: src/API/Catalog/CatalogDtos.cs
      provides: complete catalog request/response DTO contract surface for list, slug lookup, and admin mutations
      min_lines: 80
    - path: src/API/Catalog/CatalogEndpoints.cs
      provides: endpoint binding that consumes DTO types directly for query/body contracts
      min_lines: 90
    - path: tests/IntegrationTests/Catalog/CatalogCustomerEndpointsTests.cs
      provides: customer contract coverage proving DTO-backed list/slug pagination behavior
      min_lines: 160
  key_links:
    - from: src/API/Catalog/CatalogEndpoints.cs
      to: src/API/Catalog/CatalogDtos.cs
      via: direct request/response DTO type usage in route handlers
      pattern: "ProductListQueryRequest|ProductListResponse|UpdateProductPutReplaceRequest"
    - from: tests/IntegrationTests/Catalog/CatalogCustomerEndpointsTests.cs
      to: src/API/Catalog/CatalogDtos.cs
      via: response deserialization to DTO contracts
      pattern: "ReadFromJsonAsync<ProductListResponse>|ReadFromJsonAsync<ProductResponse>"
---

<objective>
Close the Phase 02 verification gap by making catalog DTO contracts substantial, explicit, and actively used by endpoint bindings/tests.

Purpose: Resolve the `CatalogDtos.cs` stub finding without weakening locked catalog decisions or API behavior.
Output: Expanded DTO contracts, endpoint binding updates, and test coverage proving DTO contracts are exercised.
</objective>

<execution_context>
@/home/natan/workspace/Site-Tibia/.opencode/get-shit-done/workflows/execute-plan.md
@/home/natan/workspace/Site-Tibia/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-catalog-product-governance/02-CONTEXT.md
@.planning/phases/02-catalog-product-governance/02-VERIFICATION.md
@.planning/phases/02-catalog-product-governance/02-03-SUMMARY.md
@src/API/Catalog/CatalogDtos.cs
@src/API/Catalog/CatalogEndpoints.cs
@tests/IntegrationTests/Catalog/CatalogCustomerEndpointsTests.cs
@tests/IntegrationTests/Catalog/CatalogAdminEndpointsTests.cs

<interfaces>
From src/API/Catalog/CatalogDtos.cs (current):
```csharp
public sealed record ProductListQueryRequest(string? Category, string? Slug, int Page = 1, int PageSize = 20);
public sealed record ProductResponse(string Name, string Slug, string Description, decimal Price, string CategorySlug);
public sealed record ProductListResponse(IReadOnlyList<ProductResponse> Items, int Page, int PageSize);
public sealed record CreateCategoryRequest(string Name, string Slug, string Description);
public sealed record CreateProductRequest(string Name, string Slug, string Description, decimal Price, string CategorySlug);
public sealed record UpdateProductPutReplaceRequest(string Slug, string Name, string Description, decimal Price, string CategorySlug);
```

From src/API/Catalog/CatalogEndpoints.cs (binding target):
```csharp
app.MapGet("/products", async (string? category, string? slug, int? page, int? pageSize, CatalogService catalogService, CancellationToken ct) =>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand DTO contracts and bind customer/admin routes directly to DTO types</name>
  <read_first>src/API/Catalog/CatalogDtos.cs, src/API/Catalog/CatalogEndpoints.cs, .planning/phases/02-catalog-product-governance/02-CONTEXT.md, .planning/phases/02-catalog-product-governance/02-VERIFICATION.md</read_first>
  <files>src/API/Catalog/CatalogDtos.cs, src/API/Catalog/CatalogEndpoints.cs</files>
  <behavior>
    - Test 1: `GET /products` binds list query through `ProductListQueryRequest` and preserves D-01..D-05 behavior (category+slug AND filters, page/pageSize pagination).
    - Test 2: `GET /products/{slug}` and admin create/update routes continue returning/accepting typed DTO contracts preserving D-03 and D-15..D-18 semantics.
    - Test 3: DTO file contains substantive request/response contract coverage (no placeholder/unused-only DTO set).
  </behavior>
  <action>Rewrite `CatalogDtos.cs` into an explicit contract module with grouped DTOs for customer list/slug responses and admin category/product mutation payloads, keeping existing JSON field names and compatibility. Expand list contract definitions to include dedicated query, item, and response metadata DTO types that are all consumed by endpoints (not dead types). Update `CatalogEndpoints.cs` to bind `GET /products` via `ProductListQueryRequest` (e.g., `[AsParameters]`) instead of primitive argument scatter, and continue mapping through `CatalogService` exactly per D-01, D-02, D-04, D-05. Preserve canonical slug route behavior per D-03 and admin PUT replace + category slug semantics per D-15, D-17, D-18.</action>
  <acceptance_criteria>
    - `src/API/Catalog/CatalogDtos.cs` has at least 80 lines of contract definitions that are referenced by endpoint handlers.
    - `src/API/Catalog/CatalogEndpoints.cs` uses `ProductListQueryRequest` as list query binding input (no separate primitive category/slug/page/pageSize parameters).
    - `src/API/Catalog/CatalogEndpoints.cs` still contains `MapGet("/products/{slug}"` and admin routes protected by `RequireAuthorization(AuthPolicies.AdminOnly)`.
    - `src/API/Catalog/CatalogDtos.cs` contains no unused-only top-level DTO left from previous compact contract.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test tests/IntegrationTests/IntegrationTests.csproj -v minimal --filter "FullyQualifiedName~CatalogCustomerEndpointsTests|FullyQualifiedName~CatalogAdminEndpointsTests"</automated>
  </verify>
  <done>Catalog DTO contracts are substantive, explicitly bound in endpoints, and preserve all locked Phase 2 API semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Align integration tests with DTO contract usage and prove gap closure</name>
  <read_first>tests/IntegrationTests/Catalog/CatalogCustomerEndpointsTests.cs, tests/IntegrationTests/Catalog/CatalogAdminEndpointsTests.cs, src/API/Catalog/CatalogDtos.cs, .planning/phases/02-catalog-product-governance/02-VERIFICATION.md</read_first>
  <files>tests/IntegrationTests/Catalog/CatalogCustomerEndpointsTests.cs, tests/IntegrationTests/Catalog/CatalogAdminEndpointsTests.cs</files>
  <behavior>
    - Test 1: Customer tests deserialize list/slug payloads using API catalog DTOs and continue validating D-01..D-05 outcomes.
    - Test 2: Admin tests continue validating authz + mutation behavior while using DTO-backed payload/response contracts.
    - Test 3: Verification evidence demonstrates `CatalogDtos.cs` is no longer a stub artifact.
  </behavior>
  <action>Update integration tests to consume catalog DTO contracts from `API.Catalog` (replace local duplicate response record declarations) so tests exercise the same DTO surface used in production endpoints. Keep all existing assertions for filter AND behavior, canonical slug lookup, invalid pagination 400, admin 401/403/2xx, slug immutability rejection, zero-price acceptance, unknown category 400, and category delete blocking. Add an explicit assertion in tests (or dedicated lightweight check within existing tests) that endpoint responses deserialize successfully against the expanded DTO contract types.</action>
  <acceptance_criteria>
    - `CatalogCustomerEndpointsTests.cs` does not define duplicate local `ProductListResponse`/`ProductResponse` records.
    - Customer and admin test suites pass without contract regression.
    - `src/API/Catalog/CatalogDtos.cs` remains >=80 lines after test alignment.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test tests/IntegrationTests/IntegrationTests.csproj -v minimal --filter "FullyQualifiedName~CatalogCustomerEndpointsTests|FullyQualifiedName~CatalogAdminEndpointsTests" && wc -l src/API/Catalog/CatalogDtos.cs</automated>
  </verify>
  <done>Integration tests prove DTO contract usage end-to-end and supply objective evidence closing the verification gap.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client query/body input → API DTO binding | Untrusted request data crosses model binding into typed contracts. |
| API DTO contracts → Application service requests | Mapping errors can silently alter filter/update semantics if DTO contracts drift. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-13 | T | `ProductListQueryRequest` binding path | mitigate | Keep explicit DTO binding and integration tests asserting AND filter + pagination semantics (D-01..D-05). |
| T-02-14 | T | Admin update DTO contract | mitigate | Preserve full PUT replace fields and immutable slug/category validation paths with integration coverage (D-15..D-18). |
| T-02-15 | I | API contract drift between endpoints and tests | mitigate | Remove duplicated local test DTOs so tests deserialize against production DTO types directly. |
| T-02-16 | R | Verification artifact traceability | mitigate | Provide objective line-count + passing test evidence to close the `CatalogDtos.cs` stub finding. |
</threat_model>

<verification>
- dotnet test tests/IntegrationTests/IntegrationTests.csproj -v minimal --filter "FullyQualifiedName~CatalogCustomerEndpointsTests|FullyQualifiedName~CatalogAdminEndpointsTests"
- wc -l src/API/Catalog/CatalogDtos.cs
- dotnet build backend.slnx -v minimal
</verification>

<success_criteria>
- `CatalogDtos.cs` is substantive (>=80 lines) and actively consumed by endpoint bindings/tests.
- No regression to existing CAT-02/CAT-03/CAT-04 API behaviors or locked decisions D-01..D-05, D-03, D-15..D-18.
- Gap in `02-VERIFICATION.md` is closable with concrete evidence instead of override.
</success_criteria>

<output>
After completion, create `.planning/phases/02-catalog-product-governance/02-04-SUMMARY.md`
</output>
