---
phase: 05-order-lifecycle-timeline-visibility
plan: 03
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
files_modified:
  - src/API/Checkout/CheckoutDtos.cs
  - src/API/Checkout/CheckoutEndpoints.cs
  - src/API/Checkout/AdminOrderEndpoints.cs
  - src/API/Program.cs
  - src/API/ErrorHandling/GlobalExceptionHandler.cs
  - src/Application/Checkout/Contracts/OrderLifecycleContracts.cs
  - src/Application/Checkout/Services/OrderLifecycleService.cs
  - tests/IntegrationTests/Checkout/CheckoutEndpointsTests.cs
  - tests/IntegrationTests/Checkout/OrderHistoryEndpointsTests.cs
  - tests/IntegrationTests/Checkout/AdminOrderManagementEndpointsTests.cs
autonomous: true
requirements:
  - ORD-03
  - ORD-04
  - ORD-01
must_haves:
  truths:
    - "Customer can list their orders with offset pagination and open detail timeline containing status code + label."
    - "Admin can search orders by status, customer identifier, and created date range."
    - "Admin/manual illegal transitions return 409 ProblemDetails with currentStatus and allowedTransitions."
    - "Manual admin transitions capture actor identity and reason metadata."
  artifacts:
    - path: src/API/Checkout/CheckoutEndpoints.cs
      provides: customer order-history list/detail timeline endpoints
      exports: [MapCheckoutEndpoints]
    - path: src/API/Checkout/AdminOrderEndpoints.cs
      provides: admin search + explicit transition action routes
      exports: [MapAdminOrderEndpoints]
    - path: tests/IntegrationTests/Checkout/OrderHistoryEndpointsTests.cs
      provides: customer history/timeline contract verification
      min_lines: 180
    - path: tests/IntegrationTests/Checkout/AdminOrderManagementEndpointsTests.cs
      provides: admin search/action authorization and conflict contract verification
      min_lines: 220
  key_links:
    - from: src/API/Program.cs
      to: src/API/Checkout/AdminOrderEndpoints.cs
      via: endpoint registration
      pattern: "app.MapAdminOrderEndpoints\(\)"
    - from: src/API/ErrorHandling/GlobalExceptionHandler.cs
      to: lifecycle conflict exception
      via: RFC7807 409 mapping
      pattern: "allowedTransitions"
---

<objective>
Deliver customer/admin order lifecycle visibility endpoints with explicit transition actions and deterministic conflict contracts aligned to all Phase 5 decisions.

Purpose: Complete ORD-03 and ORD-04 (plus ORD-01 API behavior) with end-to-end authorization, filtering, and timeline response proof.
Output: API contracts/endpoints, error mapping, and integration test suites.
</objective>

<execution_context>
@/home/natan/workspace/Site-Tibia/.opencode/get-shit-done/workflows/execute-plan.md
@/home/natan/workspace/Site-Tibia/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-order-lifecycle-timeline-visibility/05-CONTEXT.md
@.planning/phases/05-order-lifecycle-timeline-visibility/05-01-PLAN.md
@.planning/phases/05-order-lifecycle-timeline-visibility/05-02-PLAN.md
@src/API/Checkout/CheckoutEndpoints.cs
@src/API/ErrorHandling/GlobalExceptionHandler.cs
@src/API/Auth/AuthPolicies.cs
@src/API/Program.cs

<interfaces>
From src/API/Auth/AuthPolicies.cs:
```csharp
public const string AdminOnly = "AdminOnly";
public const string VerifiedForSensitiveActions = "VerifiedForSensitiveActions";
```

From src/API/Checkout/CheckoutEndpoints.cs:
```csharp
public static IEndpointRouteBuilder MapCheckoutEndpoints(this IEndpointRouteBuilder app)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add customer order-history list/detail timeline endpoints with contract tests</name>
  <read_first>.planning/phases/05-order-lifecycle-timeline-visibility/05-CONTEXT.md, src/API/Checkout/CheckoutEndpoints.cs, src/API/Checkout/CheckoutDtos.cs, tests/IntegrationTests/Checkout/CheckoutEndpointsTests.cs</read_first>
  <files>src/API/Checkout/CheckoutDtos.cs, src/API/Checkout/CheckoutEndpoints.cs, tests/IntegrationTests/Checkout/OrderHistoryEndpointsTests.cs, tests/IntegrationTests/Checkout/CheckoutEndpointsTests.cs</files>
  <behavior>
    - Test 1: Customer GET history endpoint returns only caller-owned orders with offset params `page` and `pageSize` (per D-09, D-12).
    - Test 2: Default sort is newest-first by order `CreatedAtUtc` (per D-10).
    - Test 3: Detail timeline returns status events containing raw status code + display label (per D-11) and event metadata from/to/source/occurredAtUtc (per D-06).
  </behavior>
  <action>Extend customer checkout routes with concrete endpoint `GET /checkout/orders?page={page}&pageSize={pageSize}` (per D-09, D-12) while keeping `GET /checkout/orders/{orderId:guid}` as timeline-enabled detail response. Add DTOs exposing both `statusCode` and `statusLabel` at order and timeline-event levels (per D-11). Ensure ownership filter uses JWT `sub` and never leaks other customers' orders. Implement default ordering by `CreatedAtUtc DESC` when no explicit sort is provided (per D-10). Write integration RED tests first in `OrderHistoryEndpointsTests.cs`, then implement to GREEN.</action>
  <acceptance_criteria>
    - `CheckoutEndpoints.cs` contains `MapGet("/orders"` with `page`/`pageSize` query contract.
    - `CheckoutDtos.cs` includes fields `statusCode` and `statusLabel` for order/timeline payloads.
    - `OrderHistoryEndpointsTests.cs` asserts newest-first by `CreatedAtUtc` default.
    - Integration history tests exit 0.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test tests/IntegrationTests/IntegrationTests.csproj --filter "FullyQualifiedName~OrderHistoryEndpointsTests|FullyQualifiedName~CheckoutEndpointsTests"</automated>
  </verify>
  <done>Customer order history/timeline API satisfies D-09 through D-12 and ORD-03 with verified contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add admin search + explicit transition action endpoints with 409 conflict mapping</name>
  <read_first>src/API/Auth/AuthPolicies.cs, src/API/ErrorHandling/GlobalExceptionHandler.cs, src/API/Program.cs, .planning/phases/05-order-lifecycle-timeline-visibility/05-CONTEXT.md, tests/IntegrationTests/Checkout/OrderHistoryEndpointsTests.cs</read_first>
  <files>src/API/Checkout/AdminOrderEndpoints.cs, src/API/Program.cs, src/API/ErrorHandling/GlobalExceptionHandler.cs, src/Application/Checkout/Contracts/OrderLifecycleContracts.cs, src/Application/Checkout/Services/OrderLifecycleService.cs, tests/IntegrationTests/Checkout/AdminOrderManagementEndpointsTests.cs</files>
  <behavior>
    - Test 1: Admin search endpoint filters by status, customer identifier, and created date range (per D-13).
    - Test 2: Admin transition endpoints are explicit actions (e.g., cancel) and reject generic set-status payloads (per D-14).
    - Test 3: Illegal transition returns 409 ProblemDetails with `currentStatus` and `allowedTransitions` (per D-15).
    - Test 4: Successful manual admin transition records actor from JWT `sub` and required non-empty `reason` in event metadata (per D-16).
    - Test 5: Admin endpoints enforce `AdminOnly` authz with 401/403/2xx coverage.
  </behavior>
  <action>Create new admin route group `MapGroup("/admin/orders").RequireAuthorization(AuthPolicies.AdminOnly)` and expose concrete endpoints: `GET /admin/orders?status=&customer=&createdFromUtc=&createdToUtc=&page=&pageSize=` (D-13) and explicit action route `POST /admin/orders/{orderId:guid}/actions/cancel` with body `{ reason: string }` (D-14, D-16). Do NOT create generic `set-status` endpoint. In `OrderLifecycleService`, enforce required reason for admin manual transitions and persist actor from authenticated `sub`. Extend `GlobalExceptionHandler` to map lifecycle conflict exception to HTTP 409 with ProblemDetails extensions `currentStatus` and `allowedTransitions` (D-15). Write integration RED tests in `AdminOrderManagementEndpointsTests.cs` and implement to GREEN.</action>
  <acceptance_criteria>
    - `AdminOrderEndpoints.cs` defines explicit `actions/cancel` route and no generic set-status route.
    - `GlobalExceptionHandler.cs` maps lifecycle conflict to 409 with `currentStatus` + `allowedTransitions`.
    - `AdminOrderManagementEndpointsTests.cs` asserts reason-required validation and actor capture behavior.
    - Admin endpoint integration tests exit 0 and include 401/403 checks.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test tests/IntegrationTests/IntegrationTests.csproj --filter "FullyQualifiedName~AdminOrderManagementEndpointsTests|FullyQualifiedName~OrderHistoryEndpointsTests" && dotnet test backend.slnx -v minimal</automated>
  </verify>
  <done>Admin order management/search and conflict contracts satisfy D-13 through D-16, ORD-04, and lifecycle transition API behavior for ORD-01.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated customer/admin principals → order endpoints | Claims and query/body inputs cross into history/search/transition operations. |
| lifecycle conflict internals → public ProblemDetails payload | Internal state machine constraints are exposed to clients/operators. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09 | I | Customer order history endpoints | mitigate | Scope all customer history reads by authenticated `sub`; return 404 for non-owned order detail. |
| T-05-10 | E | Admin transition/search endpoints | mitigate | Protect `/admin/orders` route group with `AuthPolicies.AdminOnly` and verify 401/403 behavior via integration tests. |
| T-05-11 | T | Lifecycle conflict API contract | mitigate | Return explicit 409 ProblemDetails with `currentStatus` and `allowedTransitions`; no silent or generic 400 downgrades. |
| T-05-12 | R | Manual transition audit metadata | mitigate | Require non-empty admin reason and persist actor identity from JWT `sub` in timeline event metadata. |
</threat_model>

<verification>
- dotnet test tests/IntegrationTests/IntegrationTests.csproj --filter "FullyQualifiedName~OrderHistoryEndpointsTests|FullyQualifiedName~AdminOrderManagementEndpointsTests|FullyQualifiedName~CheckoutEndpointsTests"
- dotnet test backend.slnx -v minimal
- dotnet build backend.slnx -v minimal
</verification>

<success_criteria>
- ORD-03 customer history/timeline visibility contracts are available and passing integration tests.
- ORD-04 admin search and explicit transition action contracts are available with authz and conflict diagnostics proofs.
- Phase 5 decisions D-09 through D-16 are implemented without simplification.
</success_criteria>

<output>
After completion, create `.planning/phases/05-order-lifecycle-timeline-visibility/05-03-SUMMARY.md`
</output>
