---
phase: 05-order-lifecycle-timeline-visibility
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/Infrastructure/Persistence/AppDbContext.cs
  - src/Infrastructure/Persistence/Configurations/OrderConfiguration.cs
  - src/Infrastructure/Persistence/Configurations/OrderStatusTransitionEventConfiguration.cs
  - src/Infrastructure/Checkout/Repositories/CheckoutRepository.cs
  - src/Application/Checkout/Contracts/IOrderLifecycleRepository.cs
  - src/Infrastructure/Checkout/Repositories/OrderLifecycleRepository.cs
  - src/Infrastructure/DependencyInjection.cs
  - src/Infrastructure/Persistence/Migrations/*
  - tests/IntegrationTests/Checkout/CheckoutPersistenceContractTests.cs
  - tests/IntegrationTests/Checkout/OrderLifecyclePersistenceTests.cs
autonomous: true
requirements:
  - ORD-02
  - ORD-03
must_haves:
  truths:
    - "Status transition events persist immutably as timestamped history tied to the order."
    - "Order-history reads can return timeline-backed data for customer APIs."
    - "Schema changes are applied before endpoint verification to avoid false-positive tests."
  artifacts:
    - path: src/Infrastructure/Persistence/Configurations/OrderStatusTransitionEventConfiguration.cs
      provides: immutable mapping/indexing for transition history events
      min_lines: 40
    - path: src/Infrastructure/Checkout/Repositories/OrderLifecycleRepository.cs
      provides: transition-aware load/save and query projections
      exports: [GetByIdAsync, SaveAsync, SearchAsync, GetCustomerHistoryAsync]
    - path: src/Infrastructure/Persistence/Migrations
      provides: migration for lifecycle status + transition event schema
      contains: "AddOrderLifecycleTimeline"
  key_links:
    - from: src/Infrastructure/Persistence/AppDbContext.cs
      to: src/Domain/Checkout/OrderStatusTransitionEvent.cs
      via: DbSet registration
      pattern: "DbSet<OrderStatusTransitionEvent>"
    - from: src/Infrastructure/DependencyInjection.cs
      to: src/Infrastructure/Checkout/Repositories/OrderLifecycleRepository.cs
      via: DI binding
      pattern: "AddScoped<IOrderLifecycleRepository"
---

<objective>
Persist lifecycle/timeline state with migration-backed durability and repository query support so customer/admin APIs can consume trustworthy status history.

Purpose: Deliver ORD-02 persistence guarantees and ORD-03 read-path foundations before API layer changes.
Output: EF mappings, repository implementations, migration/schema update, and integration persistence proof.
</objective>

<execution_context>
@/home/natan/workspace/Site-Tibia/.opencode/get-shit-done/workflows/execute-plan.md
@/home/natan/workspace/Site-Tibia/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-order-lifecycle-timeline-visibility/05-CONTEXT.md
@.planning/phases/05-order-lifecycle-timeline-visibility/05-RESEARCH.md
@.planning/phases/05-order-lifecycle-timeline-visibility/05-01-PLAN.md
@src/Infrastructure/Persistence/AppDbContext.cs
@src/Infrastructure/Persistence/Configurations/OrderConfiguration.cs
@src/Infrastructure/Checkout/Repositories/CheckoutRepository.cs
@src/Infrastructure/Persistence/Migrations/

<interfaces>
From src/Infrastructure/Checkout/Repositories/CheckoutRepository.cs:
```csharp
public sealed class CheckoutRepository : ICheckoutRepository
{
    public Task SaveOrderAsync(Order order, CancellationToken cancellationToken = default);
    public Task<Order?> GetOrderByIdAsync(Guid orderId, CancellationToken cancellationToken = default);
}
```

From src/Infrastructure/Persistence/AppDbContext.cs:
```csharp
public DbSet<Order> Orders => Set<Order>();
public DbSet<OrderItemSnapshot> OrderItemSnapshots => Set<OrderItemSnapshot>();
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement lifecycle persistence mappings/repositories with integration RED→GREEN</name>
  <read_first>src/Infrastructure/Persistence/AppDbContext.cs, src/Infrastructure/Persistence/Configurations/OrderConfiguration.cs, src/Infrastructure/Checkout/Repositories/CheckoutRepository.cs, .planning/phases/05-order-lifecycle-timeline-visibility/05-01-PLAN.md</read_first>
  <files>src/Infrastructure/Persistence/AppDbContext.cs, src/Infrastructure/Persistence/Configurations/OrderConfiguration.cs, src/Infrastructure/Persistence/Configurations/OrderStatusTransitionEventConfiguration.cs, src/Application/Checkout/Contracts/IOrderLifecycleRepository.cs, src/Infrastructure/Checkout/Repositories/OrderLifecycleRepository.cs, src/Infrastructure/DependencyInjection.cs, tests/IntegrationTests/Checkout/OrderLifecyclePersistenceTests.cs, tests/IntegrationTests/Checkout/CheckoutPersistenceContractTests.cs</files>
  <behavior>
    - Test 1: Persisted transition history rows include from/to/source/occurredAtUtc and are returned in chronological order (per D-06, D-07).
    - Test 2: Existing history rows are immutable (no update/delete path) and new transitions append additional rows (per D-08).
    - Test 3: Customer-history projection supports offset pagination and newest-by-created default ordering foundations (per D-10, D-12).
  </behavior>
  <action>Create EF mapping for transition-event entity with concrete columns `from_status`, `to_status`, `source_type`, `occurred_at_utc`, `actor_user_id` (nullable), `reason` (nullable) and FK to orders. Add indexes for query filters: `(order_id, occurred_at_utc)`, `(customer_id, created_at_utc DESC)`, and `(status, created_at_utc)` supporting D-10/D-13 paths. Implement `OrderLifecycleRepository` methods for aggregate load/save and read projections required by customer/admin APIs. Bind `IOrderLifecycleRepository` in DI. Write integration tests first, then implement until GREEN.</action>
  <acceptance_criteria>
    - `AppDbContext.cs` registers `DbSet<OrderStatusTransitionEvent>`.
    - `OrderStatusTransitionEventConfiguration.cs` enforces required fields and FK to `orders`.
    - `OrderLifecycleRepository.cs` includes query methods for customer history and admin search foundations.
    - Integration tests for `OrderLifecyclePersistenceTests` exit 0.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test tests/IntegrationTests/IntegrationTests.csproj --filter "FullyQualifiedName~OrderLifecyclePersistenceTests"</automated>
  </verify>
  <done>Lifecycle/timeline persistence model is durable, queryable, and integration-proven for ORD-02/ORD-03 foundations.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Create/apply migration for lifecycle schema before API verification</name>
  <read_first>src/Infrastructure/Persistence/AppDbContext.cs, src/Infrastructure/Persistence/Configurations/OrderStatusTransitionEventConfiguration.cs, src/Infrastructure/Persistence/Migrations/AppDbContextModelSnapshot.cs, .planning/phases/05-order-lifecycle-timeline-visibility/05-RESEARCH.md</read_first>
  <files>src/Infrastructure/Persistence/Migrations/*</files>
  <action>Generate migration named `AddOrderLifecycleTimeline` after lifecycle persistence mappings are complete. Ensure migration adds order status column(s), transition-event table, FK constraints, and indexes used for customer/admin history queries. Apply schema using blocking command: `dotnet ef database update --project src/Infrastructure/Infrastructure.csproj --startup-project src/API/API.csproj --context AppDbContext`. If DB unavailable, start local Postgres dependency and rerun. Do not proceed to API-plan verification without successful migration apply.</action>
  <acceptance_criteria>
    - Migration files with name containing `AddOrderLifecycleTimeline` exist.
    - Snapshot includes transition-event entity and new lifecycle fields/indexes.
    - `dotnet ef database update ...` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>dotnet ef database update --project src/Infrastructure/Infrastructure.csproj --startup-project src/API/API.csproj --context AppDbContext && dotnet test tests/IntegrationTests/IntegrationTests.csproj --filter "FullyQualifiedName~OrderLifecyclePersistenceTests"</automated>
  </verify>
  <done>Live development database contains lifecycle schema required by Phase 5 APIs and persistence tests remain green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Domain lifecycle data → relational schema | Status/event truth crosses from in-memory aggregate into persistent storage. |
| Migration runner → live development DB | Schema drift or partial application can invalidate verification results. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | T | Transition event persistence | mitigate | Use append-only table with no update/delete repository methods for history rows. |
| T-05-06 | I | Timeline metadata storage | mitigate | Persist only required lifecycle metadata (`from`, `to`, `source`, `occurredAtUtc`, actor/reason where needed), no sensitive payload blobs. |
| T-05-07 | D | Schema/runtime mismatch | mitigate | Enforce blocking migration apply before endpoint verification. |
| T-05-08 | R | Audit/event attribution | mitigate | Store source type plus actor/reason metadata fields for manual transitions. |
</threat_model>

<verification>
- dotnet test tests/IntegrationTests/IntegrationTests.csproj --filter "FullyQualifiedName~OrderLifecyclePersistenceTests"
- dotnet ef database update --project src/Infrastructure/Infrastructure.csproj --startup-project src/API/API.csproj --context AppDbContext
- dotnet build backend.slnx -v minimal
</verification>

<success_criteria>
- ORD-02 timeline persistence is durable and append-only with integration proof.
- Schema and repository read models are ready for customer/admin lifecycle APIs (ORD-03 foundation).
</success_criteria>

<output>
After completion, create `.planning/phases/05-order-lifecycle-timeline-visibility/05-02-SUMMARY.md`
</output>
