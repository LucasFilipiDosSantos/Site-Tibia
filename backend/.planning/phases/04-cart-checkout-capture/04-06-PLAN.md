---
phase: 04-cart-checkout-capture
plan: 06
type: execute
wave: 6
depends_on:
  - 04-05
files_modified:
  - src/Application/Inventory/Contracts/IInventoryRepository.cs
  - src/Application/Inventory/Services/InventoryService.cs
  - src/Infrastructure/Inventory/Repositories/InventoryRepository.cs
  - tests/UnitTests/Inventory/InventoryReservationServiceTests.cs
  - tests/IntegrationTests/Checkout/CheckoutEndpointsTests.cs
  - tests/IntegrationTests/Checkout/CheckoutPersistenceContractTests.cs
autonomous: true
gap_closure: true
requirements:
  - CHK-02
must_haves:
  truths:
    - "Checkout reserve step evaluates each cart line independently, even when all lines share one orderIntentKey (D-14)."
    - "A successful multi-line checkout reserves every line and persists exactly one order with full line snapshots (D-14, D-09)."
    - "If any line conflicts, checkout fails as one unit: no order persisted, cart not cleared, and all prior line reservations released (D-14, D-16)."
  artifacts:
    - path: src/Application/Inventory/Services/InventoryService.cs
      provides: product-scoped idempotency lookup and reserve semantics that do not short-circuit unrelated lines
      exports: [ReserveStockForCheckoutAsync]
    - path: src/Infrastructure/Inventory/Repositories/InventoryRepository.cs
      provides: repository query semantics keyed by orderIntentKey + productId for active reservation checks
      exports: [GetReservationByIntentAndProductAsync]
    - path: tests/UnitTests/Inventory/InventoryReservationServiceTests.cs
      provides: unit proof of product-scoped idempotency and no hollow reserve success
      min_lines: 240
    - path: tests/IntegrationTests/Checkout/CheckoutPersistenceContractTests.cs
      provides: real InventoryService/Repository-backed multi-line atomic checkout proof
      min_lines: 240
  key_links:
    - from: src/Application/Checkout/Services/CheckoutService.cs
      to: src/Application/Inventory/Services/InventoryService.cs
      via: ReserveStockForCheckoutAsync per line using shared orderIntentKey
      pattern: "ReserveStockForCheckoutAsync"
    - from: src/Application/Inventory/Services/InventoryService.cs
      to: src/Infrastructure/Inventory/Repositories/InventoryRepository.cs
      via: active reservation lookup filtered by orderIntentKey + productId
      pattern: "GetReservationByIntentAndProductAsync"
    - from: tests/IntegrationTests/Checkout/CheckoutPersistenceContractTests.cs
      to: Checkout + Inventory infrastructure path
      via: conflict and success scenarios with per-line reserve assertions
      pattern: "multi-line|atomic|reserve"
---

<objective>
Close the remaining D-14 verification gap by removing inventory intent-only short-circuit behavior that can falsely mark later checkout lines as reserved.

Purpose: Ensure checkout atomicity is real end-to-end (reserve-all-or-fail-all) when multiple products share one checkout intent.
Output: Product-scoped reservation idempotency semantics plus integration proof that multi-line checkout either reserves all lines and creates one order or fails with zero partial side effects.
</objective>

<execution_context>
@/home/natan/workspace/Site-Tibia/.opencode/get-shit-done/workflows/execute-plan.md
@/home/natan/workspace/Site-Tibia/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-cart-checkout-capture/04-CONTEXT.md
@.planning/phases/04-cart-checkout-capture/04-VERIFICATION.md
@.planning/phases/04-cart-checkout-capture/04-05-SUMMARY.md
@src/Application/Inventory/Contracts/InventoryContracts.cs
@src/Application/Inventory/Contracts/IInventoryRepository.cs
@src/Application/Inventory/Services/InventoryService.cs
@src/Infrastructure/Inventory/Repositories/InventoryRepository.cs

<interfaces>
From src/Application/Inventory/Contracts/IInventoryRepository.cs:
```csharp
public interface IInventoryRepository
{
    Task<InventoryReservationRecord?> GetReservationByIntentKeyAsync(string orderIntentKey, CancellationToken cancellationToken = default);
    Task<ReserveInventoryResult> TryReserveAsync(ReserveInventoryAttempt attempt, CancellationToken cancellationToken = default);
    Task<int> ReleaseReservationAsync(string orderIntentKey, ReservationReleaseReason reason, DateTimeOffset releasedAtUtc, CancellationToken cancellationToken = default);
}
```

From src/Application/Checkout/Services/CheckoutService.cs:
```csharp
foreach (var line in cart.Lines)
{
    await _inventoryGateway.ReserveStockForCheckoutAsync(orderId, orderIntentKey, line.ProductId, line.Quantity, cancellationToken);
}
```

From src/Application/Inventory/Contracts/InventoryContracts.cs:
```csharp
public sealed record ReserveStockForCheckoutRequest(string OrderIntentKey, Guid OrderId, Guid ProductId, int Quantity);
public sealed record InventoryReservationRecord(string OrderIntentKey, Guid OrderId, Guid ProductId, int Quantity, DateTimeOffset ReservedAtUtc, DateTimeOffset ReservationExpiresAtUtc, DateTimeOffset? ReleasedAtUtc);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix inventory idempotency to be line/product scoped for shared checkout intents</name>
  <files>src/Application/Inventory/Contracts/IInventoryRepository.cs, src/Application/Inventory/Services/InventoryService.cs, src/Infrastructure/Inventory/Repositories/InventoryRepository.cs, tests/UnitTests/Inventory/InventoryReservationServiceTests.cs</files>
  <behavior>
    - Test 1: Replaying reserve for same orderIntentKey + same productId returns existing reservation without new reserve write (idempotent replay).
    - Test 2: Reserving same orderIntentKey with different productId must execute a real reserve attempt (no short-circuit hollow success).
    - Test 3: Existing reservation with same intent+product but mismatched quantity is treated as conflict/error, not success, to preserve reserve-all correctness.
  </behavior>
  <action>Replace intent-only active reservation lookup with a product-scoped lookup (`orderIntentKey + productId`) in InventoryService and repository (per D-14 and verification gap.missing). Preserve checkout intent traceability by keeping shared `orderIntentKey`; do not introduce relaxed semantics that skip later lines. If lookup finds active record for same intent/product, only treat as replay when quantity matches exactly; otherwise throw deterministic conflict/validation exception to prevent false reserve success. Update inventory unit tests to prove no hollow success across multi-line checkout semantics.</action>
  <verify>
    <automated>dotnet test tests/UnitTests/UnitTests.csproj --filter "FullyQualifiedName~InventoryReservationServiceTests"</automated>
  </verify>
  <done>Inventory reservation idempotency is explicitly product-scoped and cannot incorrectly satisfy a different checkout line under the same intent key.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add real checkout+inventory integration proof for multi-line reserve-all atomicity</name>
  <files>tests/IntegrationTests/Checkout/CheckoutPersistenceContractTests.cs, tests/IntegrationTests/Checkout/CheckoutEndpointsTests.cs</files>
  <behavior>
    - Test 1: Success case with two products reserves both lines (real InventoryService/InventoryRepository path), creates one order containing both snapshots, and clears cart.
    - Test 2: Conflict on second line releases first-line reservation, returns 409 with line conflict detail, persists no order, and leaves cart intact.
    - Test 3: Post-conflict inventory availability reflects full rollback (reserved quantity returns to zero for all lines in that checkout attempt).
  </behavior>
  <action>Create integration scenarios that exercise CheckoutService through the real inventory persistence path (InventoryService + InventoryRepository), not only in-memory gateway doubles, to close 04-VERIFICATION line 26-27. Keep one shared orderIntentKey in checkout flow (per D-14 context) and assert per-line reserve correctness by product. Add explicit assertions for both success and conflict branches: exactly one order on success, none on conflict, cart clear only on success, and zero residual reservations after compensation.</action>
  <verify>
    <automated>dotnet test tests/IntegrationTests/IntegrationTests.csproj --filter "FullyQualifiedName~CheckoutPersistenceContractTests|FullyQualifiedName~CheckoutEndpointsTests" && dotnet build backend.slnx -v minimal</automated>
  </verify>
  <done>Integration evidence demonstrates true reserve-all-or-fail-all behavior per product line with no partial side effects, fully satisfying D-14.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checkout orchestration → inventory reservation service | Untrusted cart composition crosses into stock mutation operations; incorrect idempotency can corrupt inventory truth. |
| API conflict payload → external client | Internal reservation conflict details are exposed to client and must remain minimal and deterministic. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-19 | T | InventoryService idempotency branch | mitigate | Scope idempotent replay checks to `orderIntentKey + productId (+quantity match)` so unrelated lines cannot be marked reserved. |
| T-04-20 | D | Inventory reservation pool | mitigate | Add integration tests proving conflict path triggers compensation and returns reserved quantities to zero for all prior lines. |
| T-04-21 | I | Checkout conflict ProblemDetails | mitigate | Preserve existing 409 `lineConflicts.availableQuantity` payload shape only; do not leak reservation internals/order metadata. |
</threat_model>

<verification>
- dotnet test tests/UnitTests/UnitTests.csproj --filter "FullyQualifiedName~InventoryReservationServiceTests"
- dotnet test tests/IntegrationTests/IntegrationTests.csproj --filter "FullyQualifiedName~CheckoutPersistenceContractTests|FullyQualifiedName~CheckoutEndpointsTests"
- dotnet build backend.slnx -v minimal
</verification>

<success_criteria>
- D-14 gap in `04-VERIFICATION.md` is closed with real-path proof: each checkout line reserves by product or conflicts; no line is falsely treated as reserved by intent-only replay.
- Multi-line success case proves all lines are reserved and a single order with immutable snapshots is persisted.
- Multi-line conflict case proves fail-all behavior: zero persisted orders, cart unchanged, and reservation rollback complete for every previously reserved line.
</success_criteria>

<output>
After completion, create `.planning/phases/04-cart-checkout-capture/04-06-SUMMARY.md`
</output>
