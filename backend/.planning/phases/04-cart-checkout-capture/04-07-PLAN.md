---
phase: 04-cart-checkout-capture
plan: 07
type: execute
wave: 7
depends_on:
  - 04-06
files_modified:
  - src/Infrastructure/Inventory/Repositories/InventoryRepository.cs
  - tests/IntegrationTests/Checkout/CheckoutPersistenceContractTests.cs
autonomous: true
gap_closure: true
requirements:
  - CHK-02
must_haves:
  truths:
    - "Checkout conflict on a later line in a 3+ line cart releases every previously reserved line under the same orderIntentKey (D-14)."
    - "After failed checkout, no order is persisted and cart contents remain unchanged (D-14, D-16)."
    - "Post-conflict inventory shows zero residual reserved quantity for all lines reserved before the conflict (D-14)."
  artifacts:
    - path: src/Infrastructure/Inventory/Repositories/InventoryRepository.cs
      provides: release-all-active-reservations compensation for a checkout intent in one transactional pass
      exports: [ReleaseReservationAsync]
    - path: tests/IntegrationTests/Checkout/CheckoutPersistenceContractTests.cs
      provides: real-path 3-line late-conflict rollback proof with zero residual reservations
      min_lines: 320
  key_links:
    - from: src/Application/Checkout/Services/CheckoutService.cs
      to: src/Infrastructure/Inventory/Repositories/InventoryRepository.cs
      via: ReleaseCheckoutReservationAsync -> InventoryService.ReleaseReservationAsync -> ReleaseReservationAsync
      pattern: "ReleaseReservationAsync"
    - from: tests/IntegrationTests/Checkout/CheckoutPersistenceContractTests.cs
      to: checkout + inventory persistence path
      via: SubmitCheckout_ThirdLineConflict_* scenario assertions
      pattern: "ThirdLineConflict|reserved=0|ReleasedAtUtc"
---

<objective>
Close the final Phase 4 D-14 verification failure by fixing compensation release semantics for multi-line checkout conflicts.

Purpose: Ensure reserve-all-or-fail-all remains true when a late conflict occurs after two or more successful reservations.
Output: Repository release logic that clears all active reservations for an orderIntentKey and real-path integration proof for a 3+ line late-conflict rollback.
</objective>

<execution_context>
@/home/natan/workspace/Site-Tibia/.opencode/get-shit-done/workflows/execute-plan.md
@/home/natan/workspace/Site-Tibia/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-cart-checkout-capture/04-CONTEXT.md
@.planning/phases/04-cart-checkout-capture/04-VERIFICATION.md
@.planning/phases/04-cart-checkout-capture/04-06-SUMMARY.md
@src/Application/Checkout/Services/CheckoutService.cs
@src/Application/Inventory/Services/InventoryService.cs
@src/Infrastructure/Inventory/Repositories/InventoryRepository.cs
@tests/IntegrationTests/Checkout/CheckoutPersistenceContractTests.cs

<interfaces>
From src/Infrastructure/Inventory/Repositories/InventoryRepository.cs:
```csharp
public async Task<int> ReleaseReservationAsync(
    string orderIntentKey,
    ReservationReleaseReason reason,
    DateTimeOffset releasedAtUtc,
    CancellationToken cancellationToken = default)
```

From src/Application/Checkout/Services/CheckoutService.cs:
```csharp
if (conflicts.Count > 0)
{
    if (hasSuccessfulReserve)
    {
        await CompensateReservationsOrThrowAsync(orderIntentKey, cancellationToken);
    }

    throw new CheckoutReservationConflictException(conflicts);
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make compensation release all active reservations for the checkout intent</name>
  <files>src/Infrastructure/Inventory/Repositories/InventoryRepository.cs</files>
  <behavior>
    - Test 1: For an intent with multiple active reservation rows (different products), release operation marks all as released in one call.
    - Test 2: Release operation restores reserved stock quantity for each affected product, not only the latest reservation row.
    - Test 3: Operation remains idempotent for already released rows (no double-release side effects).
  </behavior>
  <action>Replace the single-row `FirstOrDefault` compensation logic in `ReleaseReservationAsync` with an all-active-by-intent query and loop that releases every active reservation row under `orderIntentKey` (per D-14 and 04-VERIFICATION gap.missing). Keep the operation transactional: update each reservation row and corresponding `InventoryStock` row in the same transaction, and return the total quantity released across all affected rows. Do not alter reserve timing or checkout API behavior.</action>
  <verify>
    <automated>dotnet test tests/IntegrationTests/IntegrationTests.csproj --filter "FullyQualifiedName~CheckoutPersistenceContractTests"</automated>
  </verify>
  <done>Compensation release implementation no longer leaves residual active reservations when more than one line was successfully reserved before a later conflict.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add N-line late-conflict integration proof with zero residual reservations</name>
  <files>tests/IntegrationTests/Checkout/CheckoutPersistenceContractTests.cs</files>
  <behavior>
    - Test 1: 3-line checkout where line 1 and line 2 reserve successfully but line 3 conflicts returns checkout conflict and persists no order.
    - Test 2: After conflict, cart remains intact and reserved quantity for products from line 1 and line 2 is zero.
    - Test 3: Reservation rows created before the conflict are present only as released entries (`ReleasedAtUtc != null`), proving compensation touched all prior reservations.
  </behavior>
  <action>Add a real-path integration test (CheckoutService + InventoryService + InventoryRepository) with at least three cart lines and conflict on a later line (line 3+) as required by 04-VERIFICATION gap.missing. Assert: (a) conflict is thrown with expected line conflict details, (b) `Orders` and `OrderItemSnapshots` remain zero, (c) cart is not cleared, (d) inventory availability reports reserved=0 for all previously reserved products, and (e) all reservation rows for that checkout intent are released. Keep existing 2-line conflict test; this is additive proof for N-line rollback completeness.</action>
  <verify>
    <automated>dotnet test tests/IntegrationTests/IntegrationTests.csproj --filter "FullyQualifiedName~CheckoutPersistenceContractTests|FullyQualifiedName~CheckoutEndpointsTests" && dotnet build backend.slnx -v minimal</automated>
  </verify>
  <done>Phase 4 has explicit real-path evidence that late conflicts in 3+ line checkout attempts leave zero residual reservations and no partial checkout side effects.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checkout compensation path → inventory persistence | Conflict-triggered rollback crosses service/repository boundary and mutates inventory truth; partial rollback creates integrity drift. |
| inventory conflict output → API client | Conflict details are externally visible and must remain minimal while still actionable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-22 | T | `InventoryRepository.ReleaseReservationAsync` | mitigate | Release all active reservations under intent key in one transaction; avoid single-row `FirstOrDefault` behavior. |
| T-04-23 | D | Inventory reserved pool | mitigate | Add 3-line late-conflict integration test asserting reserved=0 after compensation for all previously reserved lines. |
| T-04-24 | I | Checkout conflict response | mitigate | Preserve existing 409 `lineConflicts.availableQuantity` contract; do not expose internal reservation row metadata. |
</threat_model>

<verification>
- dotnet test tests/IntegrationTests/IntegrationTests.csproj --filter "FullyQualifiedName~CheckoutPersistenceContractTests|FullyQualifiedName~CheckoutEndpointsTests"
- dotnet build backend.slnx -v minimal
</verification>

<success_criteria>
- D-14 remaining gap in `04-VERIFICATION.md` is closed: compensation releases all previously reserved lines for the checkout intent, not only one row.
- New N-line (3+) late-conflict integration test proves zero residual reservations after rollback.
- Checkout failure semantics remain fail-all: no order persisted, cart unchanged, deterministic conflict payload intact.
</success_criteria>

<output>
After completion, create `.planning/phases/04-cart-checkout-capture/04-07-SUMMARY.md`
</output>
