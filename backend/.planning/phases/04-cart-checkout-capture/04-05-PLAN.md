---
phase: 04-cart-checkout-capture
plan: 05
type: execute
wave: 5
depends_on:
  - 04-04
files_modified:
  - src/Application/Checkout/Contracts/ICheckoutInventoryGateway.cs
  - src/Application/Checkout/Services/CheckoutService.cs
  - src/Infrastructure/Checkout/CheckoutServiceDependencies.cs
  - tests/UnitTests/Checkout/CheckoutServiceTests.cs
  - tests/IntegrationTests/Checkout/CheckoutEndpointsTests.cs
autonomous: true
gap_closure: true
requirements:
  - CHK-02
must_haves:
  truths:
    - "Checkout conflict on any line leaves zero net reservation side effects for that checkout attempt (D-14)."
    - "Checkout conflict still returns deterministic per-line 409 conflict details without creating an order (D-15)."
    - "Successful checkout behavior remains unchanged: order persists and cart clears once all reserves succeed (D-16)."
  artifacts:
    - path: src/Application/Checkout/Contracts/ICheckoutInventoryGateway.cs
      provides: reserve + compensation contract for checkout-intent rollback
      exports: [ReserveStockForCheckoutAsync, ReleaseCheckoutReservationAsync]
    - path: src/Application/Checkout/Services/CheckoutService.cs
      provides: compensation-aware reserve orchestration and fail-all semantics
      exports: [SubmitCheckoutAsync]
    - path: tests/UnitTests/Checkout/CheckoutServiceTests.cs
      provides: proof that failed multi-line checkout leaves no partial reservations
      min_lines: 240
    - path: tests/IntegrationTests/Checkout/CheckoutEndpointsTests.cs
      provides: API-level proof of no residual reservation side effects after conflict
      min_lines: 360
  key_links:
    - from: src/Application/Checkout/Services/CheckoutService.cs
      to: src/Application/Checkout/Contracts/ICheckoutInventoryGateway.cs
      via: compensation call on failure path
      pattern: "ReleaseCheckoutReservationAsync"
    - from: tests/IntegrationTests/Checkout/CheckoutEndpointsTests.cs
      to: checkout inventory in-memory gateway state
      via: post-conflict residual reservation assertion
      pattern: "GetReservedQuantity|Assert\.Equal\(0"
---

<objective>
Close the single blocker from 04-VERIFICATION by enforcing atomic checkout compensation so a multi-line conflict cannot leave partial stock reservations.

Purpose: Restore full D-14 fail-all fidelity and remove hidden reservation leakage risk before Phase 5 order lifecycle work.
Output: Compensation-capable checkout gateway contract, rollback-aware service flow, and unit/integration proof of zero partial reservation side effects.
</objective>

<execution_context>
@/home/natan/workspace/Site-Tibia/.opencode/get-shit-done/workflows/execute-plan.md
@/home/natan/workspace/Site-Tibia/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-cart-checkout-capture/04-CONTEXT.md
@.planning/phases/04-cart-checkout-capture/04-VERIFICATION.md
@.planning/phases/04-cart-checkout-capture/04-02-SUMMARY.md
@.planning/phases/04-cart-checkout-capture/04-04-SUMMARY.md
@src/Application/Inventory/Contracts/InventoryContracts.cs
@src/Application/Inventory/Services/InventoryService.cs

<interfaces>
From src/Application/Checkout/Contracts/ICheckoutInventoryGateway.cs:
```csharp
public interface ICheckoutInventoryGateway
{
    Task ReserveStockForCheckoutAsync(Guid orderId, string orderIntentKey, Guid productId, int quantity, CancellationToken cancellationToken = default);
}
```

From src/Application/Inventory/Contracts/InventoryContracts.cs:
```csharp
public sealed record ReleaseReservationRequest(string OrderIntentKey, ReservationReleaseReason Reason);
public enum ReservationReleaseReason { PaymentFailed = 1, OrderCanceled = 2, Expired = 3 }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add checkout compensation contract and failing rollback tests</name>
  <files>src/Application/Checkout/Contracts/ICheckoutInventoryGateway.cs, tests/UnitTests/Checkout/CheckoutServiceTests.cs, tests/IntegrationTests/Checkout/CheckoutEndpointsTests.cs</files>
  <behavior>
    - Test 1: When line N conflicts after line 1 already reserved, checkout throws conflict and invokes compensation for prior successful reserves (D-14).
    - Test 2: After conflict, simulated inventory gateway has zero reserved quantity for the checkout attempt key (D-14).
    - Test 3: API conflict scenario still returns 409 with `lineConflicts[*].availableQuantity` and no order persisted (D-15).
  </behavior>
  <action>Extend `ICheckoutInventoryGateway` with `ReleaseCheckoutReservationAsync(orderIntentKey, reason, cancellationToken)` per D-14 so checkout can explicitly compensate previously successful reservations. Update unit and integration test doubles to track reserved quantities by checkout intent key and expose assertions that conflict flow yields `0` residual reserved units. Keep existing D-15 conflict payload checks unchanged while adding explicit no-side-effect assertions from 04-VERIFICATION gap.missing.</action>
  <verify>
    <automated>dotnet test tests/UnitTests/UnitTests.csproj --filter "FullyQualifiedName~CheckoutServiceTests" && dotnet test tests/IntegrationTests/IntegrationTests.csproj --filter "FullyQualifiedName~CheckoutEndpointsTests"</automated>
  </verify>
  <done>Tests fail only on missing compensation behavior, and contracts now require a release path for checkout rollback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement compensation-aware checkout orchestration and gateway release wiring</name>
  <files>src/Application/Checkout/Services/CheckoutService.cs, src/Infrastructure/Checkout/CheckoutServiceDependencies.cs, tests/UnitTests/Checkout/CheckoutServiceTests.cs, tests/IntegrationTests/Checkout/CheckoutEndpointsTests.cs</files>
  <behavior>
    - Test 1: Service records successful reserve operations and compensates all successes when any later reserve conflicts (D-14).
    - Test 2: Conflict path leaves no persisted order, no cart clear, and no residual reservation state for that checkout attempt (D-14).
    - Test 3: Success path still persists one order and clears cart once (D-16).
  </behavior>
  <action>Update `CheckoutService.SubmitCheckoutAsync` to track reserve progress and execute compensation via `ReleaseCheckoutReservationAsync` before throwing `CheckoutReservationConflictException` on any conflict (D-14). Implement the new release method in `CheckoutInventoryGateway` by delegating to `InventoryService.ReleaseReservationAsync(new ReleaseReservationRequest(orderIntentKey, ReservationReleaseReason.OrderCanceled), ...)`. Ensure the implementation never swallows compensation failures silently; if compensation fails, throw a deterministic exception that prevents order persistence and cart clear. Preserve existing D-15 conflict mapping contract and D-16 success semantics.</action>
  <verify>
    <automated>dotnet test tests/UnitTests/UnitTests.csproj --filter "FullyQualifiedName~CheckoutServiceTests" && dotnet test tests/IntegrationTests/IntegrationTests.csproj --filter "FullyQualifiedName~CheckoutEndpointsTests|FullyQualifiedName~CheckoutPersistenceContractTests" && dotnet build backend.slnx -v minimal</automated>
  </verify>
  <done>Checkout conflict path is fail-all with proven compensation and zero partial reservation side effects; success path remains intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checkout service → inventory service | Reservation and compensation cross subsystem boundary where partial failure can corrupt stock integrity. |
| API conflict path → client ProblemDetails | Internal conflict details are serialized to external clients and must remain minimal/non-sensitive. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-16 | T | CheckoutService reserve loop | mitigate | Compensate all prior successful reserves before throwing any aggregate conflict (D-14). |
| T-04-17 | D | Inventory reservation pool | mitigate | Prevent reservation leakage by asserting zero residual reservation after failed multi-line submit in unit/integration tests. |
| T-04-18 | I | Conflict response payload | mitigate | Keep 409 `lineConflicts` payload limited to product/requested/available quantities (D-15). |
</threat_model>

<verification>
- dotnet test tests/UnitTests/UnitTests.csproj --filter "FullyQualifiedName~CheckoutServiceTests"
- dotnet test tests/IntegrationTests/IntegrationTests.csproj --filter "FullyQualifiedName~CheckoutEndpointsTests|FullyQualifiedName~CheckoutPersistenceContractTests"
- dotnet build backend.slnx -v minimal
</verification>

<success_criteria>
- Verification gap truth for D-14 is fully closed: any checkout conflict leaves no partial reservation side effects.
- Contract-level and API-level tests explicitly assert residual reserved quantity is zero after conflict.
- Existing D-15 409 conflict details and D-16 success behavior remain green.
</success_criteria>

<output>
After completion, create `.planning/phases/04-cart-checkout-capture/04-05-SUMMARY.md`
</output>
