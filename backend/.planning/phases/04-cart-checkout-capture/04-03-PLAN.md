---
phase: 04-cart-checkout-capture
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - src/Infrastructure/Persistence/AppDbContext.cs
  - src/Infrastructure/Persistence/Configurations/CartConfiguration.cs
  - src/Infrastructure/Persistence/Configurations/CartLineConfiguration.cs
  - src/Infrastructure/Persistence/Configurations/OrderConfiguration.cs
  - src/Infrastructure/Persistence/Configurations/OrderItemSnapshotConfiguration.cs
  - src/Infrastructure/Persistence/Configurations/DeliveryInstructionConfiguration.cs
  - src/Infrastructure/Checkout/Repositories/CartRepository.cs
  - src/Infrastructure/Checkout/Repositories/CheckoutRepository.cs
  - src/Infrastructure/DependencyInjection.cs
  - src/Infrastructure/Persistence/Migrations/*
  - tests/IntegrationTests/Checkout/CheckoutPersistenceContractTests.cs
autonomous: true
requirements:
  - CHK-01
  - CHK-02
  - CHK-03
must_haves:
  truths:
    - "Cart and checkout state persists durably in PostgreSQL across requests/restarts."
    - "Order snapshots and delivery instructions are persisted with immutable/read-safe schema constraints."
    - "Schema is migrated to live development database before verification (blocking push)."
  artifacts:
    - path: src/Infrastructure/Checkout/Repositories/CartRepository.cs
      provides: EF persistence for cart load/save/clear semantics
      exports: [GetByCustomerIdAsync, SaveAsync, ClearAsync]
    - path: src/Infrastructure/Checkout/Repositories/CheckoutRepository.cs
      provides: EF persistence for checkout order and snapshot reads
      exports: [SaveOrderAsync, GetOrderByIdAsync]
    - path: src/Infrastructure/Persistence/Migrations
      provides: migration adding cart/order/snapshot/instruction schema
      contains: "AddCartCheckoutCapture"
    - path: tests/IntegrationTests/Checkout/CheckoutPersistenceContractTests.cs
      provides: persisted contract proof for snapshot immutability and clear-on-success
      min_lines: 160
  key_links:
    - from: src/Infrastructure/DependencyInjection.cs
      to: checkout/cart repositories
      via: DI registration
      pattern: "AddScoped<ICartRepository"
    - from: src/Infrastructure/Persistence/AppDbContext.cs
      to: checkout entities
      via: DbSet registration
      pattern: "DbSet<Order"
---

<objective>
Wire checkout/cart persistence and database schema so Phase 04 business contracts from plans 01-02 become durable and verifiable against real PostgreSQL.

Purpose: Prevent false-positive API behavior by ensuring cart/order/snapshot/instruction data model exists and is migrated before final verification.
Output: EF configurations, repositories, migration/schema push, and persistence integration tests.
</objective>

<execution_context>
@/home/natan/workspace/Site-Tibia/.opencode/get-shit-done/workflows/execute-plan.md
@/home/natan/workspace/Site-Tibia/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-cart-checkout-capture/04-CONTEXT.md
@.planning/phases/04-cart-checkout-capture/04-RESEARCH.md
@.planning/phases/04-cart-checkout-capture/04-01-PLAN.md
@.planning/phases/04-cart-checkout-capture/04-02-PLAN.md
@src/Infrastructure/Persistence/AppDbContext.cs
@src/Infrastructure/Persistence/Configurations/ProductConfiguration.cs
@src/Infrastructure/Persistence/Configurations/InventoryReservationConfiguration.cs
@src/Infrastructure/Inventory/Repositories/InventoryRepository.cs

<interfaces>
From src/Infrastructure/Persistence/AppDbContext.cs:
```csharp
public sealed class AppDbContext : DbContext
{
    public DbSet<Product> Products => Set<Product>();
    public DbSet<InventoryStock> InventoryStocks => Set<InventoryStock>();
}
```

From src/Infrastructure/DependencyInjection.cs:
```csharp
services.AddScoped<IInventoryRepository, InventoryRepository>();
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add checkout/cart persistence mappings and integration RED tests</name>
  <read_first>src/Infrastructure/Persistence/AppDbContext.cs, src/Infrastructure/Persistence/Configurations/ProductConfiguration.cs, tests/IntegrationTests/Inventory/InventoryPersistenceContractTests.cs, .planning/phases/04-cart-checkout-capture/04-02-PLAN.md</read_first>
  <files>src/Infrastructure/Persistence/AppDbContext.cs, src/Infrastructure/Persistence/Configurations/CartConfiguration.cs, src/Infrastructure/Persistence/Configurations/CartLineConfiguration.cs, src/Infrastructure/Persistence/Configurations/OrderConfiguration.cs, src/Infrastructure/Persistence/Configurations/OrderItemSnapshotConfiguration.cs, src/Infrastructure/Persistence/Configurations/DeliveryInstructionConfiguration.cs, src/Infrastructure/Checkout/Repositories/CartRepository.cs, src/Infrastructure/Checkout/Repositories/CheckoutRepository.cs, src/Infrastructure/DependencyInjection.cs, tests/IntegrationTests/Checkout/CheckoutPersistenceContractTests.cs</files>
  <behavior>
    - Test 1: Cart save/load preserves merged single-line semantics per product (D-01) and absolute quantity state (D-02).
    - Test 2: Saved order item snapshots persist immutable fields and remain unaffected by later catalog updates (D-11, D-12).
    - Test 3: Delivery instruction persistence stores automated/manual required fields for each line (D-05..D-08).
    - Test 4: Successful checkout persistence path can clear cart state for customer (D-16).
  </behavior>
  <action>Create checkout/cart EF entities and configurations with concrete constraints: one cart per customer unique index, one cart line per `(cart_id, product_id)` unique index, order snapshot columns `unit_price numeric(18,2)`, `currency varchar(3)`, `product_name`, `product_slug`, `category_slug`, and immutable-at-domain write flow (no update repository method for snapshots). Add delivery instruction table linked to order line with fulfillment-type discriminator and nullable branch columns validated by service layer. Implement `CartRepository` + `CheckoutRepository` and register `ICartRepository`/`ICheckoutRepository` in DI. Write integration tests first to prove persisted contracts before implementation.</action>
  <acceptance_criteria>
    - `AppDbContext.cs` declares `DbSet` entries for cart, cart lines, orders, snapshots, delivery instructions.
    - `CartLineConfiguration.cs` contains unique index on cart/product combination.
    - `OrderItemSnapshotConfiguration.cs` maps `UnitPrice` as `numeric(18,2)` and `Currency` max length 3.
    - `CheckoutPersistenceContractTests.cs` includes assertion that order reads return stored snapshot values after product price/name changes.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test tests/IntegrationTests/IntegrationTests.csproj --filter "FullyQualifiedName~CheckoutPersistenceContractTests"</automated>
  </verify>
  <done>Checkout/cart persistence contracts are implemented and integration-proven against EF mappings.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Generate/apply migration and push schema before verification</name>
  <read_first>src/Infrastructure/Persistence/AppDbContext.cs, src/Infrastructure/Persistence/Migrations/AppDbContextModelSnapshot.cs, .planning/phases/04-cart-checkout-capture/04-RESEARCH.md</read_first>
  <files>src/Infrastructure/Persistence/Migrations/*</files>
  <action>Generate migration named `AddCartCheckoutCapture` after checkout/cart mappings are complete. Ensure migration creates tables/indexes/FKs for carts, cart_lines, orders, order_item_snapshots, and delivery_instructions. Then run blocking push command exactly: `dotnet ef database update --project src/Infrastructure/Infrastructure.csproj --startup-project src/API/API.csproj --context AppDbContext`. If local DB is unavailable, start required Postgres container/service first, then apply migration. Do not proceed to API verification without successful schema update.</action>
  <acceptance_criteria>
    - New migration files exist under `src/Infrastructure/Persistence/Migrations/` with name containing `AddCartCheckoutCapture`.
    - `AppDbContextModelSnapshot.cs` includes checkout/cart entities and constraints.
    - `dotnet ef database update ...` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>dotnet ef database update --project src/Infrastructure/Infrastructure.csproj --startup-project src/API/API.csproj --context AppDbContext && dotnet test tests/IntegrationTests/IntegrationTests.csproj --filter "FullyQualifiedName~CheckoutPersistenceContractTests"</automated>
  </verify>
  <done>Database schema is live and persistence tests pass on migrated schema.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| application checkout/cart contracts → PostgreSQL schema | Untrusted/derived values become persistent business records. |
| migration execution → live development DB | Schema drift risk if push step skipped or partial. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | T | Snapshot persistence layer | mitigate | Persist immutable snapshot columns and omit mutable update paths for order-item snapshots (D-11). |
| T-04-09 | D | Checkout durability | mitigate | Enforce migration + blocking schema push before verification to prevent runtime/persistence mismatch. |
| T-04-10 | R | Cart line state integrity | mitigate | Enforce unique `(cart_id, product_id)` index to preserve merged-line semantics at DB boundary (D-01). |
| T-04-11 | I | Delivery instruction storage | mitigate | Persist only required instruction fields by fulfillment type and verify via integration contract tests (D-05..D-08). |
</threat_model>

<verification>
- dotnet ef database update --project src/Infrastructure/Infrastructure.csproj --startup-project src/API/API.csproj --context AppDbContext
- dotnet test tests/IntegrationTests/IntegrationTests.csproj --filter "FullyQualifiedName~CheckoutPersistenceContractTests"
- dotnet build backend.slnx -v minimal
</verification>

<success_criteria>
- Checkout/cart persistence schema exists, migrated, and tested against real database.
- Blocking schema push is complete before API-level verification.
</success_criteria>

<output>
After completion, create `.planning/phases/04-cart-checkout-capture/04-03-SUMMARY.md`
</output>
