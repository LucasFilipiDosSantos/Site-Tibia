---
phase: 04-cart-checkout-capture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Domain/Checkout/Cart.cs
  - src/Domain/Checkout/CartLine.cs
  - src/Application/Checkout/Contracts/CartContracts.cs
  - src/Application/Checkout/Contracts/ICartRepository.cs
  - src/Application/Checkout/Contracts/ICartProductAvailabilityGateway.cs
  - src/Application/Checkout/Services/CartService.cs
  - tests/UnitTests/Checkout/CartServiceTests.cs
autonomous: true
requirements:
  - CHK-01
must_haves:
  truths:
    - "Authenticated customer can add products to cart and re-adding same product merges quantity (D-01)."
    - "Cart quantity update sets final quantity explicitly instead of delta math (D-02)."
    - "Cart add/update rejects stock overflow with deterministic conflict contract (D-03)."
    - "Cart line deletion is explicit remove operation, not quantity-zero shorthand (D-04)."
  artifacts:
    - path: src/Application/Checkout/Services/CartService.cs
      provides: cart add/set/remove orchestration with stock conflict checks
      exports: [AddItemAsync, SetItemQuantityAsync, RemoveItemAsync, GetCartAsync]
    - path: src/Application/Checkout/Contracts/CartContracts.cs
      provides: cart command/query and response contracts used by API + tests
      min_lines: 60
    - path: tests/UnitTests/Checkout/CartServiceTests.cs
      provides: executable proof of D-01, D-02, D-03, D-04
      min_lines: 160
  key_links:
    - from: src/Application/Checkout/Services/CartService.cs
      to: src/Application/Checkout/Contracts/ICartProductAvailabilityGateway.cs
      via: availability validation before mutation
      pattern: "GetAvailabilityAsync"
    - from: src/Application/Checkout/Services/CartService.cs
      to: src/Application/Checkout/Contracts/ICartRepository.cs
      via: load/save cart aggregate
      pattern: "SaveAsync"
---

<objective>
Define and implement cart behavior contracts so CHK-01 is delivered with explicit locked semantics for merge, absolute quantity updates, conflict handling, and explicit remove operations.

Purpose: Establish the cart core as a deterministic, test-driven boundary before checkout orchestration and persistence.
Output: Cart domain entities, application contracts/ports, service orchestration, and red→green unit proof.
</objective>

<execution_context>
@/home/natan/workspace/Site-Tibia/.opencode/get-shit-done/workflows/execute-plan.md
@/home/natan/workspace/Site-Tibia/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-cart-checkout-capture/04-CONTEXT.md
@.planning/phases/03-inventory-integrity-reservation-control/03-03-SUMMARY.md
@src/Application/Inventory/Contracts/InventoryContracts.cs
@src/Application/Inventory/Services/InventoryService.cs

<interfaces>
From src/Application/Inventory/Contracts/InventoryContracts.cs:
```csharp
public sealed record InventoryAvailabilityResponse(int Available, int Reserved, int Total);
public sealed class InventoryReservationConflictException : InvalidOperationException
{
    public int AvailableQuantity { get; }
}
```

From src/Application/Inventory/Services/InventoryService.cs:
```csharp
public Task<InventoryAvailabilityResponse> GetAvailabilityAsync(
    GetInventoryAvailabilityRequest request,
    CancellationToken cancellationToken = default);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create cart contracts and failing unit tests for locked cart rules</name>
  <read_first>.planning/phases/04-cart-checkout-capture/04-CONTEXT.md, src/Application/Catalog/Contracts/CatalogContracts.cs, tests/UnitTests/Inventory/InventoryReservationServiceTests.cs</read_first>
  <files>src/Domain/Checkout/Cart.cs, src/Domain/Checkout/CartLine.cs, src/Application/Checkout/Contracts/CartContracts.cs, src/Application/Checkout/Contracts/ICartRepository.cs, src/Application/Checkout/Contracts/ICartProductAvailabilityGateway.cs, tests/UnitTests/Checkout/CartServiceTests.cs</files>
  <behavior>
    - Test 1: Add same product twice merges into one line and increments quantity (D-01).
    - Test 2: Set quantity uses absolute final quantity and replaces prior value (D-02).
    - Test 3: Remove uses explicit remove command and does not rely on quantity zero (D-04).
    - Test 4: Add/set exceeding available stock throws checkout/cart conflict contract containing available quantity (D-03).
  </behavior>
  <action>Create cart-focused domain/contracts with exact command shapes: `AddCartItemRequest(customerId, productId, quantity)`, `SetCartItemQuantityRequest(customerId, productId, quantity)`, `RemoveCartItemRequest(customerId, productId)`. In tests, encode D-01/D-02/D-04 using these exact operations and assert no quantity-zero deletion path is accepted. Encode D-03 by stubbing `ICartProductAvailabilityGateway.GetAvailabilityAsync(productId)` to return a lower available count and assert conflict exception exposes `ProductId`, `RequestedQuantity`, and `AvailableQuantity` fields.</action>
  <acceptance_criteria>
    - `CartContracts.cs` contains request records named exactly `AddCartItemRequest`, `SetCartItemQuantityRequest`, and `RemoveCartItemRequest`.
    - `CartServiceTests.cs` contains test names including `MergesExistingLine`, `SetQuantityUsesAbsoluteValue`, `RemoveUsesExplicitCommand`, and `ExceedsStockThrowsConflict`.
    - `CartServiceTests.cs` has no branch using quantity=0 as delete shorthand.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test tests/UnitTests/UnitTests.csproj --filter "FullyQualifiedName~CartServiceTests"</automated>
  </verify>
  <done>RED tests fully encode D-01 through D-04 behavior contracts and compile against explicit cart interfaces.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement CartService to satisfy merge/set/remove/conflict tests</name>
  <read_first>src/Domain/Checkout/Cart.cs, src/Application/Checkout/Contracts/CartContracts.cs, src/Application/Checkout/Contracts/ICartRepository.cs, tests/UnitTests/Checkout/CartServiceTests.cs</read_first>
  <files>src/Application/Checkout/Services/CartService.cs</files>
  <behavior>
    - Test 1: Re-adding same product mutates existing line quantity instead of duplicating line (D-01).
    - Test 2: Quantity set writes exact provided value and fails for non-positive quantity (D-02).
    - Test 3: Remove command deletes line only when explicit remove endpoint/service method is called (D-04).
    - Test 4: Stock conflicts throw deterministic conflict exception with available quantity (D-03).
  </behavior>
  <action>Implement `CartService` with methods `AddItemAsync`, `SetItemQuantityAsync`, `RemoveItemAsync`, `GetCartAsync`. For D-01, enforce single line per product in aggregate and increment existing line quantity on add. For D-02, update line quantity to exact requested value (no +/- logic). For D-03, call `ICartProductAvailabilityGateway.GetAvailabilityAsync` before add/set and throw `CartStockConflictException` with per-product available quantity when requested quantity exceeds availability. For D-04, only remove via `RemoveItemAsync`; reject quantity <=0 in add/set paths with `ArgumentOutOfRangeException`.</action>
  <acceptance_criteria>
    - `CartService.cs` contains methods `AddItemAsync`, `SetItemQuantityAsync`, `RemoveItemAsync`, `GetCartAsync`.
    - `CartService.cs` calls `ICartProductAvailabilityGateway.GetAvailabilityAsync` in both add and set flows.
    - `CartService.cs` throws `CartStockConflictException` with available quantity payload.
    - `dotnet test ...CartServiceTests` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test tests/UnitTests/UnitTests.csproj --filter "FullyQualifiedName~CartServiceTests" && dotnet build backend.slnx -v minimal</automated>
  </verify>
  <done>CHK-01 cart semantics are implemented and test-proven with full D-01..D-04 fidelity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → cart service input | Untrusted product/quantity input crosses into state mutation paths. |
| cart service → stock availability gateway | External availability data influences cart acceptance/rejection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | T | Cart quantity mutation | mitigate | Validate quantity > 0 and use explicit add/set/remove operations per D-02/D-04 to prevent ambiguous mutation paths. |
| T-04-02 | D | Add/update oversell path | mitigate | Enforce pre-mutation availability checks and throw structured conflict exception with available quantity (D-03). |
| T-04-03 | R | Cart line identity ambiguity | mitigate | Merge by product id into one line and keep deterministic line recalculation rules (D-01). |
</threat_model>

<verification>
- dotnet test tests/UnitTests/UnitTests.csproj --filter "FullyQualifiedName~CartServiceTests"
- dotnet build backend.slnx -v minimal
</verification>

<success_criteria>
- Cart service behavior satisfies D-01, D-02, D-03, D-04 with automated unit proof.
- CHK-01 cart mutation contract is stable and ready for persistence/API wiring.
</success_criteria>

<output>
After completion, create `.planning/phases/04-cart-checkout-capture/04-01-SUMMARY.md`
</output>
