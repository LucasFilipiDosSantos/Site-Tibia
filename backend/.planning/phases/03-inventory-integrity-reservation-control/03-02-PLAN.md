---
phase: 03-inventory-integrity-reservation-control
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/Infrastructure/Persistence/AppDbContext.cs
  - src/Infrastructure/Persistence/Configurations/InventoryStockConfiguration.cs
  - src/Infrastructure/Persistence/Configurations/InventoryReservationConfiguration.cs
  - src/Infrastructure/Persistence/Configurations/StockAdjustmentAuditConfiguration.cs
  - src/Infrastructure/Inventory/Repositories/InventoryRepository.cs
  - src/Infrastructure/DependencyInjection.cs
  - src/Infrastructure/Persistence/Migrations/*
  - tests/IntegrationTests/Inventory/InventoryPersistenceContractTests.cs
autonomous: true
requirements:
  - INV-01
  - INV-02
  - INV-04
must_haves:
  truths:
    - "Available/reserved/total inventory values are persisted and read correctly from the database in real time."
    - "Reservation write path atomically enforces sufficiency and persists order-intent idempotency key uniqueness."
    - "Admin adjustments persist audit rows with complete required fields."
  artifacts:
    - path: src/Infrastructure/Inventory/Repositories/InventoryRepository.cs
      provides: EF-backed inventory reserve/release/availability/adjustment persistence
      exports: [TryReserveAsync, ReleaseByOrderIntentKeyAsync, GetAvailabilityAsync, ApplyAdjustmentAsync]
    - path: src/Infrastructure/Persistence/Migrations
      provides: schema for inventory stock, reservations, and adjustment audits
      contains: "AddInventoryIntegrityAndReservations"
    - path: tests/IntegrationTests/Inventory/InventoryPersistenceContractTests.cs
      provides: integration proof of constraints and transactional behavior
      min_lines: 120
  key_links:
    - from: src/Infrastructure/DependencyInjection.cs
      to: src/Infrastructure/Inventory/Repositories/InventoryRepository.cs
      via: DI registration
      pattern: "AddScoped<IInventoryRepository, InventoryRepository>"
    - from: src/Infrastructure/Persistence/AppDbContext.cs
      to: inventory entity DbSets
      via: EF model registration
      pattern: "DbSet<Inventory"
---

<objective>
Implement persistent inventory storage and transactional reservation/adjustment behavior, including migration and blocking schema push before API wiring.

Purpose: Make inventory correctness durable and enforce concurrency/constraint behavior at database level for D-05, D-08, and D-12.
Output: Inventory EF configurations, repository implementation, migration/schema update, and integration persistence tests.
</objective>

<execution_context>
@/home/natan/workspace/Site-Tibia/.opencode/get-shit-done/workflows/execute-plan.md
@/home/natan/workspace/Site-Tibia/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-inventory-integrity-reservation-control/03-CONTEXT.md
@.planning/phases/03-inventory-integrity-reservation-control/03-RESEARCH.md
@.planning/phases/03-inventory-integrity-reservation-control/03-01-PLAN.md
@src/Infrastructure/Persistence/AppDbContext.cs
@src/Infrastructure/Persistence/Configurations/ProductConfiguration.cs
@src/Infrastructure/Catalog/Repositories/ProductRepository.cs
@src/Infrastructure/Persistence/Migrations/20260416180310_AddCatalogAndCategoryGovernance.cs

<interfaces>
From src/Infrastructure/Persistence/AppDbContext.cs:
```csharp
public sealed class AppDbContext : DbContext
{
    public DbSet<Product> Products => Set<Product>();
}
```

From src/Infrastructure/Catalog/Repositories/ProductRepository.cs:
```csharp
public sealed class ProductRepository : IProductRepository
{
    // EF repository pattern baseline
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add inventory persistence model, repository, and integration RED tests</name>
  <read_first>.planning/phases/03-inventory-integrity-reservation-control/03-01-PLAN.md, src/Infrastructure/Persistence/AppDbContext.cs, src/Infrastructure/Catalog/Repositories/ProductRepository.cs, tests/IntegrationTests/Catalog/CatalogPersistenceContractTests.cs</read_first>
  <files>src/Infrastructure/Persistence/AppDbContext.cs, src/Infrastructure/Persistence/Configurations/InventoryStockConfiguration.cs, src/Infrastructure/Persistence/Configurations/InventoryReservationConfiguration.cs, src/Infrastructure/Persistence/Configurations/StockAdjustmentAuditConfiguration.cs, src/Infrastructure/Inventory/Repositories/InventoryRepository.cs, src/Infrastructure/DependencyInjection.cs, tests/IntegrationTests/Inventory/InventoryPersistenceContractTests.cs</files>
  <behavior>
    - Test 1: Reserve operation succeeds only when `available >= requested` in one transactional write (D-05).
    - Test 2: Duplicate `orderIntentKey` does not create a second reservation row (D-07).
    - Test 3: Admin stock adjustment uses optimistic concurrency token and retries stale update path (D-08).
    - Test 4: Audit row persists all required fields (D-15).
  </behavior>
  <action>Create inventory tables/entity mappings for stock, reservation, and adjustment audit with concrete constraints: unique index on `(order_intent_key, product_id)`, non-negative check constraints for total/reserved quantities, and concurrency token (`xmin` or explicit row-version column) on stock row for optimistic concurrency (D-08). Implement `InventoryRepository` EF logic for transactional reserve/release/availability/adjustment operations and wire `IInventoryRepository` in DI. Write integration tests first to verify transactional reserve predicate behavior, idempotency uniqueness, concurrency conflict retry handling, and audit row persistence shape.</action>
  <acceptance_criteria>
    - `InventoryReservationConfiguration.cs` contains unique index for `OrderIntentKey` + `ProductId`.
    - `InventoryStockConfiguration.cs` contains concurrency token mapping.
    - `InventoryRepository.cs` contains explicit transaction usage for reserve path.
    - `InventoryPersistenceContractTests.cs` contains assertions for all audit fields: admin identity, product identity, delta, before, after, reason, timestamp.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test tests/IntegrationTests/IntegrationTests.csproj --filter "FullyQualifiedName~InventoryPersistenceContractTests"</automated>
  </verify>
  <done>Persistence layer enforces D-05, D-07, D-08, and D-15 semantics with integration proof.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Generate/apply inventory migration and push schema before verification</name>
  <read_first>src/Infrastructure/Persistence/Migrations/20260416180310_AddCatalogAndCategoryGovernance.cs, src/Infrastructure/Persistence/AppDbContext.cs, .planning/phases/03-inventory-integrity-reservation-control/03-RESEARCH.md</read_first>
  <files>src/Infrastructure/Persistence/Migrations/*</files>
  <action>Generate EF migration named `AddInventoryIntegrityAndReservations` after inventory entity/configuration changes. Ensure migration creates inventory stock/reservation/audit tables, indexes, check constraints, and FK relationships. Then run schema push command `dotnet ef database update --project src/Infrastructure/Infrastructure.csproj --startup-project src/API/API.csproj --context AppDbContext` to apply schema to live development database before any final verification. If environment lacks DB, start required local postgres service first and then apply migration; do not skip push step.</action>
  <acceptance_criteria>
    - New migration files exist under `src/Infrastructure/Persistence/Migrations/` with `AddInventoryIntegrityAndReservations` name.
    - `AppDbContextModelSnapshot` includes inventory entities and constraints.
    - `dotnet ef database update ...` exits with code 0.
  </acceptance_criteria>
  <verify>
    <automated>dotnet ef database update --project src/Infrastructure/Infrastructure.csproj --startup-project src/API/API.csproj --context AppDbContext && dotnet test tests/IntegrationTests/IntegrationTests.csproj --filter "FullyQualifiedName~InventoryPersistenceContractTests"</automated>
  </verify>
  <done>Inventory schema is applied to database and integration persistence contracts pass against migrated schema.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Application inventory commands → PostgreSQL | Untrusted command inputs become transactional write operations. |
| EF repository → schema constraints | Domain invariants rely on DB-enforced predicates and uniqueness. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | T | Reserve transaction | mitigate | Perform reserve in single transaction with availability predicate and rollback on insufficiency. |
| T-03-06 | R | Reservation idempotency key | mitigate | Enforce unique order-intent key + product index and repository replay path for duplicates. |
| T-03-07 | D | Concurrent stock writes | mitigate | Use optimistic concurrency token and bounded retry semantics for admin stock updates. |
| T-03-08 | R | Adjustment audit persistence | mitigate | Persist immutable audit entity with required actor/product/delta/before/after/reason/timestamp fields. |
</threat_model>

<verification>
- dotnet ef database update --project src/Infrastructure/Infrastructure.csproj --startup-project src/API/API.csproj --context AppDbContext
- dotnet test tests/IntegrationTests/IntegrationTests.csproj --filter "FullyQualifiedName~InventoryPersistenceContractTests"
- dotnet build backend.slnx -v minimal
</verification>

<success_criteria>
- Inventory schema + repository logic are durable, transactional, and idempotent under integration tests.
- Blocking schema push has been executed before phase verification.
</success_criteria>

<output>
After completion, create `.planning/phases/03-inventory-integrity-reservation-control/03-02-SUMMARY.md`
</output>
