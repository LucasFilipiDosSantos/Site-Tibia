---
phase: 03-inventory-integrity-reservation-control
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Domain/Inventory/InventoryStock.cs
  - src/Domain/Inventory/InventoryReservation.cs
  - src/Domain/Inventory/StockAdjustmentAudit.cs
  - src/Application/Inventory/Contracts/InventoryContracts.cs
  - src/Application/Inventory/Contracts/IInventoryRepository.cs
  - src/Application/Inventory/Services/InventoryService.cs
  - tests/UnitTests/Inventory/InventoryReservationServiceTests.cs
  - tests/UnitTests/Inventory/InventoryAdjustmentInvariantTests.cs
autonomous: true
requirements:
  - INV-02
  - INV-03
  - INV-04
must_haves:
  truths:
    - "System can reserve stock at checkout submit and avoid duplicate reservation for the same order intent key."
    - "Reservation expires after 15 minutes and stock becomes available again."
    - "Admin adjustment requests reject negative resulting stock and missing reason text."
  artifacts:
    - path: src/Application/Inventory/Services/InventoryService.cs
      provides: reservation, release, availability, and adjustment orchestration
      exports: [ReserveStockForCheckoutAsync, ReleaseReservationAsync, GetAvailabilityAsync, AdjustStockAsync]
    - path: src/Application/Inventory/Contracts/InventoryContracts.cs
      provides: typed inventory command/response contracts
      min_lines: 40
    - path: tests/UnitTests/Inventory/InventoryReservationServiceTests.cs
      provides: reservation idempotency, conflict, and TTL expiry behavior coverage
      min_lines: 80
  key_links:
    - from: src/Application/Inventory/Services/InventoryService.cs
      to: src/Application/Inventory/Contracts/IInventoryRepository.cs
      via: repository port calls per use-case method
      pattern: "_inventoryRepository\\."
    - from: tests/UnitTests/Inventory/InventoryReservationServiceTests.cs
      to: src/Application/Inventory/Services/InventoryService.cs
      via: direct unit test behavior assertions
      pattern: "InventoryService"
---

<objective>
Define the inventory domain/application contracts and invariants that enforce reservation lifecycle, idempotency, and admin adjustment rules before persistence/API wiring.

Purpose: Establish locked-decision behavior (D-01..D-04, D-07, D-10, D-13..D-16) in testable inner-layer code.
Output: Inventory domain entities, repository contracts, inventory service orchestration, and focused unit tests.
</objective>

<execution_context>
@/home/natan/workspace/Site-Tibia/.opencode/get-shit-done/workflows/execute-plan.md
@/home/natan/workspace/Site-Tibia/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-inventory-integrity-reservation-control/03-CONTEXT.md
@.planning/phases/03-inventory-integrity-reservation-control/03-RESEARCH.md
@src/Application/Catalog/Services/CatalogService.cs
@src/Application/Catalog/Contracts/CatalogContracts.cs

<interfaces>
From src/Application/Catalog/Services/CatalogService.cs:
```csharp
public sealed class CatalogService
{
    // service pattern: validate -> repository calls -> explicit exceptions
}
```

From src/Application/Catalog/Contracts/CatalogContracts.cs:
```csharp
public sealed record ProductBySlugResponse(string Name, string Slug, string Description, decimal Price, string CategorySlug);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define inventory contracts and failing reservation/adjustment tests</name>
  <read_first>.planning/phases/03-inventory-integrity-reservation-control/03-CONTEXT.md, src/Application/Catalog/Contracts/CatalogContracts.cs, tests/UnitTests/Catalog/CatalogServiceFilterAndPaginationTests.cs</read_first>
  <files>src/Application/Inventory/Contracts/InventoryContracts.cs, src/Application/Inventory/Contracts/IInventoryRepository.cs, tests/UnitTests/Inventory/InventoryReservationServiceTests.cs, tests/UnitTests/Inventory/InventoryAdjustmentInvariantTests.cs</files>
  <behavior>
    - Test 1: Reserve call for same `orderIntentKey` does not double-reserve quantity (D-07).
    - Test 2: Reservation TTL is fixed at 15 minutes and expired reservations are releasable (D-02).
    - Test 3: Admin adjustment rejects empty reason and rejects any adjustment yielding negative stock (D-14, D-16).
  </behavior>
  <action>Create inventory request/response contracts with concrete fields: `orderIntentKey`, `orderId`, `productId`, `quantity`, `reservationExpiresAtUtc`, and availability payload `{available, reserved, total}` (D-09). Add `IInventoryRepository` methods for reserve, release, availability, adjustment, and reservation lookup by intent key. Write failing unit tests for idempotent reserve, 15-minute TTL expiry, immediate release behavior trigger contract, delta-only adjustments, required reason, and negative-stock blocking; tests must fail before service implementation exists.</action>
  <acceptance_criteria>
    - `InventoryContracts.cs` contains `record InventoryAvailabilityResponse(int Available, int Reserved, int Total)`.
    - `IInventoryRepository.cs` contains `GetReservationByIntentKeyAsync` and `TryReserveAsync` method signatures.
    - `InventoryReservationServiceTests.cs` includes explicit assertion for 15-minute expiry (`AddMinutes(15)`).
    - `InventoryAdjustmentInvariantTests.cs` includes explicit assertion for missing reason rejection.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test tests/UnitTests/UnitTests.csproj --filter "FullyQualifiedName~InventoryReservationServiceTests|FullyQualifiedName~InventoryAdjustmentInvariantTests"</automated>
  </verify>
  <done>Inventory contracts and RED tests exist and encode D-02, D-07, D-09, D-14, and D-16 behaviors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement inventory domain entities and service orchestration to satisfy tests</name>
  <read_first>tests/UnitTests/Inventory/InventoryReservationServiceTests.cs, tests/UnitTests/Inventory/InventoryAdjustmentInvariantTests.cs, src/Domain/Catalog/Product.cs, src/Application/Catalog/Services/CatalogService.cs</read_first>
  <files>src/Domain/Inventory/InventoryStock.cs, src/Domain/Inventory/InventoryReservation.cs, src/Domain/Inventory/StockAdjustmentAudit.cs, src/Application/Inventory/Services/InventoryService.cs</files>
  <behavior>
    - Test 1: Reserve is created only at checkout submit flow and stores aggregate quantity by product line (D-01, D-04).
    - Test 2: Release operation is explicit for payment-failure/cancel trigger and returns released quantity (D-03).
    - Test 3: Pre-check sufficiency is advisory; authoritative failure path comes from reserve outcome and exposes available quantity (D-10, D-11).
  </behavior>
  <action>Implement inventory domain models with explicit fields for stock totals, reserved totals, and reservation status timestamps. Implement `InventoryService` with methods `ReserveStockForCheckoutAsync`, `ReleaseReservationAsync`, `GetAvailabilityAsync`, and `AdjustStockAsync`; enforce reserve-on-checkout behavior (D-01), 15-minute TTL calculation (D-02), idempotency via prior intent-key lookup (D-07), and delta-only adjustment command validation (D-13). Throw deterministic conflict exception carrying `availableQuantity` when reserve cannot satisfy requested quantity (D-06, D-11). Keep all reads against repository each call without cache state (D-12).</action>
  <acceptance_criteria>
    - `InventoryService.cs` contains `ReserveStockForCheckoutAsync` and `ReleaseReservationAsync`.
    - `InventoryService.cs` contains explicit `AddMinutes(15)` TTL logic.
    - `InventoryService.cs` contains conflict exception creation with available quantity payload.
    - `StockAdjustmentAudit.cs` contains fields for admin identity, product identity, delta, before/after, reason, and timestamp.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test tests/UnitTests/UnitTests.csproj --filter "FullyQualifiedName~Inventory"</automated>
  </verify>
  <done>Unit tests pass and inner-layer inventory behavior fully encodes D-01..D-04, D-07, D-10, D-11, D-13..D-16.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Checkout command → Application service | Untrusted quantity and intent key enter reservation logic. |
| Admin adjustment command → Application service | Untrusted adjustment delta/reason enter stock mutation rules. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | T | InventoryService reserve path | mitigate | Require repository intent-key lookup before reserve and reject double-reserve writes per D-07. |
| T-03-02 | D | Reservation TTL handling | mitigate | Enforce fixed 15-minute expiry and release-expired flow in service methods; covered by unit tests. |
| T-03-03 | E | Admin adjustment command | mitigate | Validate delta-only input, required reason, and negative resulting stock guard before mutation. |
| T-03-04 | R | Adjustment audit model | mitigate | Require audit entity fields for actor, delta, before/after, reason, and timestamp at service write boundary. |
</threat_model>

<verification>
- dotnet test tests/UnitTests/UnitTests.csproj --filter "FullyQualifiedName~Inventory"
</verification>

<success_criteria>
- Inventory domain/application code expresses reservation and adjustment invariants with passing unit tests.
- No deferred ideas introduced; all locked decisions mapped in code-level contracts for downstream plans.
</success_criteria>

<output>
After completion, create `.planning/phases/03-inventory-integrity-reservation-control/03-01-SUMMARY.md`
</output>
