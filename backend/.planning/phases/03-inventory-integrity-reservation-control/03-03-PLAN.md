---
phase: 03-inventory-integrity-reservation-control
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - src/API/Inventory/InventoryDtos.cs
  - src/API/Inventory/InventoryEndpoints.cs
  - src/API/ErrorHandling/GlobalExceptionHandler.cs
  - src/API/Program.cs
  - tests/IntegrationTests/Inventory/InventoryEndpointsTests.cs
autonomous: true
requirements:
  - INV-01
  - INV-02
  - INV-03
  - INV-04
must_haves:
  truths:
    - "System returns real-time availability with available/reserved/total per product."
    - "Checkout reservation returns 409 ProblemDetails with available quantity detail when insufficient."
    - "Cancellation/payment-failure release endpoints immediately release reserved stock."
    - "Admin delta adjustment endpoint enforces AdminOnly and records auditable adjustment events."
  artifacts:
    - path: src/API/Inventory/InventoryEndpoints.cs
      provides: public/admin inventory minimal API contracts
      exports: [MapInventoryEndpoints]
    - path: src/API/Inventory/InventoryDtos.cs
      provides: inventory transport contracts for availability/reserve/release/adjust
      min_lines: 80
    - path: tests/IntegrationTests/Inventory/InventoryEndpointsTests.cs
      provides: end-to-end contract verification for 200/409/401/403 behavior
      min_lines: 140
  key_links:
    - from: src/API/Program.cs
      to: src/API/Inventory/InventoryEndpoints.cs
      via: endpoint registration
      pattern: "app.MapInventoryEndpoints\(\)"
    - from: src/API/ErrorHandling/GlobalExceptionHandler.cs
      to: inventory conflict exception mapping
      via: ProblemDetails translation
      pattern: "StatusCodes.Status409Conflict"
---

<objective>
Expose inventory API contracts and complete end-to-end behavior verification for availability, reservation/release, and admin adjustment with conflict-safe ProblemDetails semantics.

Purpose: Deliver user-visible outcomes for INV-01..INV-04 and locked API behaviors D-06, D-09, D-11.
Output: Inventory endpoints/DTOs, exception mapping updates, and integration endpoint test coverage.
</objective>

<execution_context>
@/home/natan/workspace/Site-Tibia/.opencode/get-shit-done/workflows/execute-plan.md
@/home/natan/workspace/Site-Tibia/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-inventory-integrity-reservation-control/03-CONTEXT.md
@.planning/phases/03-inventory-integrity-reservation-control/03-RESEARCH.md
@.planning/phases/03-inventory-integrity-reservation-control/03-01-PLAN.md
@.planning/phases/03-inventory-integrity-reservation-control/03-02-PLAN.md
@src/API/Catalog/CatalogEndpoints.cs
@src/API/Catalog/CatalogDtos.cs
@src/API/ErrorHandling/GlobalExceptionHandler.cs
@src/API/Auth/AuthPolicies.cs
@src/API/Program.cs

<interfaces>
From src/API/Auth/AuthPolicies.cs:
```csharp
public const string AdminOnly = "AdminOnly";
```

From src/API/ErrorHandling/GlobalExceptionHandler.cs:
```csharp
private static (int status, string title, string detail) Map(Exception exception)
```

From plan 03-01 expected output:
```csharp
public sealed class InventoryService
{
    Task<InventoryAvailabilityResponse> GetAvailabilityAsync(Guid productId, CancellationToken cancellationToken = default);
    Task<ReservationResultResponse> ReserveStockForCheckoutAsync(ReserveStockRequest request, CancellationToken cancellationToken = default);
    Task<ReleaseResultResponse> ReleaseReservationAsync(ReleaseReservationRequest request, CancellationToken cancellationToken = default);
    Task<AdjustmentResultResponse> AdjustStockAsync(AdjustStockRequest request, CancellationToken cancellationToken = default);
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement inventory endpoint contracts and conflict ProblemDetails mapping</name>
  <read_first>src/API/Catalog/CatalogEndpoints.cs, src/API/Catalog/CatalogDtos.cs, src/API/ErrorHandling/GlobalExceptionHandler.cs, .planning/phases/03-inventory-integrity-reservation-control/03-01-PLAN.md</read_first>
  <files>src/API/Inventory/InventoryDtos.cs, src/API/Inventory/InventoryEndpoints.cs, src/API/ErrorHandling/GlobalExceptionHandler.cs, src/API/Program.cs, tests/IntegrationTests/Inventory/InventoryEndpointsTests.cs</files>
  <behavior>
    - Test 1: `GET /inventory/{productId}/availability` returns `available`, `reserved`, and `total` fields (D-09).
    - Test 2: Reservation insufficiency/conflict returns HTTP 409 ProblemDetails containing available quantity detail (D-06, D-11).
    - Test 3: Release endpoint for cancellation/payment failure returns successful released quantity (D-03).
  </behavior>
  <action>Create inventory DTOs for availability read, reserve command, release command, and admin adjustment command. Add `MapInventoryEndpoints` with concrete routes: `GET /inventory/{productId}/availability`, `POST /inventory/reservations`, `POST /inventory/reservations/release`, and admin `POST /admin/inventory/adjustments`. Register endpoint mapping in `Program.cs`. Extend `GlobalExceptionHandler` to map inventory conflict exception to 409 ProblemDetails with an extension key `availableQuantity` and stable title `Conflict.`. Write integration tests first for availability payload shape, reserve conflict semantics with actionable detail, and release immediate behavior.</action>
  <acceptance_criteria>
    - `InventoryEndpoints.cs` contains all four routes with exact paths listed in action.
    - `InventoryDtos.cs` includes response contract with `Available`, `Reserved`, and `Total` properties.
    - `GlobalExceptionHandler.cs` maps inventory conflict exception to status 409 and includes `availableQuantity` in ProblemDetails extensions.
    - `InventoryEndpointsTests.cs` asserts 409 response includes available quantity field.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test tests/IntegrationTests/IntegrationTests.csproj --filter "FullyQualifiedName~InventoryEndpointsTests"</automated>
  </verify>
  <done>Inventory API surfaces D-03, D-06, D-09, and D-11 behavior contracts with passing integration tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce admin-only stock adjustment endpoint and final requirement proof suite</name>
  <read_first>src/API/Auth/AuthPolicies.cs, tests/IntegrationTests/Catalog/CatalogAdminEndpointsTests.cs, src/API/Inventory/InventoryEndpoints.cs, .planning/REQUIREMENTS.md</read_first>
  <files>src/API/Inventory/InventoryEndpoints.cs, tests/IntegrationTests/Inventory/InventoryEndpointsTests.cs</files>
  <behavior>
    - Test 1: Unauthenticated and non-admin requests to admin inventory adjustment endpoint are rejected (401/403).
    - Test 2: Admin delta adjustment succeeds, persists audit-visible fields, and availability reflects updated totals (INV-04).
    - Test 3: Oversell prevention at checkout reserve path returns deterministic 409 under contention/insufficiency (INV-03).
  </behavior>
  <action>Apply `RequireAuthorization(AuthPolicies.AdminOnly)` to admin inventory adjustment route group and verify with integration tests mirroring existing catalog admin authorization style. Extend endpoint tests to assert admin happy path adjustment updates availability output and that insufficient reservation attempts are blocked with 409 semantics. Ensure tests use production API inventory DTO contracts (no local anonymous payload drift) to preserve contract fidelity.
</action>
  <acceptance_criteria>
    - `InventoryEndpoints.cs` contains admin group with `RequireAuthorization(AuthPolicies.AdminOnly)`.
    - `InventoryEndpointsTests.cs` contains explicit assertions for 401, 403, and admin 2xx adjustment path.
    - `InventoryEndpointsTests.cs` verifies oversell attempt returns 409 and includes actionable available quantity detail.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test tests/IntegrationTests/IntegrationTests.csproj --filter "FullyQualifiedName~InventoryEndpointsTests" && dotnet test backend.slnx -v minimal</automated>
  </verify>
  <done>INV-01..INV-04 are proven through API-level automated contracts with required auth and conflict semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Inventory API | Untrusted input enters availability, reserve, release, and admin adjustment routes. |
| API exception handler → external response | Internal failure details cross boundary into public ProblemDetails payloads. |
| Auth middleware → admin adjustment endpoint | Role claims boundary for privileged stock mutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | E | Admin inventory adjustment endpoint | mitigate | Gate admin route with `AuthPolicies.AdminOnly` and verify 401/403/2xx integration outcomes. |
| T-03-10 | I | 409 ProblemDetails payload | mitigate | Return only actionable available quantity detail; avoid leaking internal stack/SQL details. |
| T-03-11 | T | Reservation command endpoint | mitigate | Surface deterministic 409 on insufficiency/conflict and rely on transactional reserve path from Plan 02. |
| T-03-12 | R | Adjustment write auditability | mitigate | Ensure adjustment path invokes service/repository audit persistence and verify via integration assertions. |
</threat_model>

<verification>
- dotnet test tests/IntegrationTests/IntegrationTests.csproj --filter "FullyQualifiedName~InventoryEndpointsTests"
- dotnet test backend.slnx -v minimal
- dotnet build backend.slnx -v minimal
</verification>

<success_criteria>
- Inventory API endpoints and tests prove INV-01, INV-02, INV-03, INV-04 with full locked-decision fidelity.
- Conflict semantics, admin authorization, and audit visibility behaviors are objectively verified.
</success_criteria>

<output>
After completion, create `.planning/phases/03-inventory-integrity-reservation-control/03-03-SUMMARY.md`
</output>
