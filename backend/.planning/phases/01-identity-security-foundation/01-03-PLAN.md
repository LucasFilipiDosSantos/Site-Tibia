---
phase: 01-identity-security-foundation
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - src/API/Program.cs
  - src/API/API.csproj
  - src/API/Auth/AuthEndpoints.cs
  - src/API/Auth/AuthDtos.cs
  - src/API/Auth/AuthPolicies.cs
  - src/API/Auth/AuthRateLimitMiddleware.cs
  - src/API/Auth/HttpsSecurityExtensions.cs
  - src/Infrastructure/Persistence/Seed/FirstAdminSeed.cs
  - src/Application/Identity/Services/IdentityService.cs
  - src/Application/Identity/Services/TokenRotationService.cs
  - src/Application/Identity/Services/SecurityAuditService.cs
  - tests/IntegrationTests/IntegrationTests.csproj
  - tests/IntegrationTests/Identity/AuthEndpointContractTests.cs
  - tests/IntegrationTests/Identity/AuthorizationPolicyTests.cs
  - tests/IntegrationTests/Identity/ThrottlingAndLockoutTests.cs
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, SEC-01, SEC-02]
must_haves:
  truths:
    - "User can register/login and refresh sessions with secure policy enforcement"
    - "User can request and complete email verification/password reset tokenized flows"
    - "Admin-only endpoints reject non-admin users and verified gating blocks sensitive actions"
    - "First admin account can only be created through controlled seed/migration path"
    - "Auth/reset endpoints enforce throttling and lockout responses safely"
    - "HTTPS and hash-based credential handling are enforced across auth stack"
  artifacts:
    - path: "src/API/Auth/AuthEndpoints.cs"
      provides: "Identity HTTP surface for register/login/refresh/verify/reset"
    - path: "src/API/Auth/AuthPolicies.cs"
      provides: "AdminOnly and VerifiedForSensitiveActions policies"
    - path: "src/Infrastructure/Persistence/Seed/FirstAdminSeed.cs"
      provides: "Controlled first-admin bootstrap routine"
    - path: "src/API/Auth/AuthRateLimitMiddleware.cs"
      provides: "user+IP throttling and lockout integration"
  key_links:
    - from: "AuthEndpoints"
      to: "IdentityService/TokenRotationService"
      via: "DTO mapping and use-case invocation"
      pattern: "MapPost\("/auth"
    - from: "Program.cs"
      to: "AuthPolicies"
      via: "authorization registration"
      pattern: "AddAuthorization|RequireClaim"
    - from: "FirstAdminSeed"
      to: "UserAccount persistence"
      via: "manual migration/seed execution only"
      pattern: "seed|migration|first admin"
---

<objective>
Expose complete Phase 1 identity/security behavior through API endpoints and middleware/policy wiring.

Purpose: Deliver user-visible auth capabilities and enforce role/verification/security boundaries end-to-end.
Output: auth endpoints, RBAC/verification policies, throttling/lockout + audit wiring, HTTPS hardening, and end-to-end auth tests.
</objective>

<execution_context>
@/home/natan/workspace/Site-Tibia/.opencode/get-shit-done/workflows/execute-plan.md
@/home/natan/workspace/Site-Tibia/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-identity-security-foundation/01-CONTEXT.md
@.planning/phases/01-identity-security-foundation/01-RESEARCH.md
@.planning/phases/01-identity-security-foundation/01-01-PLAN.md
@.planning/phases/01-identity-security-foundation/01-02-PLAN.md
@src/API/Program.cs

<interfaces>
From current API bootstrap:
```csharp
var builder = WebApplication.CreateBuilder(args);
app.UseHttpsRedirection();
```

Contracts available from prior plans:
```csharp
public interface IIdentityService;
public interface ITokenService;
public interface IRefreshSessionRepository;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement auth HTTP contracts and endpoint handlers</name>
  <read_first>
    - src/API/Program.cs
    - src/Application/Identity/Services/IdentityService.cs
    - src/Application/Identity/Services/TokenRotationService.cs
    - .planning/phases/01-identity-security-foundation/01-CONTEXT.md
  </read_first>
  <files>src/API/Auth/AuthDtos.cs, src/API/Auth/AuthEndpoints.cs, src/API/API.csproj, src/API/Program.cs, tests/IntegrationTests/IntegrationTests.csproj, tests/IntegrationTests/Identity/AuthEndpointContractTests.cs</files>
  <behavior>
    - Register/login available before verification but sensitive actions remain blocked by policy (D-04, D-05).
    - Refresh endpoint rotates tokens and revokes prior refresh token immediately (D-02, D-03).
    - Password reset token is one-time and 30-minute expiry enforced (D-06).
  </behavior>
  <action>Create minimal API route group `/auth` with concrete endpoints: `POST /register`, `POST /login`, `POST /refresh`, `POST /verify-email/request`, `POST /verify-email/confirm`, `POST /password-reset/request`, `POST /password-reset/confirm`. Define DTOs with explicit fields and validation rules. Wire endpoints in Program.cs and ensure responses avoid account enumeration leakage. Add endpoint contract tests for success/failure branches aligned to D-04/D-05/D-06 in `tests/IntegrationTests` (not `tests/UnitTests`, per AGENTS.md isolation boundary).</action>
  <acceptance_criteria>
    - `AuthEndpoints.cs` contains all 7 endpoint mappings under `/auth`.
    - `AuthDtos.cs` includes DTOs for register/login/refresh/verification/reset request+confirm.
    - Tests assert refresh response invalidates previous token path and reset confirm rejects expired/consumed token.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test tests/IntegrationTests/IntegrationTests.csproj -v minimal --filter "FullyQualifiedName~AuthEndpointContract"</automated>
  </verify>
  <done>Public identity endpoints satisfy AUTH-01/AUTH-02/AUTH-04 behavior with executable coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce admin/verified access policies and HTTPS hardening</name>
  <read_first>
    - src/API/Program.cs
    - src/API/Auth/AuthEndpoints.cs
    - .planning/ROADMAP.md
    - AGENTS.md
  </read_first>
  <files>src/API/Auth/AuthPolicies.cs, src/API/Auth/HttpsSecurityExtensions.cs, src/API/Program.cs, src/Infrastructure/Persistence/Seed/FirstAdminSeed.cs, tests/IntegrationTests/Identity/AuthorizationPolicyTests.cs</files>
  <behavior>
    - Admin endpoints require strict Admin role claim (D-08).
    - First admin provisioning is available only via controlled seed/migration path (D-07).
    - Sensitive operations require email verified claim (D-04/D-05).
    - HTTPS-only behavior configured for deployed environments (SEC-01).
  </behavior>
  <action>Add policy registration helper with exact policies: `AdminOnly` requiring `role=Admin`, `VerifiedForSensitiveActions` requiring `email_verified=true`. Implement controlled first-admin bootstrap per D-07 using a manual seed/migration path (`FirstAdminSeed`) that inserts initial admin only when no admin exists and is never exposed via public HTTP endpoint. Apply policies to representative protected endpoints in auth group or sample admin route. Add HTTPS extension that enables redirection always and HSTS when environment is not Development. Add tests asserting policy requirement metadata exists and non-compliant claims are rejected.</action>
  <acceptance_criteria>
    - `AuthPolicies.cs` defines `AdminOnly` and `VerifiedForSensitiveActions` with exact claim requirements.
    - `FirstAdminSeed.cs` enforces one-time first-admin creation guarded by explicit manual execution path and no public endpoint exposure.
    - `Program.cs` calls policy registration and HTTPS extension wiring.
    - Authorization tests verify non-admin and unverified principals are denied.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test tests/IntegrationTests/IntegrationTests.csproj -v minimal --filter "FullyQualifiedName~AuthorizationPolicy"</automated>
  </verify>
  <done>RBAC and verification gates enforce AUTH-03 plus SEC-01 transport hardening.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add auth throttling, lockout enforcement, and security audit events</name>
  <read_first>
    - src/API/Program.cs
    - src/API/Auth/AuthEndpoints.cs
    - src/Application/Identity/Services/IdentityService.cs
    - .planning/phases/01-identity-security-foundation/01-CONTEXT.md
  </read_first>
  <files>src/API/Auth/AuthRateLimitMiddleware.cs, src/Application/Identity/Services/IdentityService.cs, src/Application/Identity/Services/TokenRotationService.cs, src/Application/Identity/Services/SecurityAuditService.cs, src/API/Program.cs, tests/IntegrationTests/Identity/ThrottlingAndLockoutTests.cs</files>
  <behavior>
    - Throttle login/reset attempts by both user identifier and IP (D-10).
    - Lockout duration fixed at 15 minutes after repeated failures (D-11).
    - Security-relevant events are recorded for auth/session/lockout flow transitions (D-12).
  </behavior>
  <action>Implement middleware/service hooks that track request counts keyed by `(normalizedUserOrEmail, ipAddress)` for login and password-reset endpoints with deterministic throttle responses. Ensure repeated failed auth calls activate 15-minute lockout and emit audit events for: login_failed, lockout_applied, lockout_released, refresh_rotated, refresh_reuse_blocked, password_reset_requested, password_reset_completed. Wire middleware into Program.cs before auth endpoints and add tests validating throttle trigger and lockout window behavior.</action>
  <acceptance_criteria>
    - `AuthRateLimitMiddleware.cs` contains combined user+IP key strategy for login/reset.
    - `SecurityAuditService.cs` contains event names: `login_failed`, `lockout_applied`, `refresh_rotated`, `password_reset_completed`.
    - `ThrottlingAndLockoutTests.cs` validates request throttling and 15-minute lockout behavior.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test tests/IntegrationTests/IntegrationTests.csproj -v minimal --filter "FullyQualifiedName~ThrottlingAndLockout"</automated>
  </verify>
  <done>Account protection controls satisfy D-10/D-11/D-12 and complete Phase 1 security baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet client → `/auth/*` endpoints | Untrusted credentials, reset requests, and tokens cross into security-sensitive routes |
| Claims principal → protected endpoint policies | Authorization decision boundary for admin/sensitive operations |
| API flow → audit logging | Security event integrity and traceability boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | S | `/auth/login` and `/auth/refresh` | mitigate | Validate credentials/token signatures, block replayed rotated tokens, enforce throttling by user+IP |
| T-01-08 | E | admin/sensitive endpoints | mitigate | Enforce policy-based role/verified claims (`AdminOnly`, `VerifiedForSensitiveActions`) on protected routes |
| T-01-09 | R | security event trail | mitigate | Emit immutable security audit events for auth/session/lockout/reset transitions with timestamp + actor context |
| T-01-10 | I | transport security | mitigate | Apply HTTPS redirection + production HSTS to prevent plaintext credential transit |
</threat_model>

<verification>
Run endpoint/policy/throttle tests, then full solution tests:
- `dotnet test tests/IntegrationTests/IntegrationTests.csproj -v minimal --filter "FullyQualifiedName~AuthEndpointContract|FullyQualifiedName~AuthorizationPolicy|FullyQualifiedName~ThrottlingAndLockout"`
- `dotnet test backend.slnx -v minimal`
</verification>

<success_criteria>
- All Phase 1 requirements (AUTH-01..04, SEC-01..02) are covered by executable auth/security behavior.
- Role/verification boundaries are policy-enforced, not convention-based.
- Throttling, lockout, refresh rotation, and tokenized reset/verification are implemented and tested.
</success_criteria>

<output>
After completion, create `.planning/phases/01-identity-security-foundation/01-03-SUMMARY.md`
</output>
