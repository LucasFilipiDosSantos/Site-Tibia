---
phase: 01-identity-security-foundation
plan: 05
type: execute
wave: 1
depends_on:
  - 01-03
files_modified:
  - src/API/Auth/JwtOptions.cs
  - src/API/Program.cs
  - src/API/appsettings.json
  - src/API/appsettings.Development.json
  - tests/UnitTests/Identity/AdminJwtAuthorizationTests.cs
autonomous: true
requirements:
  - AUTH-03
gap_closure: true
must_haves:
  truths:
    - Admin endpoint accepts valid admin JWT signed with configured key and returns success.
    - Admin endpoint rejects non-admin JWT even when token is otherwise valid.
    - Admin endpoint rejects invalid/mis-signed JWT with unauthorized response.
  artifacts:
    - path: src/API/Program.cs
      provides: JwtBearer runtime validation configured with issuer/audience/signing key.
      contains: AddJwtBearer with TokenValidationParameters
    - path: src/API/appsettings.json
      provides: Jwt validation settings section bound by API host.
      contains: Jwt:Issuer, Jwt:Audience, Jwt:SigningKey
    - path: tests/UnitTests/Identity/AdminJwtAuthorizationTests.cs
      provides: Automated positive+negative authorization behavior checks using real JWT validation pipeline.
      contains: admin success, non-admin forbidden, invalid token unauthorized
  key_links:
    - from: src/API/Program.cs
      to: src/Infrastructure/Identity/Services/JwtTokenService.cs
      via: Same issuer/audience/signing key semantics for issuance and bearer validation
      pattern: "ValidateIssuer|ValidateAudience|ValidateIssuerSigningKey"
    - from: src/API/appsettings.json
      to: src/API/Program.cs
      via: bound Jwt options consumed by AddJwtBearer
      pattern: "Configuration.GetSection\(\"Jwt\"\)"
    - from: tests/UnitTests/Identity/AdminJwtAuthorizationTests.cs
      to: src/API/Auth/AuthEndpoints.cs
      via: GET /auth/admin/probe authorization checks
      pattern: "/auth/admin/probe"
---

<objective>
Close AUTH-03 verification gap by configuring JWT bearer validation in runtime and proving authorized-admin positive path with automated tests.

Purpose: Role policy checks are only meaningful when issued JWTs are cryptographically and semantically validated by the API pipeline.
Output: Bound JWT config + AddJwtBearer validation parameters + admin authorization tests for success/forbidden/unauthorized paths.
</objective>

<execution_context>
@/home/natan/workspace/Site-Tibia/.opencode/get-shit-done/workflows/execute-plan.md
@/home/natan/workspace/Site-Tibia/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-identity-security-foundation/01-VERIFICATION.md
@.planning/phases/01-identity-security-foundation/01-02-SUMMARY.md
@.planning/phases/01-identity-security-foundation/01-03-SUMMARY.md
@src/API/Program.cs
@src/Infrastructure/Identity/Services/JwtTokenService.cs

<interfaces>
From src/Infrastructure/Identity/Services/JwtTokenService.cs:
```csharp
public sealed class JwtTokenServiceOptions
{
    public string Issuer { get; init; }
    public string Audience { get; init; }
    public string SigningKey { get; init; }
}
```

From src/API/Auth/AuthPolicies.cs:
```csharp
public const string AdminOnly = "AdminOnly";
// policy requires claim role=Admin
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add explicit JWT bearer validation configuration in API host</name>
  <files>src/API/Auth/JwtOptions.cs, src/API/Program.cs, src/API/appsettings.json, src/API/appsettings.Development.json</files>
  <behavior>
    - Test 1: token with expected issuer/audience/signing key and role=Admin authenticates successfully.
    - Test 2: token with wrong signing key is rejected as unauthorized.
    - Test 3: token with wrong audience/issuer is rejected as unauthorized.
  </behavior>
  <action>Create API-level Jwt options binding and configure `AddJwtBearer` with explicit `TokenValidationParameters` that validate signing key, issuer, audience, expiration, and clock skew policy. Keep values aligned with token issuance contract in infrastructure JWT service. Do not leave `AddJwtBearer()` in default-only mode. Preserve strict admin claim policy behavior (per D-08).</action>
  <verify>
    <automated>dotnet test tests/UnitTests/UnitTests.csproj -v minimal --filter "FullyQualifiedName~AdminJwtAuthorizationTests.JwtValidation"</automated>
  </verify>
  <done>Runtime authentication pipeline validates JWTs using configured issuer/audience/signing key instead of defaults.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add admin endpoint authorization pipeline tests with real bearer tokens</name>
  <files>tests/UnitTests/Identity/AdminJwtAuthorizationTests.cs</files>
  <behavior>
    - Test 1: valid admin token returns 200 on `/auth/admin/probe`.
    - Test 2: valid non-admin token returns 403 on `/auth/admin/probe`.
    - Test 3: invalid/unsigned/mis-signed token returns 401 on `/auth/admin/probe`.
  </behavior>
  <action>Add integration-style API authorization tests that boot the auth pipeline and assert status-code outcomes for admin probe endpoint. Use real JWT generation aligned to configured settings and include positive authorized-admin case explicitly requested by gap report.</action>
  <verify>
    <automated>dotnet test tests/UnitTests/UnitTests.csproj -v minimal --filter "FullyQualifiedName~AdminJwtAuthorizationTests"</automated>
  </verify>
  <done>Automated tests prove authorized admin access works and non-admin/invalid tokens are blocked as expected.</done>
</task>

<task type="auto">
  <name>Task 3: Re-run focused identity/security regression suite</name>
  <files>tests/UnitTests/Identity/AdminJwtAuthorizationTests.cs</files>
  <action>Execute focused authorization and auth contract tests to confirm JWT validation wiring does not regress existing throttling/policy/security behavior and closes AUTH-03 blocker from verification.</action>
  <verify>
    <automated>dotnet test tests/UnitTests/UnitTests.csproj -v minimal --filter "FullyQualifiedName~AdminJwtAuthorizationTests|FullyQualifiedName~AuthorizationPolicyTests|FullyQualifiedName~AuthEndpointContractTests|FullyQualifiedName~ThrottlingAndLockoutTests"</automated>
  </verify>
  <done>All targeted identity/security tests pass with explicit admin-positive authorization proof in place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Bearer token from untrusted client enters auth middleware and must be validated before principal creation. |
| config→auth middleware | Secrets/issuer/audience from configuration affect runtime trust decisions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05-01 | S | JWT bearer middleware | mitigate | Enable issuer/audience/signature validation and reject invalid/mis-signed tokens before authorization. |
| T-01-05-02 | E | Admin policy boundary | mitigate | Keep `AdminOnly` policy claim requirement and validate with positive/negative endpoint tests. |
| T-01-05-03 | T | JWT config source | mitigate | Bind strongly typed options and fail fast when signing key/issuer/audience are missing or invalid. |
| T-01-05-04 | R | admin access events | accept | Existing structured logs are sufficient for this phase; deeper audit enrichment deferred to Phase 9 admin ops. |
</threat_model>

<verification>
- dotnet test tests/UnitTests/UnitTests.csproj -v minimal --filter "FullyQualifiedName~AdminJwtAuthorizationTests|FullyQualifiedName~AuthorizationPolicyTests|FullyQualifiedName~AuthEndpointContractTests"
- dotnet test backend.slnx -v minimal
</verification>

<success_criteria>
- AUTH-03 gap item "AddJwtBearer validation alignment" is closed by explicit runtime TokenValidationParameters + config binding.
- AUTH-03 gap item "positive authorization test" is closed by automated admin JWT success test.
- Non-admin and invalid token paths remain blocked with correct HTTP semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/01-identity-security-foundation/01-05-SUMMARY.md`
</output>
