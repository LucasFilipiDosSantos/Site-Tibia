---
phase: 01-identity-security-foundation
plan: 04
type: execute
wave: 1
depends_on:
  - 01-03
files_modified:
  - src/Application/Identity/Contracts/IIdentityTokenDelivery.cs
  - src/Application/Identity/Contracts/IIdentityService.cs
  - src/Application/Identity/Services/IdentityService.cs
  - src/Infrastructure/Identity/Services/InMemoryIdentityTokenDelivery.cs
  - src/Infrastructure/DependencyInjection.cs
  - src/API/Auth/AuthDtos.cs
  - src/API/Auth/AuthEndpoints.cs
  - tests/UnitTests/Identity/VerificationAndPasswordResetRoundTripTests.cs
autonomous: true
requirements:
  - AUTH-02
gap_closure: true
must_haves:
  truths:
    - User can request email verification and complete confirmation using a delivered token.
    - User can request password reset and complete reset using a delivered one-time token within expiry.
    - Request endpoints remain enumeration-safe while still enabling a valid end-to-end round-trip.
  artifacts:
    - path: src/Application/Identity/Contracts/IIdentityTokenDelivery.cs
      provides: Explicit delivery port for verification/reset raw tokens generated by IdentityService.
      exports: ["IIdentityTokenDelivery"]
    - path: src/Application/Identity/Services/IdentityService.cs
      provides: Token request methods dispatch generated raw token through delivery port before returning.
      contains: RequestEmailVerificationAsync, RequestPasswordResetAsync
    - path: tests/UnitTests/Identity/VerificationAndPasswordResetRoundTripTests.cs
      provides: Executable proof of request->deliver->confirm round-trip for both flows.
      contains: Email verification and password-reset integration scenarios
  key_links:
    - from: src/Application/Identity/Services/IdentityService.cs
      to: src/Application/Identity/Contracts/IIdentityTokenDelivery.cs
      via: Dispatch call after token hashing/persistence
      pattern: "Deliver(EmailVerification|PasswordReset)TokenAsync"
    - from: src/API/Auth/AuthEndpoints.cs
      to: src/Application/Identity/Services/IdentityService.cs
      via: /auth/verify-email/request and /auth/password-reset/request handlers
      pattern: "Request(EmailVerification|PasswordReset)Async"
    - from: tests/UnitTests/Identity/VerificationAndPasswordResetRoundTripTests.cs
      to: src/API/Auth/AuthEndpoints.cs
      via: HTTP request endpoints then confirm endpoints with delivered token
      pattern: "/auth/(verify-email|password-reset)"
---

<objective>
Close AUTH-02 verification gap by wiring a concrete token delivery path and proving both verification and password-reset token flows complete end-to-end.

Purpose: Users must actually receive usable secure tokens (not only hashed persistence) to finish account recovery/verification.
Output: Delivery contract + implementation wired into identity flows and automated round-trip tests for both tokenized journeys.
</objective>

<execution_context>
@/home/natan/workspace/Site-Tibia/.opencode/get-shit-done/workflows/execute-plan.md
@/home/natan/workspace/Site-Tibia/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-identity-security-foundation/01-VERIFICATION.md
@.planning/phases/01-identity-security-foundation/01-03-SUMMARY.md
@src/Application/Identity/Services/IdentityService.cs
@src/API/Auth/AuthEndpoints.cs

<interfaces>
From src/Application/Identity/Contracts/IIdentityService.cs:
```csharp
public interface IIdentityService
{
    Task RequestEmailVerificationAsync(string email, CancellationToken cancellationToken = default);
    Task<bool> ConfirmEmailVerificationAsync(string token, CancellationToken cancellationToken = default);
    Task RequestPasswordResetAsync(string email, CancellationToken cancellationToken = default);
    Task<bool> ConfirmPasswordResetAsync(string token, string newPassword, CancellationToken cancellationToken = default);
}
```

From src/API/Auth/AuthEndpoints.cs:
```csharp
group.MapPost("/verify-email/request", ...);
group.MapPost("/verify-email/confirm", ...);
group.MapPost("/password-reset/request", ...);
group.MapPost("/password-reset/confirm", ...);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add delivery contract and dispatch generated raw tokens</name>
  <files>src/Application/Identity/Contracts/IIdentityTokenDelivery.cs, src/Application/Identity/Contracts/IIdentityService.cs, src/Application/Identity/Services/IdentityService.cs</files>
  <behavior>
    - Test 1: verification request for an existing user dispatches one token payload while still returning generic success semantics.
    - Test 2: password-reset request for an existing user dispatches one token payload with one-time token purpose and expiry policy.
    - Test 3: unknown email does not dispatch token and does not reveal account existence.
  </behavior>
  <action>Create an application-level delivery port that receives raw token payloads for email verification and password reset, then update `IdentityService` request methods to invoke it after persisting token hash. Keep secure generic-response behavior for unknown users (per D-05), preserve one-time + 30-minute token semantics (per D-06), and keep verification gating intent unchanged (per D-04). Record security audit events for token dispatch attempts consistent with D-12.</action>
  <verify>
    <automated>dotnet test tests/UnitTests/UnitTests.csproj -v minimal --filter "FullyQualifiedName~VerificationAndPasswordResetRoundTripTests.Dispatch"</automated>
  </verify>
  <done>Both request flows dispatch token payloads for valid accounts through an explicit port while retaining non-enumerating request behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire infrastructure delivery sink and keep API request/confirm contracts coherent</name>
  <files>src/Infrastructure/Identity/Services/InMemoryIdentityTokenDelivery.cs, src/Infrastructure/DependencyInjection.cs, src/API/Auth/AuthDtos.cs, src/API/Auth/AuthEndpoints.cs</files>
  <behavior>
    - Test 1: request endpoints continue returning generic success message regardless of account existence.
    - Test 2: delivered verification token can be supplied to confirm endpoint and mark email verified once.
    - Test 3: delivered password-reset token can reset password once and replay is rejected.
  </behavior>
  <action>Implement a deterministic in-memory delivery sink used by tests/runtime wiring, register it in infrastructure DI as `IIdentityTokenDelivery`, and ensure auth endpoint handlers stay aligned with current request/confirm DTO contracts. Do not expose raw tokens directly in HTTP responses. Maintain request endpoint semantics from existing API while enabling round-trip completion through wired delivery channel.</action>
  <verify>
    <automated>dotnet test tests/UnitTests/UnitTests.csproj -v minimal --filter "FullyQualifiedName~AuthEndpointContractTests|FullyQualifiedName~VerificationAndPasswordResetRoundTripTests"</automated>
  </verify>
  <done>Runtime wiring provides a concrete token delivery path without leaking tokens in request responses, and confirm endpoints can now complete with delivered tokens.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add round-trip automated tests for verification and password reset</name>
  <files>tests/UnitTests/Identity/VerificationAndPasswordResetRoundTripTests.cs</files>
  <behavior>
    - Test 1: request verification -> obtain delivered token from sink -> confirm returns success -> second confirm fails.
    - Test 2: request reset -> obtain delivered token from sink -> confirm with compliant password succeeds -> replay fails.
    - Test 3: reset token expiry path fails after configured lifetime boundary.
  </behavior>
  <action>Create integration-style test coverage that exercises full HTTP flow for both tokenized journeys, including one-time use and expiry guarantees. Tests must demonstrate the exact missing behavior called out in 01-VERIFICATION.md is now closed for AUTH-02.</action>
  <verify>
    <automated>dotnet test tests/UnitTests/UnitTests.csproj -v minimal --filter "FullyQualifiedName~VerificationAndPasswordResetRoundTripTests"</automated>
  </verify>
  <done>Automated tests prove request->delivery->confirm works for both verification and password reset with replay/expiry protections.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Untrusted request body and identifier input cross into token request handlers. |
| API→application delivery port | Sensitive raw token crosses service boundary and must not be logged/leaked. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-01 | I | `AuthEndpoints` request handlers | mitigate | Return uniform generic responses for request endpoints to prevent account enumeration (no existence disclosure). |
| T-01-04-02 | T | `IdentityService` token issue path | mitigate | Store only hashed tokens, keep raw token only in transient delivery payload, preserve one-time consume semantics. |
| T-01-04-03 | R | token request/dispatch flow | mitigate | Emit structured audit events for request/dispatch attempts (D-12) with actor/email/IP metadata. |
| T-01-04-04 | D | verification/reset request endpoints | mitigate | Keep throttling middleware in path and preserve user+IP key strategy from Phase 1 controls (D-10/D-11). |
</threat_model>

<verification>
- dotnet test tests/UnitTests/UnitTests.csproj -v minimal --filter "FullyQualifiedName~VerificationAndPasswordResetRoundTripTests|FullyQualifiedName~AuthEndpointContractTests"
- dotnet test backend.slnx -v minimal
</verification>

<success_criteria>
- AUTH-02 gap item "token handoff path" is closed by concrete delivery port + runtime implementation.
- AUTH-02 gap item "request->receive token->confirm" is closed by automated round-trip tests for both flows.
- Verification and reset request endpoints remain non-enumerating while enabling successful confirm completion.
</success_criteria>

<output>
After completion, create `.planning/phases/01-identity-security-foundation/01-04-SUMMARY.md`
</output>
