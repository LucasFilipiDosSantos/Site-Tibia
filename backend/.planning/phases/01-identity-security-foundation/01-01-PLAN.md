---
phase: 01-identity-security-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Domain/Identity/UserAccount.cs
  - src/Domain/Identity/RefreshSession.cs
  - src/Domain/Identity/SecurityPolicy.cs
  - src/Application/Identity/Contracts/IIdentityService.cs
  - src/Application/Identity/Contracts/ITokenService.cs
  - src/Application/Identity/Contracts/IUserRepository.cs
  - src/Application/Identity/Contracts/IRefreshSessionRepository.cs
  - src/Application/Identity/Services/IdentityService.cs
  - src/Application/Identity/Services/TokenRotationService.cs
  - tests/UnitTests/Identity/PasswordPolicyTests.cs
  - tests/UnitTests/Identity/TokenRotationTests.cs
  - tests/UnitTests/Identity/LockoutPolicyTests.cs
autonomous: true
requirements: [AUTH-01, AUTH-04, SEC-02]
must_haves:
  truths:
    - "User can authenticate with short-lived access credentials and renewable refresh session"
    - "Refresh token reuse is blocked after successful rotation"
    - "Weak passwords and repeated failed logins are rejected by policy"
  artifacts:
    - path: "src/Application/Identity/Services/IdentityService.cs"
      provides: "Registration/login policy orchestration"
    - path: "src/Application/Identity/Services/TokenRotationService.cs"
      provides: "Refresh rotation + immediate old-token revocation"
    - path: "tests/UnitTests/Identity/TokenRotationTests.cs"
      provides: "Executable proof of rotation invariants"
  key_links:
    - from: "IdentityService"
      to: "IUserRepository"
      via: "credential verification and lockout persistence"
      pattern: "RecordFailedLogin|ResetFailedLogin"
    - from: "TokenRotationService"
      to: "IRefreshSessionRepository"
      via: "rotate and revoke previous refresh record"
      pattern: "RevokeCurrentAndInsertNext"
---

<objective>
Establish identity domain/application contracts and core security logic so later infrastructure/API work plugs into deterministic auth rules.

Purpose: Implement non-negotiable token/password/lockout invariants early to prevent security drift in outer layers.
Output: Domain identity models, application contracts/services, and unit tests proving D-01/D-02/D-03/D-09/D-11 behavior.
</objective>

<execution_context>
@/home/natan/workspace/Site-Tibia/.opencode/get-shit-done/workflows/execute-plan.md
@/home/natan/workspace/Site-Tibia/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-identity-security-foundation/01-CONTEXT.md
@.planning/phases/01-identity-security-foundation/01-RESEARCH.md
@src/API/Program.cs

<interfaces>
From src/API/Program.cs:
```csharp
var builder = WebApplication.CreateBuilder(args);
builder.Services.AddOpenApi();
app.UseHttpsRedirection();
```

Plan 01 creates contracts consumed by later plans:
```csharp
public interface IIdentityService;
public interface ITokenService;
public interface IUserRepository;
public interface IRefreshSessionRepository;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define identity contracts and security domain models</name>
  <read_first>
    - .planning/phases/01-identity-security-foundation/01-CONTEXT.md
    - .planning/research/ARCHITECTURE.md
    - src/Application/Application.csproj
    - src/Domain/Domain.csproj
  </read_first>
  <files>src/Domain/Identity/UserAccount.cs, src/Domain/Identity/RefreshSession.cs, src/Domain/Identity/SecurityPolicy.cs, src/Application/Identity/Contracts/IIdentityService.cs, src/Application/Identity/Contracts/ITokenService.cs, src/Application/Identity/Contracts/IUserRepository.cs, src/Application/Identity/Contracts/IRefreshSessionRepository.cs</files>
  <behavior>
    - Password policy enforces minimum 10 characters with uppercase/lowercase/digit/special (D-09).
    - Lockout duration is 15 minutes after repeated failed login threshold (D-11).
    - Token policy constants define access lifetime = 15 minutes and refresh lifetime = 30 days (D-01, D-02).
  </behavior>
  <action>Create domain models and contracts with concrete constants: AccessTokenLifetimeMinutes=15, RefreshTokenLifetimeDays=30, LockoutMinutes=15, PasswordMinLength=10. Include explicit method signatures for login success/failure mutation and refresh-session rotation hooks used by later plans (per D-01/D-02/D-09/D-11). Do not add ASP.NET/EF references in Domain/Application layers.</action>
  <acceptance_criteria>
    - `src/Domain/Identity/SecurityPolicy.cs` contains `AccessTokenLifetimeMinutes = 15` and `RefreshTokenLifetimeDays = 30`.
    - `src/Domain/Identity/SecurityPolicy.cs` contains `LockoutDurationMinutes = 15` and `PasswordMinLength = 10`.
    - Contract files under `src/Application/Identity/Contracts/` compile with no references to `Microsoft.AspNetCore` or `Microsoft.EntityFrameworkCore`.
  </acceptance_criteria>
  <verify>
    <automated>dotnet build backend.slnx</automated>
  </verify>
  <done>Identity domain and contract layer exists with fixed security values and clean architecture boundaries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement auth core services and prove invariants with unit tests</name>
  <read_first>
    - src/Application/Identity/Contracts/IIdentityService.cs
    - src/Application/Identity/Contracts/ITokenService.cs
    - tests/UnitTests/UnitTest1.cs
    - tests/UnitTests/UnitTests.csproj
  </read_first>
  <files>src/Application/Identity/Services/IdentityService.cs, src/Application/Identity/Services/TokenRotationService.cs, tests/UnitTests/Identity/PasswordPolicyTests.cs, tests/UnitTests/Identity/TokenRotationTests.cs, tests/UnitTests/Identity/LockoutPolicyTests.cs</files>
  <behavior>
    - Rotation invalidates old refresh token immediately after issuing new token (D-03).
    - Reusing rotated token fails deterministically.
    - Failed login increments counter and applies 15-minute lockout.
  </behavior>
  <action>Implement application services that enforce security policy and repository-driven state changes: password policy validation, failed-login/lockout orchestration, refresh rotation with `RevokeCurrentAndInsertNext` semantics (D-03). Write unit tests first for password rejection/acceptance, lockout timing, and rotation replay rejection; then implement minimum code to pass tests.</action>
  <acceptance_criteria>
    - `tests/UnitTests/Identity/TokenRotationTests.cs` includes a test asserting old token cannot be used after rotation.
    - `tests/UnitTests/Identity/LockoutPolicyTests.cs` includes assertion for 15-minute lockout window.
    - `tests/UnitTests/Identity/PasswordPolicyTests.cs` includes failing cases for length/complexity violations and passing case for compliant password.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test tests/UnitTests/UnitTests.csproj -v minimal --filter "FullyQualifiedName~Identity"</automated>
  </verify>
  <done>Core identity logic is executable and protected by fast unit tests aligned with D-01/D-02/D-03/D-09/D-11.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API client → auth use case | Untrusted credentials and tokens enter identity flow |
| Application services → repositories | Security state persisted across requests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | S | `IdentityService` login path | mitigate | Enforce password verification + lockout thresholds in application service before issuing tokens |
| T-01-02 | R | token rotation flow | mitigate | Persist revoke+rotate sequence with explicit repository contract and test replay rejection |
| T-01-03 | E | role/sensitive claims surface | mitigate | Include strict role and email-verified claim contract in token service interface for downstream policy checks |
</threat_model>

<verification>
Run `dotnet build backend.slnx` then `dotnet test tests/UnitTests/UnitTests.csproj -v minimal --filter "FullyQualifiedName~Identity"`.
</verification>

<success_criteria>
- Domain/Application layers provide stable identity contracts for downstream implementation.
- Unit tests fail on policy violations and pass on compliant behavior.
- Rotation replay and lockout invariants are enforced by code, not docs.
</success_criteria>

<output>
After completion, create `.planning/phases/01-identity-security-foundation/01-01-SUMMARY.md`
</output>
