---
phase: 01-identity-security-foundation
plan: 08
type: execute
wave: 5
depends_on:
  - 01-07
files_modified:
  - src/Infrastructure/Identity/Options/IdentityTokenDeliveryOptionsValidator.cs
  - src/Infrastructure/DependencyInjection.cs
  - src/Application/Identity/Exceptions/TokenDeliveryUnavailableException.cs
  - src/Application/Identity/Services/SecurityAuditService.cs
  - src/Application/Identity/Services/IdentityService.cs
  - src/API/ErrorHandling/GlobalExceptionHandler.cs
  - tests/IntegrationTests/Identity/ExternalTokenDeliveryRoundTripTests.cs
autonomous: true
requirements:
  - AUTH-02
gap_closure: true
user_setup:
  - service: smtp
    why: "Real token dispatch in UAT/runtime"
    env_vars:
      - name: IdentityTokenDelivery__Provider
        source: "Runtime environment variable (set to smtp only when endpoint is reachable)"
      - name: IdentityTokenDelivery__Smtp__Host
        source: "SMTP provider dashboard or local SMTP service hostname"
      - name: IdentityTokenDelivery__Smtp__Port
        source: "SMTP provider port"
      - name: IdentityTokenDelivery__Smtp__Username
        source: "SMTP provider credential"
      - name: IdentityTokenDelivery__Smtp__Password
        source: "SMTP provider credential"
      - name: IdentityTokenDelivery__Smtp__FromEmail
        source: "Verified sender mailbox in provider"
must_haves:
  truths:
    - "Verify-email and password-reset request endpoints keep generic success responses even when SMTP transport is unavailable (no unhandled exception leakage)."
    - "Runtime fails fast with actionable error when SMTP provider is selected with placeholder/invalid host settings."
    - "When SMTP transport is healthy, verify/reset token round-trip still confirms exactly once and rejects replay."
  artifacts:
    - path: src/Infrastructure/Identity/Options/IdentityTokenDeliveryOptionsValidator.cs
      provides: SMTP provider guardrails for placeholder/invalid config.
      contains: Validate logic for Provider=smtp host/credential sanity checks
    - path: src/Application/Identity/Exceptions/TokenDeliveryUnavailableException.cs
      provides: Application-level abstraction for delivery failures without leaking transport details.
      contains: Exception type carrying generic response contract intent
    - path: src/API/ErrorHandling/GlobalExceptionHandler.cs
      provides: Safe mapping from token-delivery transport failure to enumeration-safe request contract.
      contains: explicit TokenDeliveryUnavailableException mapping branch
    - path: tests/IntegrationTests/Identity/ExternalTokenDeliveryRoundTripTests.cs
      provides: Regression proof for both healthy SMTP path and transport-failure generic-response path.
      contains: failing transport tests for /auth/verify-email/request and /auth/password-reset/request
  key_links:
    - from: src/Application/Identity/Services/IdentityService.cs
      to: src/API/ErrorHandling/GlobalExceptionHandler.cs
      via: TokenDeliveryUnavailableException thrown on provider dispatch failures
      pattern: RequestEmailVerificationAsync|RequestPasswordResetAsync + catch/throw
    - from: src/Infrastructure/DependencyInjection.cs
      to: src/Infrastructure/Identity/Options/IdentityTokenDeliveryOptionsValidator.cs
      via: options validation at startup for IdentityTokenDelivery:Provider=smtp
      pattern: AddOptions<IdentityTokenDeliveryOptions>.*ValidateOnStart
    - from: tests/IntegrationTests/Identity/ExternalTokenDeliveryRoundTripTests.cs
      to: src/API/Auth/AuthEndpoints.cs
      via: HTTP request contract assertions for generic success responses under transport failure
      pattern: /auth/verify-email/request|/auth/password-reset/request
---

<objective>
Close UAT blockers for tests 4 and 5 by preventing SMTP transport faults from surfacing as unhandled request errors, while enforcing fail-fast SMTP configuration guardrails.

Purpose: AUTH-02 remains blocked in runtime/UAT because placeholder SMTP configuration and transport failures currently bubble to 500 and leak operational failure details.
Output: Guardrailed SMTP configuration validation + safe token-delivery failure mapping + integration regression proof for both verify-email and password-reset request flows.
</objective>

<execution_context>
@/home/natan/workspace/Site-Tibia/.opencode/get-shit-done/workflows/execute-plan.md
@/home/natan/workspace/Site-Tibia/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-identity-security-foundation/01-CONTEXT.md
@.planning/phases/01-identity-security-foundation/01-HUMAN-UAT.md
@.planning/phases/01-identity-security-foundation/01-07-SUMMARY.md
@src/Application/Identity/Services/IdentityService.cs
@src/API/ErrorHandling/GlobalExceptionHandler.cs
@src/Infrastructure/DependencyInjection.cs
@src/Infrastructure/Identity/Services/SmtpClientTokenTransport.cs
@tests/IntegrationTests/Identity/ExternalTokenDeliveryRoundTripTests.cs

<interfaces>
From src/Application/Identity/Services/IdentityService.cs:
```csharp
public Task RequestEmailVerificationAsync(string email, CancellationToken cancellationToken = default);
public Task RequestPasswordResetAsync(string email, CancellationToken cancellationToken = default);
```

From src/API/ErrorHandling/GlobalExceptionHandler.cs:
```csharp
private static (int status, string title, string detail) Map(Exception exception)
```

From src/Infrastructure/Identity/Options/IdentityTokenDeliveryOptions.cs:
```csharp
public sealed class IdentityTokenDeliveryOptions
{
    public const string SectionName = "IdentityTokenDelivery";
    public string Provider { get; set; } = "smtp";
    public IdentityTokenDeliverySmtpOptions Smtp { get; set; } = new();
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add SMTP runtime guardrails for placeholder/invalid provider config</name>
  <files>src/Infrastructure/Identity/Options/IdentityTokenDeliveryOptionsValidator.cs, src/Infrastructure/DependencyInjection.cs, tests/IntegrationTests/Identity/ExternalTokenDeliveryRoundTripTests.cs</files>
  <behavior>
    - Test 1: Startup/options validation rejects smtp provider when host is known placeholder (`smtp.dev.local`, `smtp.example.com`) or required credentials are placeholder values.
    - Test 2: Validation allows smtp provider when host and credentials are explicitly non-placeholder and required fields are set.
    - Test 3: Existing external-delivery round-trip tests keep passing with valid test configuration.
  </behavior>
  <action>Implement an `IValidateOptions&lt;IdentityTokenDeliveryOptions&gt;` validator and wire it in DI to fail fast at startup for invalid SMTP config (per D-12 operational auditability and UAT gap missing item: guardrails). Keep provider routing unchanged from plan 01-07, but replace broad inline checks with reusable validator logic producing deterministic error messages. Explicitly treat placeholder hosts/credentials currently in appsettings as invalid for SMTP runtime, requiring environment override before using provider `smtp`.</action>
  <verify>
    <automated>dotnet test tests/IntegrationTests/IntegrationTests.csproj -v minimal --filter "FullyQualifiedName~ExternalTokenDeliveryRoundTripTests.Adapter_Constructor_WithMissingSmtpSettings_ThrowsDeterministicError|FullyQualifiedName~ExternalTokenDeliveryRoundTripTests"</automated>
  </verify>
  <done>Runtime cannot start with `Provider=smtp` and placeholder SMTP settings; failure message is deterministic and actionable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Map token-delivery transport failures to safe generic request contracts</name>
  <files>src/Application/Identity/Exceptions/TokenDeliveryUnavailableException.cs, src/Application/Identity/Services/SecurityAuditService.cs, src/Application/Identity/Services/IdentityService.cs, src/API/ErrorHandling/GlobalExceptionHandler.cs</files>
  <behavior>
    - Test 1: If token delivery transport throws during verify-email request, API returns generic success contract instead of unhandled 500.
    - Test 2: If token delivery transport throws during password-reset request, API returns generic success contract instead of unhandled 500.
    - Test 3: Delivery failure path records a dedicated audit event (D-12) without logging raw token value.
  </behavior>
  <action>Create an application exception for token-delivery unavailability and update `IdentityService.RequestEmailVerificationAsync` / `RequestPasswordResetAsync` to catch provider transport failures, record dedicated audit events (per D-12), and throw only the application-level exception (no transport detail leakage). Update `GlobalExceptionHandler` mapping so this exception returns endpoint-appropriate generic success semantics for request flows, preserving D-05 enumeration-safe behavior and preventing user-visible SMTP failure leakage. Keep confirm endpoints and D-06 one-time token rules unchanged.</action>
  <verify>
    <automated>dotnet test tests/IntegrationTests/IntegrationTests.csproj -v minimal --filter "FullyQualifiedName~ExternalTokenDeliveryRoundTripTests.External_VerificationRequest_WhenTransportFails_ReturnsGenericSuccess|FullyQualifiedName~ExternalTokenDeliveryRoundTripTests.External_PasswordResetRequest_WhenTransportFails_ReturnsGenericSuccess"</automated>
  </verify>
  <done>Verify/reset request endpoints no longer surface SMTP exceptions; they return generic success while preserving auditability and security posture.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expand integration coverage for healthy + failure delivery paths</name>
  <files>tests/IntegrationTests/Identity/ExternalTokenDeliveryRoundTripTests.cs</files>
  <behavior>
    - Test 1: Healthy path still supports request→captured token→single confirm success→replay rejection for verify-email.
    - Test 2: Healthy path still supports request→captured token→single confirm success→replay rejection for password reset.
    - Test 3: Faulted transport path returns generic success payload for both request endpoints and does not break anti-enumeration parity.
  </behavior>
  <action>Add dedicated integration tests with two transport doubles: one capturing transport (existing) and one faulting transport that throws deterministic SMTP-like exceptions. Assert that healthy flows remain unchanged per D-04 and D-06, and faulted flows remain generic/safe per D-05 (status 200 + generic body parity with existing/unknown email behavior). Keep tests in `tests/IntegrationTests` only to preserve AGENTS.md isolation rules.</action>
  <verify>
    <automated>dotnet test tests/IntegrationTests/IntegrationTests.csproj -v minimal --filter "FullyQualifiedName~ExternalTokenDeliveryRoundTripTests" && dotnet test tests/UnitTests/UnitTests.csproj -v minimal --filter "FullyQualifiedName~VerificationAndPasswordResetRoundTripTests|FullyQualifiedName~AdminJwtAuthorizationTests|FullyQualifiedName~TokenRotationTests"</automated>
  </verify>
  <done>Automated suite proves UAT blocker behavior is closed without regressing verify/reset security invariants.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → SMTP transport | Raw token dispatch crosses process boundary into external SMTP provider/network. |
| Runtime config → DI options | Untrusted/misconfigured env/appsettings values determine security-critical delivery behavior. |
| Failure mapping → HTTP response | Internal transport exceptions cross boundary into public API contract responses. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08-01 | I | `GlobalExceptionHandler` delivery-failure mapping | mitigate | Return generic success contract for token-delivery unavailability to avoid leaking SMTP/network internals in client responses. |
| T-01-08-02 | T | `IdentityTokenDeliveryOptions` runtime config | mitigate | Add startup validator rejecting placeholder/invalid SMTP values and requiring explicit non-placeholder overrides for `Provider=smtp`. |
| T-01-08-03 | R | `IdentityService` delivery failure path | mitigate | Record explicit audit events for delivery failure attempts (D-12) with user/email metadata and timestamp, excluding raw tokens. |
| T-01-08-04 | D | SMTP network dependency | accept | Endpoint availability is preserved via safe response contract; actual delivery reliability/retries handled in Phase 7 async/retry requirements. |
</threat_model>

<verification>
- dotnet test tests/IntegrationTests/IntegrationTests.csproj -v minimal --filter "FullyQualifiedName~ExternalTokenDeliveryRoundTripTests"
- dotnet test tests/UnitTests/UnitTests.csproj -v minimal --filter "FullyQualifiedName~VerificationAndPasswordResetRoundTripTests|FullyQualifiedName~AdminJwtAuthorizationTests|FullyQualifiedName~TokenRotationTests"
- dotnet build backend.slnx
</verification>

<success_criteria>
- UAT verify/reset request paths no longer emit unhandled SMTP exceptions and preserve generic response contracts.
- SMTP provider mode cannot run with placeholder host/credential configuration.
- Existing one-time verify/reset confirm semantics remain intact and test-proven.
</success_criteria>

<output>
After completion, create `.planning/phases/01-identity-security-foundation/01-08-SUMMARY.md`
</output>
